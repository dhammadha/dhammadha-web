# DHAMMADHA — project.md (จุดเริ่มอ่านตอนเปิด session ใหม่)

> ไฟล์นี้คือ**แผนที่** ไม่ใช่แหล่งความจริงทั้งหมด — อ่านที่นี่ก่อนเพื่อรู้ว่า
> "ตอนนี้อยู่ตรงไหน" แล้วค่อยกดลิงก์ไปอ่านรายละเอียดเฉพาะจุดที่ต้องใช้จริง
> ไม่ต้องไล่อ่านประวัติแชทเก่า

## โปรเจกต์นี้คืออะไร

แพลตฟอร์มตลาดฟอนต์ไทย (dhammadha.com) — designer ฝากขายฟอนต์ ลูกค้าซื้อ/ขอใบเสนอราคา
รายได้หลัก B2C ขายรายฟอนต์ (designer 75% / เว็บ 25%), B2B quote เป็นเครื่องมือฟรีให้ designer
(เงินเข้า designer ตรง), subscription เป็นแผนอนาคต — รายละเอียดโมเดลธุรกิจเต็ม ๆ ดู
[docs/ROADMAP.md](docs/ROADMAP.md) หัวข้อ "ภาพรวมธุรกิจ"

## Stack + สถาปัตยกรรม (สรุป — เต็ม ๆ ดู [CLAUDE.md](CLAUDE.md))

Next.js `output:"export"` (**ไม่มี server runtime จริง**) → Cloudflare Pages ·
Supabase (Postgres+RLS+Storage เป็นแนวป้องกันจริง ไม่ใช่การซ่อนปุ่ม) · จุดที่ต้องมี server
ใช้ Pages Functions เฉพาะจุด (`/api/send-email`, `/api/checkout`, `/api/stripe-webhook`)
Publish ฟอนต์ = rebuild ทั้งเว็บ (~2 นาที เพราะหน้า detail เป็น SSG)

## สถานะปัจจุบัน (25 ก.ค. 2026)

| ส่วน | สถานะ |
|---|---|
| Backend Phase 0–3 (ซ่อมช่องโหว่ / go-live / quote-to-cash manual / Stripe checkout) | ✅ โค้ดเสร็จ + apply DB จริงแล้ว |
| Backend Phase 4.2–4.3 (Revenue & Payout / Analytics + Search) | ✅ โค้ดเสร็จ + apply DB จริงแล้ว |
| Backend Phase 4.1 (Subscription) | Milestone **A+B เสร็จ · C1 ครบทั้งชุด (C1 → C1b → C1c → C1d → C1e)** = ฝั่ง server พร้อมใช้ทั้งเส้น · เหลือ **C2 (Tauri desktop app)** และ **D (เปิด trial)** · **C2 ต้องมี Rust ในเครื่อง (ยังไม่ได้ติดตั้ง) และควรเคาะชื่อแบรนด์ก่อน** เพราะ `BRAND_DOMAIN` ถูกประทับลงไฟล์ฟอนต์ที่ส่งออก แก้ย้อนหลังไม่ได้ · ราคา/กติกายังรอข้อมูลยอดขายจริงก่อนเคาะ |
| **Subscription C1 — Edge Function `sub-font`** | ✅ โค้ดเสร็จ + deploy จริงแล้ว (3 ส.ค. 2569, **ไม่แตะ DB**) — 4 actions: `status`/`list`/`download`/`heartbeat` · **ทำได้ระหว่างรอ go-live เพราะช่วงทดสอบเป็น ฿0 สมัครตรงผ่าน RPC ไม่แตะ payment gateway** · ย้าย `stamp.ts` → `supabase/functions/_shared/` ใช้ร่วมกับ `download-font` (redeploy ทั้งคู่แล้ว เทียบ byte ก่อน/หลังเหมือนเดิมทุกอย่าง) · **🔴 `sub-font` มีสำเนา `BRAND_DOMAIN` ของตัวเองอีกชุด — เปลี่ยนชื่อแบรนด์ต้อง redeploy สอง function ไม่ใช่หนึ่ง** · **admin ใช้แอปได้ไม่ต้องสมัคร แต่ font-days ไม่นับเงิน** — การกรองอยู่ที่ RPC `subscription_month_data` ไม่ใช่ที่ Edge Function (พิสูจน์แล้ว: heartbeat ของ admin → `contributing_users: 0` ส่วน subscriber จริง → นับเข้า 2 ฟอนต์ share 0.5/0.5) · **วันของ `stream_days` ต้องเป็น Asia/Bangkok ไม่ใช่ UTC** ไม่งั้นช่วง 00:00–07:00 น. ไทยถูกนับเป็นเมื่อวาน · stamp ของ subscription ไม่มี `order_no`/`verify_token` ใช้ user id แทน = audit trail ไม่ใช่ DRM · ทดสอบด้วย JWT จริงครบทุก action แล้วล้างข้อมูลทดสอบทิ้ง |
| **Subscription C1e — 38% คิดจากระยะเวลาจริงแล้ว** | ✅ โค้ดเสร็จ + apply DB (`0082`, 3 ส.ค. 2569) — สลับตัวคิดเงินจาก "จำนวนวันที่เคยเปิด" เป็น **"ระยะเวลาที่เปิดใช้จริง"** ตามที่เจ้าของเคาะ · RPC `subscription_month_data` อ่านจาก `stream_sessions` เป็นวินาที ตัดขอบเดือนด้วย `least`/`greatest` (session คร่อมเดือนถูกแบ่งให้ถูกเดือน — ปัญหาที่ font-days ไม่เคยมี) · `font_days` → `font_seconds` ทั้ง RPC/lib/UI · **สูตรถ่วงน้ำหนัก `sum(x/total)/N` ไม่เปลี่ยนแม้แต่ตัวอักษรเดียว** เปลี่ยนแค่หน่วยของ `x` · **🔴 ขอบเดือนยึดเวลาไทยทั้ง RPC แล้ว** ของเดิม `revenue`/`subscriber_count` ใช้เที่ยงคืน UTC = 07:00 น.ไทย เหลื่อม 7 ชม. ยังไม่เคยแสดงผลเพราะไม่มี subscription จริง แก้ตอนนี้ได้ฟรี · สัญญา designer ข้อ 4 เปลี่ยนเป็น "ระยะเวลา" + **เพิ่มการเปิดเผยข้อจำกัดการวัด** (ขาดการติดต่อ → นับถึงการแจ้งครั้งสุดท้าย ไม่ใช่ realtime) กันข้อพิพาท · **ไม่ต้อง `legal:freeze`** เพราะคนเดียวที่เซ็นคือ `montonn@outlook.com` (552 Studio) ซึ่งเป็นบัญชีเจ้าของเอง · **⚠️ `stream_days` เลิกเป็นแหล่งเงินแล้วแต่ยังเขียนอยู่ ห้ามลบ** — หน้า `/admin/members` ใช้เป็นสัญญาณ "โหลดมากแต่ใช้จริงน้อย" · **พิสูจน์ตัวเลขตรงกับที่คำนวณมือทุกตัว:** เคสเจ้าของ A/B → 0.500000 / 0.05×10 = 0.500000 / รวม 1.000000 · คร่อมเดือน 32 ชม. → 4+28 · 03:00 น.ไทยเข้าเดือนถูก · session เปิดค้างนับถึง `last_seen_at` · redact `0057` ยังครบ |
| **Subscription — สถานะ "Subscription Exclusive" + ถ่วงน้ำหนักสองชั้น** | ✅ โค้ดเสร็จ + apply DB (`0083`–`0085`, 4 ส.ค. 2569) — ฟอนต์ `is_sub_exclusive` **ไม่จำหน่ายรายชุด** ให้บริการเฉพาะสมาชิก (ใบเสนอราคายังขอได้ เพราะราคาสิทธิ์องค์กรมาจาก license tier ไม่ใช่ `fonts.price`) และได้น้ำหนัก **1.25 เท่าใน pool 38%** · **flag ที่สอง ไม่ใช่ตัวแทน `is_subscription`** — check constraint บังคับว่า exclusive ต้องอยู่ใน subscription และห้ามเป็นฟอนต์ฟรี · **🔴 ตัวคูณต้องเข้าก่อนสร้างตัวหารรายคนเสมอ** ไม่งั้น `sum(stream_share) > 1` แล้วเงินส่วนเกินไปโผล่เป็น `platformAmount` ติดลบ **โดยไม่มี error** · ผลพลอยได้ที่ยืนยันว่าสูตรถูก: ถ้าทุกฟอนต์เป็น exclusive ส่วนแบ่งเท่าเดิมเป๊ะ (ทดสอบจริง 3 กรณี baseline/บางตัว/ทุกตัว ผลรวม share = 1.00000000 ทุกกรณี · `font_seconds` ยังเป็นวินาทีจริงไม่ถูกคูณ) · **pool 12% เปลี่ยนจาก "หารด้วยจำนวนฟอนต์ ณ ตอนเรียก" เป็น "เฉลี่ยตามจำนวนวันที่อยู่ในแผนจริง"** ผ่านตาราง `font_subscription_periods` + trigger บน `fonts` (นิยาม "อยู่ในแผน" = `is_subscription and is_active` **ชุดเดียวกับที่ `sub-font` ใช้ตัดสินสิทธิ์** — เกณฑ์สิทธิ์กับเกณฑ์เงินต้องเป็นอันเดียวกัน) ปิดช่องเข้าแผนวันที่ 30 แล้วได้เต็มเดือน และหยุดอาการตัวเลขเดือนเก่าขยับเวลามีคน toggle · client ต้องอ่าน `equal_share` จาก RPC ห้ามหารเอง · ด่านกันขายรายชุดอยู่ที่ `checkout-service` (`is_sub_exclusive=eq.false`) ยิง API ตรงได้ `font_not_found` · สัญญา designer ข้อ 4 แก้ครบทั้งเรื่องน้ำหนักและการเฉลี่ยตามวัน · **⚠️ กับดัก: `subscription_month_data` เรียกซ้ำใน transaction เดียวไม่ได้** (temp table `on commit drop` ชนกันเอง) — ไม่กระทบ production เพราะ client เรียกครั้งละ request แต่เขียนสคริปต์ทดสอบต้องแยก transaction |
| **Subscription C1d — เก็บช่วงเวลาการใช้งานจริง (`stream_sessions`)** | ✅ โค้ดเสร็จ + apply DB (`0081`, 3 ส.ค. 2569) — เจ้าของต้องการให้ **ทุกการเคลื่อนไหวมีข้อมูลมาคำนวณรายได้** เริ่มนับตอน activate หยุดตอน deactivate/signout/เครื่องดับ/ออฟไลน์ นับต่อเมื่อกลับมา · **สูตรเดียวที่ต้องจำ: `coalesce(ended_at, last_seen_at) - started_at`** ปิดสวย ๆ ได้เวลาเป๊ะ เครื่องดับได้ถึงแค่ heartbeat ครั้งสุดท้าย → **ไม่มีทางนับเกินความจริง และไม่ต้องมี cron ไล่ปิด session ค้าง** · **🔴 รูที่ปิดไปด้วย: `session_start` เขียน `stream_days` ทันทีไม่รอ heartbeat** เดิม activate 09:05 → deactivate 11:00 → heartbeat รอบถัดไป 15:00 ฟอนต์นั้นไม่อยู่ในรายการแล้ว = **ไม่เคยถูกบันทึกเลย เงินรั่วเงียบ ๆ ไม่มี error ให้เห็น** (เอกสาร `PHASE-4.1` เขียนตกไปว่า heartbeat ต้องยิงตอน activation เปลี่ยนด้วย แก้แล้ว) · `session_end` ไม่ติดประตู active device โดยตั้งใจ เครื่องที่ถูกแย่งสิทธิ์ต้องปิดของตัวเองได้ · `claim_activation` ปิด session เครื่องอื่นให้ (`switched`) · heartbeat ซ่อมตัวเองถ้าไม่มี session ค้าง · `at` รับคิวออฟไลน์ย้อนหลัง บีบด้วย `clampTime()` · **🔴 ยังไม่ใช้คิดเงินโดยเจตนา** — 38% ยังคิดตาม `stream_days` เพราะสัญญา designer ข้อ 4 เขียนว่า "นับจากจำนวนวัน" **RPC `subscription_month_data` ไม่ถูกแตะเลย** เก็บข้อมูลจริง 2-3 เดือนค่อยเคาะ · พิสูจน์ 11 ข้อ + ตรวจตัวเลขจริง: ย้อนหลัง 3 ชม.→180.0 นาที · ย้อนหลัง 60 วัน→บีบเหลือ 7 วันพอดี |
| **Subscription C1c — ใช้งานได้ทีละเครื่อง + หน้า `/admin/members`** | ✅ โค้ดเสร็จ + apply DB (`0080`, 3 ส.ค. 2569) — **โมเดล: ลงทะเบียนได้ 2 เครื่อง แต่ activate ได้ทีละเครื่อง (แบบ Adobe CC)** เปิดแอปที่ B แล้วฟอนต์บน A ดับ แต่ **vault ยังอยู่ครบทั้งสองเครื่อง สลับกลับไม่ต้องโหลดใหม่** — ไม่เลือก "1 เครื่องแล้วเตะ" เพราะสลับทีต้องดึงไฟล์ใหม่ทั้งชุด (~130 ไฟล์ถ้าใช้ 10 ฟอนต์) · **เตะที่ระดับ activate ไม่ใช่ระดับ login** เครื่องที่ถูกเตะยังเรียก `list` ดูไลบรารีได้ · เครื่องที่ `activated_at` ใหม่ที่สุด = เครื่องที่ถือสิทธิ์ **ไม่มีธงตัวที่สอง** · `register_device` ยึดสิทธิ์ให้ทันที (ไม่งั้นผู้ใช้ใหม่เจอ 409 ตั้งแต่ไฟล์แรก) · **🔴 ช่องที่ปิดไปพร้อมกัน: เพดานเดิมนับ `device_id` จึงรีเซ็ตได้ด้วยการถอนแล้วลงใหม่** เปลี่ยนเป็นนับ `user_id` ทั้งสามชั้น — ที่ 2 เครื่องยังพอไหวเพราะการวน device ผิดปกติและจับได้ แต่ถ้าเป็นโมเดล 1 เครื่อง+เตะ การวนจะกลายเป็นพฤติกรรมปกติจนแยกคนดูดคลังไม่ออก **คำถามเรื่องจำนวนเครื่องเป็นตัวเปิดโปงบั๊กนี้** · หน้า `/admin/members` ใหม่รับ สมาชิก/อุปกรณ์/การใช้งาน 7 วัน (`/admin/subscriptions` เหลือเฉพาะการ์ดตั้งค่า) ตารางอุปกรณ์มีป้าย "ถือสิทธิ์อยู่" · พิสูจน์ 16 ข้อ รวม **ชนเพดานแล้วถอน+ลงใหม่ยังคง 429** |
| **Subscription C1b — ผูก `sub-font` เข้ากับอุปกรณ์ + เข้ารหัส payload** | ✅ โค้ดเสร็จ + apply DB (`0077`–`0079`, 3 ส.ค. 2569) — **ช่องโหว่ที่ปิด: JWT อย่างเดียวไม่พอ** `src/lib/supabase.ts` สร้าง client ด้วยค่า default = persistSession ลง localStorage → สมาชิกเปิดเว็บ ก๊อป access token จาก devtools แล้วยิง endpoint ตรงได้ทันที ได้ไฟล์ฟอนต์จริงโดยไม่ต้องแตะแอปเลย · และเพดานเดิม 300 ไฟล์/24 ชม. เทียบคลัง 284 ไฟล์ = **ดูดทั้งคลังจบในวันเดียว** · ตอนนี้ `list`/`download`/`heartbeat` ต้องมีลายเซ็น `HMAC(device_key,"<ts>.<rawBody>")` (รูปแบบเดียวกับ `verifyStripeSignature`) จาก device key ที่อยู่ใน OS keychain · body ของ `download` เข้ารหัส HKDF→AES-256-GCM ถึงเครื่องนั้น · **เพดาน 2 เครื่อง/บัญชี** ตรงกับ `/agreement` ข้อ 7 · **กติกา OTF (เจ้าของกำหนด): มีทั้งสองฟอร์แมตให้สตรีม .otf มีแค่ .ttf ใช้ .ttf** — `pickStreamFiles()` ตัวเดียวใช้ทั้ง `list`/`download` (ถ้ากรองแค่ที่ list ผู้ใช้ขอ index ของ .ttf ตรงได้) **ไฟล์ที่ปล่อยลดจาก 284 เหลือ 144** · เพดานรายเครื่อง 3 ชั้น ตัวที่กันดูดคลังจริงคือ "ฟอนต์ที่ต่างกัน/วัน" · **🔴 กับดัก: RLS policy กับ table grant เป็นสองชั้นที่ต้องผ่านทั้งคู่** — `0078` มี policy `for all` แล้วแต่ `0077` grant ไว้แค่ select → ปุ่มถอนอุปกรณ์คืน 403 42501 เจอเพราะยิง PATCH จริงไม่ได้ดูแค่ว่า policy มีอยู่ (แก้ใน `0079`) · **⚠️ ไม่ได้ทำให้คัดลอกไม่ได้** เจ้าของเครื่องงัด key จาก keychain ได้เสมอ — ที่ได้คือปิดทางง่ายที่สุด + ระบุตัวและตัดสิทธิ์รายเครื่องได้ · พิสูจน์ 34 ข้อด้วย Node ที่จำลองแอป (เซ็น HMAC + ถอดรหัสจริง) รวมสาขา TTF-only ที่คลังจริงไม่มี |
| **สัญญาลูกค้า `/agreement`: เพิ่มข้อ Subscription** | ✅ โค้ดเสร็จ (3 ส.ค. 2569, ไม่แตะ DB) — **ช่องว่างที่หนักกว่าฝั่ง designer เพราะเป็นเอกสารที่ผูกกับคนที่จะดึงไฟล์**: เดิม `/agreement` ไม่มีคำว่า subscription เลย และ**ข้อ 7 เดิมเขียนว่า "สิทธิ์มีอายุตลอดไป ไม่มีกำหนดวันหมดอายุ"** → สมาชิกที่กดยอมรับอ้างสิทธิ์ตลอดชีพได้ = ล้มโมเดล subscription ทั้งอัน · เพิ่ม**ข้อ 7 "การใช้งานผ่านสมาชิก"** (เลื่อน 7–15 เป็น 8–16 + แก้ cross-ref "ข้อ 13" → "ข้อ 14"): **สมาชิกไม่ได้รับไฟล์ฟอนต์ ได้แค่สิทธิ์ให้เครื่องเรียกใช้** · สิทธิ์สิ้นสุดพร้อมความเป็นสมาชิก · **ห้ามสกัด/คัดลอก/เก็บไฟล์ออกจากแอป · ห้ามหลบเลี่ยงมาตรการทางเทคนิค · ห้ามใช้บัญชีร่วม** · งานสำเร็จรูปที่ทำไว้ก่อนยังใช้ต่อได้ · **ข้อ 8 เติมวรรคจำกัดสิทธิ์ตลอดชีพให้เฉพาะการซื้อรายชุด** · ขยายนิยาม "ผู้ได้รับอนุญาต" ให้ครอบคลุมผู้ถือบัญชีสมาชิก (เดิมผูกกับ "เอกสารการสั่งซื้อ" อย่างเดียว) + เพิ่มนิยาม "แอปพลิเคชัน" · **ยังไม่มีสมาชิกสักคน จึงไม่ต้อง `legal:freeze`** |
| **สัญญา designer: เพิ่มข้อ Subscription** | ✅ โค้ดเสร็จ (3 ส.ค. 2569, ไม่แตะ DB) — **ช่องว่างที่เจอ: `/designer-agreement` 12 ข้อที่เพิ่งเขียนใหม่ 31 ก.ค. ไม่มีคำว่า subscription เลยสักคำ ทั้งที่ `fonts.is_subscription` default = เปิด** → designer ที่เซ็นตอน go-live จะถูกดึงฟอนต์เข้า pool โดยสัญญาไม่ได้พูดถึง · เพิ่มเป็น**ข้อ 4** (เลื่อน 4–12 เป็น 5–13 + แก้ cross-ref "ข้อ 10" → "ข้อ 11" สองจุด): สิทธิ์ opt-out รายชุด · สูตร 50/12/38 · pool ที่แบ่งไม่หมดตกเป็นของแพลตฟอร์ม · จ่ายรวมรอบไตรมาสเดียวกัน · **ระบุตรง ๆ ว่า stamp เป็นการตรวจสอบย้อนหลัง ไม่ใช่การป้องกันสำเนา** · ข้อ 5 (sub-license) ครอบคลุมสมาชิกด้วยแล้ว · **ยังไม่มี designer ภายนอกเซ็น จึงไม่ต้อง `npm run legal:freeze`** |
| **รีแบรนด์เฟส 1 — palette ส้ม typedee** | ✅ โค้ดเสร็จ (8 ส.ค. 2569, **ไม่แตะ DB · ยังไม่ push**) — `mint` ถูกลบทั้ง token · `navy` ตกค้างฝั่งเว็บเก็บหมดแล้ว · **พลิกขั้วพื้นหลัง: หน้าเว็บ `page` #F0F0F0 / การ์ด `surface` #FFFFFF** (เดิมกลับกัน — `bg-surface` 170 call site ไม่ต้องแตะเลย แก้แค่ค่าใน config · `bg-white` 112 จุด→`bg-page`) · **🔴 `bg-white` ที่เหลือ 6 จุดคือ "ขาวจริง" ห้ามแตะ** (ขีดแฮมเบอร์เกอร์ · ลูกบิด toggle · กระดาษ PDF ใน iframe · พื้นโปร่งทับรูปปก) · **ปุ่มกลับทาง ตามที่เจ้าของสั่ง: พื้นดำ+ตัวขาว → hover ส้ม+ตัวดำ** (เดิม mint→hover ดำ) เจตนา "หน้าจอเป็นดำกับเทาตามโลโก้ ส้มเป็นสีของการโต้ตอบ" · **🔴 กติกาที่ผิดแล้วไม่มีอะไรเตือน: ตัวอักษรบนพื้นส้มต้องเป็นดำเสมอ (6.02:1) ตัวขาวได้แค่ 3.33:1** — ป้าย FREE ที่เจ้าของเคยสั่งให้เป็นตัวขาวบน mint จึงพลิกเป็นตัวดำ · `text-mint-text` 80 จุด**แยกตามบทบาท ไม่ใช่แทนที่ด้วยสีเดียว**: ลิงก์ 50 → `.link-accent` (อักษรดำ+เส้นใต้ส้ม) · ตัวเน้น ≥24px/≥18.66px bold 15 → `orange-text` · **เล็กกว่านั้น 13 → ดำ เพราะ E03C00 ได้ 3.83:1 = ตกจริง** · **🔴 กับดักลำดับ: `\btext-mint\b` กิน `text-mint-text` ได้** รัน C2 ก่อน C1 = ลิงก์ 50 จุดกลายเป็น orange-text เงียบ ๆ build ผ่าน ไม่มี error · **🔴 กับดักที่สอง: `bg-mint` 12 จุดอยู่ใน ternary เดี่ยว ๆ ส่วนลูกเป็น `text-black` ตายตัว** → สลับเป็น `bg-black` = ตัวดำบนพื้นดำ จึงใช้ `bg-page` (เทาบนการ์ดขาว) แทนสำหรับ "แถวที่เลือก" ส่วน "แท็บ/pill" ที่คุมสีตัวอักษรเองได้ใช้ `bg-black text-white` · **sidebar/nav เป็นพื้นดำอยู่แล้ว → "เลือกอยู่" ใช้เม็ด `bg-page` ไม่ใช่ดำ** · onboarding: done=ส้ม/active=ดำ/todo=เทา (active เป็นดำอยู่ก่อนแล้ว ดำจะซ้ำ) · เก็บของที่พังจากการพลิกซึ่งไม่มี pattern จับได้: ป้าย `tag` (ขาวบนขาว) · ปุ่มบนแถบดำใน `PrintLightbox` · ลูกบิดสไลเดอร์ (ขาวบนขาว เหลือขอบ 1px) · โดนัท `/become-a-designer` (`grey-400` บน F0F0F0 = 1.87:1) · **แก้บั๊กที่มีอยู่ก่อน: ช่องกรอก `WaitlistForm` มองไม่เห็นมาตลอด** (คอมเมนต์บอก surface แต่โค้ดเป็น white) · ตัดคำว่า "โดย" หน้าชื่อดีไซน์เนอร์บนการ์ด+ตะกร้า · ลบ token ตาย `bg`/`border`/`mint-*` + `:root` ใน globals.css (ไม่มี `var()` อ่านเลย) · มิเรอร์สีไป `desktop/tailwind.config.ts` · **⚠️ Tailwind ไม่ error เมื่อ token หาย คลาสที่ตกหล่นกลายเป็น no-op เงียบ ๆ — grep คือหลักฐานเดียว** · ตรวจแล้ว: lint 0 errors · tsc สะอาด · `npm run build` ผ่าน · **CSS ที่ export ออกมาไม่มี mint เหลือสักไบต์** · **รอบ 2 (8 ส.ค. 2569) ปรับตามที่เจ้าของตรวจ:** **🔑 นิยามที่ใช้ตลอดจากนี้ — "ดำ-เทา" = สองสีบนโลโก้ `#080808` + `#F0F0F0`** (ตรวจ `public/brand/*.svg` แล้ว ลงสีจริงแค่สองค่านี้ ส้มทั้งสามถูกประกาศใน `<style>` แต่ไม่มี path ไหนใช้) → **ตัวอักษรบนพื้นดำใช้ `text-page` ไม่ใช่ `text-white`** สลับไป 74 จุด · **🔴 เว้น `text-white` บนพื้น feedback ไว้ 40 จุด — ขาวบน `success` ได้ 4.21:1 อยู่แล้ว ถ้าเปลี่ยนเป็น page เหลือ 3.70:1 = แย่ลงจริง** คู่สองสีเป็นกติกาของ*แบรนด์* ไม่ใช่ของทุกพื้น · **ป้าย FREE/NEW สลับคู่เป็นเทาพื้น-ดำตัว เพราะวางทับรูปปกซึ่งคุมสีไม่ได้** (ปกดำมีราว 1 ใน 3: KENTT/KAMINN/SUKSA/TANKK — พื้นดำจะหายสนิท) ส่วน `solid` ดำพื้น-เทาตัวใช้กับป้ายบทบาท/สถานะบนการ์ดขาว · **แก้บั๊กที่เห็นบนจอ: ป้าย "Admin" เป็นเทาบนเทาอ่านไม่ออก** เพราะ `<Badge className="bg-black text-white">` ทับ variant ปริยาย `tag` แล้ว **`cn()` ไม่ merge class ที่ชนกัน** → ต้องใช้ variant ห้าม override ด้วย className · **สถานะ "เลือกอยู่" 9 จุดเปลี่ยนจาก `bg-page` (กลืนพื้นหน้าเว็บ) เป็น `bg-black` + อักษรเทา** — **🔴 ต้องพลิกสีลูกครบทุกตัว ~28 จุด ไม่งั้นดำบนดำโดยไม่มี error** รวมป้ายที่ฝังในแถว (`bg-black`→`bg-page`), ปุ่ม radio (`accent-black`→`accent-page`) และลิงก์ `.link-accent` (เติม `text-page` ทับได้เพราะ utility ปล่อยหลัง `@layer components`) · **ยกเว้น sidebar ที่เป็นพื้นดำอยู่แล้ว → เลือก = เม็ด `bg-page`** · แผงที่กางใน OwnRevenue/MyDownloads เป็นพื้นขาว · toggle กับรางสไลเดอร์ถอดส้มเป็นดำ · เส้นใต้ชื่อดีไซน์เนอร์บนการ์ด hover เป็นส้ม · **⚠️ กับดักที่เจอ: `{/* comment */}` วางเป็นตัวแรกหลัง `{cond && (` ไม่ได้ JSX พังทันที** (โดน 2 ไฟล์) |
| **รีแบรนด์เฟส 2 รอบ 3 — เก็บรายละเอียดหน้าเว็บตามที่เจ้าของตรวจ** | ✅ โค้ดเสร็จ (8 ส.ค. 2569, ไม่แตะ DB) — **การ์ดราคาหน้าแรก**: หัวข้อรวมเป็นบรรทัดเดียว ปล่อยให้ตัดคำเองตามความกว้าง (เดิมแยกเป็นสอง `<div>` = บังคับขึ้นบรรทัดใหม่ตายตัวแม้จอกว้างพอ) · **`WaitlistForm` สีช่องกรอกตามพื้นที่วางอยู่** ผ่าน prop `on` — **ฟอร์มนี้ถูกใช้สองที่ที่พื้นต่างกัน (การ์ดหน้าแรก = `surface` · หน้า `/subscribe` = `page`) ฮาร์ดโค้ดสีเดียวจะได้ช่องล่องหนที่อีกฝั่งเสมอ เกิดมาแล้วทั้งสองฝั่ง** (เดิม surface ทั้งคู่ → หน้าแรกมองไม่เห็น · แก้เป็น page → เสียที่ /subscribe แทน) · `on` = พื้นที่**วางอยู่บน** ไม่ใช่สีช่อง ช่องเอาสีตรงข้ามเสมอ · **⚠️ ต้อง*เลือก*คลาสพื้น ห้ามต่อท้าย เพราะ `cn()` ไม่ merge คลาสที่ชนกัน** · **`/quote`**: `<select>` เลือกฟอนต์ cap 420px — **ป๊อปอัพของ `<select>` กว้างตามตัว select เสมอ สั่ง CSS ที่ `<option>` ไม่ได้** ต้องคุมที่ตัว select เท่านั้น · `LABEL` ใน `components/form/field.tsx` เปลี่ยนสีเป็น `grey-600` ให้ตรงกับหัวข้อย่อยของฟอร์มในแดชบอร์ด (`OwnSettings`) — **แก้จุดเดียวเปลี่ยนทุกฟอร์มสาธารณะ** (quote/auth/account/subscribe) · **`/fonts`**: ปุ่ม "ล้างตัวกรอง" `ghost`→`outline` ให้เป็นกล่องเหมือนปุ่มกรองตัวอื่น · **ตรึงแถบกรองทั้ง 3 แถวไว้ใต้ nav ตอนเลื่อน** สูตรเดียวกับหัวเรื่องใน `FontDetail` (`sticky top-[70px] z-30 bg-page` + `-mx/px` ให้พื้นเต็ม Container) — **`bg-page` จำเป็น ไม่งั้นการ์ดที่เลื่อนผ่านทะลุขึ้นมาซ้อน** และ **ห้ามมี `overflow-*` ที่บรรพบุรุษ ไม่งั้น sticky ตายเงียบ ๆ** · ย้ายระยะห่างจาก `mb-4` ของหัวเรื่องไปเป็น `pt-4` ของแถบเอง (ไม่งั้นตอนตรึงการ์ดมาชิดขอบล่าง nav) พร้อมเติม `mt-4` ให้ skeleton ตอนโหลดซึ่งไม่มีแถบกรอง · **ตอบข้อสงสัยเรื่องหน้า `/fonts`: 36 ฟอนต์/หน้า = 9 แถว × 4 คอลัมน์ (เดสก์ท็อป) · โฆษณาคั่นทุก 3 แถว · แถบเลขหน้าโผล่เองเมื่อเกิน 36 แต่ยังพิมพ์เลขทุกหน้าไม่มี `…` ย่อ** (ตอนนี้ 35 ฟอนต์ = 1 หน้า ยังไม่โผล่) · **เก็บ `desktop/src/App.tsx` ให้ตรงกติกาใหม่**: `font-heading text-h2` → `font-ui` · `text-white` → `text-page` · **`font-heading` เหลือที่ใช้จุดเดียวคือ wordmark ในแถบหัวแอป ซึ่งเป็นข้อยกเว้นโดยตั้งใจ** (เป็นตัวแบรนด์ ไม่ใช่ข้อความ UI · ฝั่งเว็บใช้ไฟล์ SVG แต่แอปยังเป็นเปลือก S1 จึงวางเป็นตัวหนังสือไปก่อน) → **desktop จึงต้อง bundle typedee Bold 700 ด้วย ต่างจากเว็บที่ส่งแค่ Black** ถึง S8 เปลี่ยนเป็น `<img>` wordmark แล้วถอด Bold ออกได้ · **✅ ยืนยัน desktop ด้วยตาแล้ว** — รัน `vite dev` ที่พอร์ต 1420 ในเบราว์เซอร์ (Rust ยังไม่มี IPC จึงขึ้น "เชื่อมต่อกับตัวแอปไม่ได้" ซึ่งเป็นสาขาที่ถูกต้อง) วัด `getComputedStyle`: wordmark = typedee 700 สี `#F0F0F0` · หัวข้อ = Plex Bold 700 · เนื้อความ = Plex 300 · `document.fonts` โหลดแค่ face ที่ใช้จริง typedee Black ไม่ถูกดึงลงมา |
| **รีแบรนด์เฟส 2 รอบ 2 — ถอย typedee เหลือแค่หัวข้อใหญ่** | ✅ โค้ดเสร็จ (8 ส.ค. 2569, ไม่แตะ DB) — **เจ้าของตรวจของจริงบนจอแล้วสั่งถอย: "บุคลิกฟอนต์มันจัดเกินไป"** · เส้นแบ่งใหม่: `font-heading` = typedee **เฉพาะ `text-hero`/`text-font-slug`/`text-h1`** ส่วน **h2 ลงมาทั้งหมดเป็น Plex Bold 700** (การ์ดฟอนต์รวม badge · ปุ่ม · nav · แท็บ · label ฟอร์ม · หัวตาราง · หัวข้อการ์ดราคา · slug อังกฤษบนหัวหน้า font detail · บรรทัดสเปกน้ำหนัก/สไตล์) · **วิธีที่ใช้: เปลี่ยน `font-ui` ใน config ให้ชี้ Plex แล้วกวาด `font-heading` → `font-ui` 157 บรรทัด ทุกบรรทัดที่ไม่ได้คู่กับหัวข้อใหญ่** — ตรวจก่อนกวาดว่าไม่มีบรรทัดไหนที่ `text-h1` อยู่คนละบรรทัดกับ `font-heading` (ถ้ามีจะกวาดพลาดเงียบ ๆ) · **🔴 กับดักที่กัดจริงตอนกวาด: `os.walk` ที่ prune ชื่อโฟลเดอร์ `fonts` เพื่อข้าม `src/fonts/` ไปตัด `src/app/fonts/` ทิ้งด้วย** → FontDetail/TypeTester ไม่ถูกแตะ จับได้เพราะวัด `getComputedStyle` แล้วเจอ slug อังกฤษยังเป็น typedee ไม่ใช่เพราะดูรูป · **ถอด typedee Bold ออกจาก `layout.tsx`** เพราะหัวข้อใหญ่เป็น 900 ทั้งหมดแล้ว — **`next/font/local` preload ทุกไฟล์ใน `src` ไม่ว่ามีใครใช้จริงหรือไม่** ปล่อยไว้ = เสีย 40KB ทุกครั้งที่เปิดหน้า (ไฟล์ยังอยู่ เติมกลับได้) → **payload ฟอนต์เหลือ 119KB น้อยกว่าก่อนรีแบรนด์ (Noto ~140KB) ทั้งที่เพิ่ม subset latin เข้ามาใหม่** · **🔑 แก้ vertical metrics ของ typedee** — ต้นฉบับ typo `750/-250/gap200` ทำให้กึ่งกลางกล่องอยู่ 250 เหนือเส้นฐาน แต่กึ่งกลางสายตาของตัวอักษรอยู่ 295 (ไทย)–343 (ละติน) **ตัวอักษรจึงลอยสูง เหลือที่ว่างใต้กล่องเยอะกว่าบน** (เจ้าของสังเกตจากป้าย/ปุ่ม) → `scripts/build-typedee-webfont.py` เขียนเป็น `920/-280/gap0` (กึ่งกลาง 320) **โดยคงผลรวม asc+desc = 1200 เท่าเดิม ความสูงบรรทัดจึงไม่เปลี่ยน** · ต้องตั้ง hhea ให้ตรง OS/2 ด้วย และ `usWinAscent` ต้องคลุม `head.yMax` (ของเดิม 950 < 1057 = เสี่ยงโดนตัด) · วัดจริงในเบราว์เซอร์ด้วย `measureText` ที่ 100px: ที่ว่างบน/ล่าง 32.4/28.6 (ก่อนแก้ต่างกัน 11%) · **nav บนจอแคบเรียงใหม่ตามที่เจ้าของสั่ง: สามขีดซ้าย · wordmark กลาง · ตะกร้าขวาสุด** — เดิมสามขีดอยู่ในกลุ่ม `ml-auto` จึงไปโผล่ขวา และตะกร้า absolute กลาง · wordmark ใช้ `absolute left-1/2 md:static` เพื่อให้กลาง**แถบจริง** ไม่ใช่กลางที่ว่างระหว่างสองปุ่มที่กว้างไม่เท่ากัน · ตัดปุ่ม "ดูแผนบริการ" ในส่วน "ฟอนต์ที่บันทึกไว้" ของ `/account` ทิ้ง (ซ้ำกับการ์ด Subscription ในหน้าเดียวกัน) |
| **รีแบรนด์เฟส 2 — ฟอนต์ typedee + IBM Plex Sans Thai Looped** | ✅ โค้ดเสร็จ (8 ส.ค. 2569, **ไม่แตะ DB · ยังไม่ push**) — หัวข้อ/ปุ่ม/ป้าย = `typedee` · เนื้อความ = IBM Plex Sans Thai Looped **ทั้งไทยและละติน** *(ขอบเขต typedee ถูกลดลงในรอบ 2 ดูแถวบน)* · **ขอบเขต: หน้าเว็บเท่านั้น เอกสาร PDF คงฟอนต์ Noto ตามที่เจ้าของเคาะ** (`doc-layout.ts` วัดความกว้างจาก metrics ของฟอนต์จริงเพื่อตัดบรรทัดไทย/วางคอลัมน์ เปลี่ยนฟอนต์ = ต้องไล่ตรวจทุกใบใหม่รวมช่อง comb ในฟอร์ม 50 ทวิ และเอกสารที่ส่งไปแล้วต้องหน้าตาไม่เปลี่ยน) · `render-tester` ไม่แตะ (วาดด้วยฟอนต์ของลูกค้า) · **🔴 typedee ส่งขึ้นเว็บแค่ Bold 700 + Black 900 ไม่ส่ง Regular** (Regular คือน้ำหนักที่ขโมยไปใช้คุ้มสุด) + `fsType=4` Preview&Print + license string ใน name nameID 13/14 (nameID 14 เดิมชี้ `dhammadha.com/agreement` ซึ่งจะตายหลัง DNS cutover → `https://typedee.com`) · แปลง OTF→woff2 ด้วย fontTools+brotli ที่มีในเครื่องอยู่แล้ว **ไม่ subset** เพราะขอบเขตใช้งานคือหัวข้อ+ปุ่ม+ป้ายทั้งเว็บ ต้องมีไทยครบอยู่ดี (80KB/2 ไฟล์ จาก OTF 82KB → woff2 40KB ต่อไฟล์) · **ไฟล์อยู่ `src/fonts/` ไม่ใช่ `public/`** — `next/font/local` emit เป็น `/_next/static/media/<hash>.woff2` ให้เอง (public/ เปิดโหลดตรงทาง URL ได้) · **🔴 กับดักที่ทำให้ build ล้มถ้าลอกของเดิมมา: IBM Plex Sans Thai Looped ไม่มี variable font** ต่างจาก Noto ที่จงใจละ `weight` ไว้ได้ → ต้องประกาศ `weight: ["300","400","700"]` ตรง ๆ · **🔴 กับดักที่สอง ไม่มีอะไรเตือน: `font-heading`/`font-ui` ที่ไปโผล่ที่น้ำหนัก 300/400 เบราว์เซอร์เลือก 700 ให้เงียบ ๆ** สำรวจแล้วมี 22 จุด — เมนู drawer มือถือ 8 จุดใน `Nav.tsx` เติม `text-ui` · ป้ายตัวเลขบนไอคอนตะกร้า 2 จุด + `Badge` size `sm` + ราคา tier ใน `/quote` เติม `font-bold` (ทั้งสี่ตั้งใจให้หนาอยู่แล้ว แค่ไม่เคยประกาศ) · ป้ายสไลเดอร์ใน `TypeTester` เป็น `font-body` เพราะเป็นป้ายกำกับไม่ใช่หัวข้อ · **`<th>` 13 จุดไม่ต้องแก้** UA ให้ bold 700 อยู่แล้ว (พิสูจน์ในเบราว์เซอร์ด้วย `<th>` จำลอง ไม่ได้เดา) · **remap น้ำหนักหัวข้อ: hero/font-slug/h1 800→900 · h2 800→700** เพราะ typedee ไม่มี 800 — h2 ไม่เป็น Black เพราะโผล่ 77 จุดในตาราง/การ์ด/แดชบอร์ด จะทึบ · **แก้ค่าใน config ที่เดียว call site 856 จุดไม่ต้องแตะ** (เหมือนทริค `surface` ในเฟส 1) · `body{font-family}` ใน globals.css กลับด้านจากคำเตือนเดิม เป็น Looped ได้แล้วเพราะมันคือฟอนต์เนื้อความของแบรนด์ · ลบ key `thai` (config ตาย 0 call site) · **เก็บบั๊กที่มีอยู่ก่อน: ยอดรวมตะกร้าใช้ `text-h3` ซึ่งไม่มีใน type scale = คลาสตาย** ได้ขนาด/น้ำหนักที่สืบทอดมา 16/400 → `text-h2` (20/700) · มิเรอร์ไป `desktop/` ครบ (@fontsource ibm-plex + @font-face typedee ใน `desktop/src/fonts/` เพราะ CSP `default-src 'self'` โหลด Google Fonts ไม่ได้) **`npx vite build` ผ่านและ bundle ฟอนต์ครบ แต่แอปเต็มยังรัน Tauri ไม่ได้ (รอ Rust)** · ตรวจแล้ว: lint 0 errors · tsc สะอาด · `npm run build` ผ่าน · CSS ที่ export ไม่มี `noto` เหลือสักไบต์ · ไม่มี `.otf` หลุดเข้า `out/` · **วัดด้วย `getComputedStyle` บนเบราว์เซอร์จริงทุกบทบาท** (hero=typedee/900 · h2/ปุ่ม/nav/ป้าย/ชื่อฟอนต์=typedee/700 · เนื้อความ/byline/footnote=Plex/300-400) และ `document.fonts` ยืนยันว่าโหลดครบ ไม่มี Noto ค้าง · **payload ฟอนต์ 158KB (14 ไฟล์)** จากเดิม ~140KB — ที่เพิ่มมาจาก typedee เป็น static สองน้ำหนักเต็ม + subset `latin` ที่เพิ่มเข้ามาใหม่ · **⚠️ ยังไม่ได้ตรวจหน้าหลัง login ด้วยตา** (เข้าไม่ได้) — โครงสร้างตรวจด้วย grep + `<th>` จำลองแล้ว เจ้าของควรกวาดตาแดชบอร์ดอีกรอบ |
| **รีแบรนด์เฟส 1 — เก็บ navy ฝั่ง server (type tester)** | ✅ โค้ดเสร็จ + **deploy จริงแล้ว** (8 ส.ค. 2569, ไม่แตะ DB) — `render-tester` v13 · `INK` `#2B1B3D` → `#080808` · **ต้องขยับ 3 ที่พร้อมกันเสมอ**: `render.ts:19` (สี) · `index.ts:23` `CACHE_VERSION` v1→v2 · **`TypeTester.tsx:191` เป็น literal `v2/` ไม่มี const** — ขยับไม่ครบ = รูปเก่าใน `tester-cache` ยังเป็น navy หรือ client probe พลาดทุกครั้งแล้วตกไปทางช้า **โดยหน้าเว็บดูปกติ** · deploy ผ่าน Supabase MCP ต้องส่ง `verify_jwt: false` + `entrypoint_path: index.ts` ให้ตรงของเดิม (function นี้เปิดสาธารณะ) · **พิสูจน์บน production แล้ว**: ยิง render จริงแล้วถอด PNG ทีละพิกเซล — สีหลัก `#080808` 5,075 px (ที่เหลือเป็นขอบ antialias) · header `x-cache-key` ขึ้นต้น `v2/` |
| **Cloudflare Pages deploy — ตรวจแล้วทำงานปกติ** | ระหว่างงานเคยสงสัยว่า Pages ไม่ rebuild (เจ้าของเห็น sidebar admin ขึ้น "DHAMMADHA" ทั้งที่ `origin/main` ใช้ `SHORT_NAME`=`typedee` มาตั้งแต่ `41178cf`) **แต่หลัง push รอบ 8 ส.ค. 2569 ตรวจ CSS ที่ deploy จริงแล้วเป็นของใหม่ครบ** — mint 0 จุด · `body{background:#f0f0f0}` · มี `.link-accent` → **สรุปว่าท่อ deploy ไม่ได้พัง** ที่เห็นของเก่าน่าจะเป็น cache ฝั่งเบราว์เซอร์ · วิธีตรวจเร็ว ๆ ครั้งหน้า: `curl -s https://dhammadha-web.pages.dev/ | grep -oE '/_next/static/[^"]+\.css'` แล้ว curl ไฟล์นั้นมา grep ค่าที่คาดหวัง · **ยังควรเช็ค deploy hook (`CF_DEPLOY_HOOK`) แยกต่างหากก่อน go-live** เพราะปุ่ม publish ฟอนต์ใช้คนละเส้นกับ git push |
| **🔴 แก้บั๊กใบเสร็จการซื้อรายชุด (retail)** | ✅ โค้ดเสร็จ (8 ส.ค. 2569, ไม่แตะ DB) — **ใบเสร็จพิมพ์ยอดชำระน้อยกว่าที่ Stripe เก็บจริง** (RC-2569-0017: เก็บ 1,100 แต่ใบพิมพ์ 1,067) เพราะ `generateQuotePdf` หัก ณ ที่จ่าย 3% **เสมอ** — ถูกสำหรับใบเสนอราคา/ใบแจ้งหนี้ B2B ที่ลูกค้านิติบุคคลมีหน้าที่หักจริง แต่ผิดสำหรับ retail ที่ลูกค้าจ่ายเต็มผ่านบัตร/PromptPay · แก้ด้วย flag `withholding` ใน `QuoteDocData` **ปริยาย true** (คงพฤติกรรม B2B เดิมทุกจุด) แล้ว `retail-receipt.ts` ส่ง `false` — เป็น call site เดียวที่เปลี่ยน · **⚠️ อย่าสับสนกับหัก ณ ที่จ่ายตอนโอนส่วนแบ่งให้ดีไซน์เนอร์** อันนั้นอยู่ `payout-doc.ts` เปิดตาม `LEGAL_ENTITY_IS_JURISTIC` (ยังไม่จดบริษัท = ปิดอยู่) · พร้อมกัน: `licenseDocLines("personal")` เคยตกเข้ากิ่ง fallback แล้วแปะหัวข้อ **"สำหรับ ห้างร้าน องค์กร และบริษัท"** ลงใบของลูกค้าทั่วไป → ดักไว้ก่อน คืน `"สิทธิการใช้งานสำหรับบุคคลธรรมดา"` (เขียนเต็มมี "สำหรับ" ต่างจาก `LICENSE_LABEL.personal` ที่ตัดออกเพื่อความกระชับบนจอ) · **ใบเสร็จ retail สร้างในเบราว์เซอร์ตอนกดดู ไม่ได้แนบไปกับอีเมล → ไม่มีใบผิดหลุดออกไป ใบเก่าถูกทันทีที่เปิดใหม่** · พิสูจน์ด้วยการเรนเดอร์ PDF จริงจากโค้ดของโปรเจกต์เอง (stub `fetch` ให้อ่านฟอนต์จากดิสก์) แล้วดูภาพ: retail = 1,100→1,100 ไม่มีแถวหัก · B2B = 3,500→105→3,395 เหมือนเดิมเป๊ะ · แถบยอดชำระเลื่อนขึ้นเองเพราะ `drawTotalRow` เป็น flow-based เลย์เอาต์ไม่เพี้ยน |
| Redesign หน้าสาธารณะ Phase 0–10 (หน้าแรก, /fonts, FontDetail, เอกสาร, become-a-designer, contact, verify, auth, account, quote, subscribe, checkout) | ✅ เสร็จหมด — merge เข้า `main` แล้ว |
| Redesign dashboard admin/designer | ✅ restyle ครบทุกหน้า (Phase D1–D4) — ดู [docs/design/DESIGN.md](docs/design/DESIGN.md) §18 · **เจ้าของตรวจสดหลัง login แล้วหลังรีแบรนด์เฟส 1 (8 ส.ค. 2569) ทุกหน้าถูกต้อง** — รายได้ / payouts / orders / เพิ่มฟอนต์ รวมสถานะ "เลือกอยู่" ที่เป็นพื้นดำ+อักษรเทา และแผงที่กางออกมาเป็นพื้นขาว |
| Dashboard: รวม designer section ของ admin + ปุ่มลบฟอนต์ + share มือถือ | ✅ โค้ดเสร็จ (24 ก.ค. 2026) — `/admin/*` designer section เป็น wrapper ครอบ `src/components/dashboard/Own*.tsx` ชุดเดียวกับ `/designer/*` แล้ว (ไม่มีปุ่มลบ) · ปุ่มลบย้ายไป `/admin/font-review` พร้อม modal พิมพ์ชื่อยืนยัน |
| Payout รายไตรมาส + สรุปยอดต่อ designer + เอกสาร/อีเมล | ✅ โค้ดเสร็จ + apply DB จริงแล้ว (0064/0065, 24 ก.ค. 2026) — จ่ายทุก 3 เดือน โอน ม.ค./เม.ย./ก.ค./ต.ค. · **เลย์เอาต์ PDF เสร็จแล้ว (31 ก.ค. 2026)** — ยังไม่มีบรรทัดหักภาษี ณ ที่จ่าย/เลขที่เอกสาร ต้องเพิ่มตอนจดบริษัท (ดูงานที่เหลือข้อ 1b) |
| Dashboard รอบ 2: รายได้ designer 4 กล่องกดได้ + Payouts filter ช่วงเวลา + Fonts search | ✅ โค้ดเสร็จ (24 ก.ค. 2026) — filter dropdown (ไตรมาสนี้/ก่อน/3 ปี/**ทั้งหมด**) helper กลางใน `src/lib/revenue.ts` (`periodOptions`/`monthsInPeriod`/`buildFontSales`) · ศัพท์บนจอ B2C→Retail Font · โหมดรายปี/ทั้งหมดใน Payouts = อ่านอย่างเดียว · ตาราง Retail Font **แยกแถวตามจุดราคา** · top tile "สะสมทั้งหมด" = all-time outstanding (`earned Retail − paid`) เงินค้างเก่าไม่หาย |
| Dashboard: เมนู Orders + สถานะการโอนตามเวลา | ✅ โค้ดเสร็จ + apply DB (0066/0067, 24 ก.ค. 2026) — หน้า `/admin/orders` (Retail/Subscription tab) · สถานะ payout: **ยังไม่ถึงรอบโอน**(เขียวอ่อน)/**รอโอน**(เหลือง)/**ค้างชำระ**(แดง)/จ่ายครบแล้ว |
| Payout: สถานะ+ยอดค้างโอนเป็น**รายไตรมาสที่เลือก** + badge แจ้งเตือน all-time | ✅ โค้ดเสร็จ (24 ก.ค. 2026, `admin/payouts/page.tsx`) — `statusByDesigner` เลิก rollup ข้ามช่วง คิดเฉพาะ `quartersInPeriod` (ดูไตรมาสไหน = สถานะไตรมาสนั้น) · คอลัมน์ยอดค้างโอนในตาราง = ยอดรายไตรมาส (`r.pending`) · **badge "รอโอน/ค้างชำระ X ราย" บนแถวเลือกช่วงเวลา นับ all-time (รวมทุกไตรมาส) เจตนาให้เตือนข้ามช่วงกันตกหล่น** — ต่างจากคอลัมน์ที่เป็นรายไตรมาส |
| เกณฑ์เลือกฟอนต์ "น่าจะสนใจ" / สไลด์คัดสรร | ✅ โค้ดเสร็จ + apply DB (0068, 25 ก.ค. 2026) — **ยกเลิกแนวยอดขาย/RPC `font_sales_counts()`** และ **ลบ `is_popular` (dead column)** ตามที่เจ้าของสั่ง · เกณฑ์ใหม่ทำฝั่ง client ล้วน (runtime, re-roll): related บน FontDetail = 2 ฟอนต์ designer เดียวกัน + 2 จากทั้งหมด เลือกตัว tag ตรง≥1/category เดียวกันก่อน ไม่พอเติมสุ่ม (`pickRelated`) · หน้าแรก slider 8 = โควตาลดราคาสูงสุด 3 + สุ่มทั้งหมดอีก 5 · หน้า designer slider 4 = ลดราคาก่อนแล้วสุ่ม · **เพิ่ม sale badge "ลด X%" มุมขวาบน cover สไลด์ (หน้าแรก+designer) เฉพาะฟอนต์ลดราคา** — `CoverCarousel` map `saleLabel` จาก `effectiveSale()`, โหมด `slides` (font detail) ไม่ขึ้น |
| Type tester เร่งความเร็ว (interactive ลื่นขึ้น) | ✅ โค้ดเสร็จ (25 ก.ค. 2026, `TypeTester.tsx` ไฟล์เดียว — Edge Function ไม่แตะ/ไม่ต้อง redeploy) — client cache ต่อ session (combination ที่เคย render โชว์ทันที 0 request) · debounce ตามงาน (เปลี่ยนน้ำหนัก=ทันที, พิมพ์/สไลเดอร์=250ms) · pre-warm ข้อความ default · **probe CDN `tester-cache` เฉพาะข้อความ default** (ข้อความที่ผู้ใช้พิมพ์เองแทบไม่มีทาง hit → ยิง Edge Function ตรง ประหยัด 1 RTT ต่อการพิมพ์) · **แท็บ "พิมพ์ทดสอบ" mount ค้างไว้หลังเปิดครั้งแรก** (`testerOpened` ใน `FontDetail.tsx` — ยังไม่กดดู = ไม่ mount/ไม่ยิง Edge Function เหมือนเดิม, กดแล้วซ่อนด้วย `hidden` แทนถอดออก) เดิมสลับแท็บกลับมาเสีย ~2 วิ (ยิง `info` ใหม่ ~1–1.4 วิ) + ล้างข้อความ/ขนาด/น้ำหนัก/cache ทิ้ง ตอนนี้ 0 request ~20ms · client cache เป็น LRU เพดาน 40 ภาพ (revoke objectURL ตัวที่ถูกไล่ออก กัน blob บวมเพราะ panel อยู่ยาว) · **กับดัก: `normalizeTesterText()` + hash ต้อง byte-match กับ Edge Function เป๊ะ ๆ ไม่งั้น cache key ไม่ตรง** · **เจตนาคง server-render PNG ไม่ย้ายไป client-side path rendering (opentype.js) แม้จะเร็วกว่า เพราะไฟล์ฟอนต์จะหลุดถึงเบราว์เซอร์** |
| ไล่ตรวจเก็บงานค้าง (audit รอบ 25 ก.ค.) | ✅ เสร็จ — lint 0 errors / typecheck ซอร์สสะอาด · แก้ที่ผู้ใช้เห็น: ตัดคำ "B2B" 2 จุดที่ `admin/payouts` (ตกหล่นจากรอบเปลี่ยนศัพท์), ช่อง "ที่อยู่" ใน `OwnSettings.tsx` ถูก `h-[42px]` ทับ `rows={2}` จนเหลือบรรทัดเดียว, **เมนูมือถือฝั่ง designer ไม่มีปุ่มออกจากระบบ/กลับหน้าแรกเลย** (drawer จบที่ `</nav>` ต่างจาก admin) · รวมสูตรส่วนแบ่งที่กระจาย 4 ที่เป็น `designerShareOf()` ใน `lib/revenue.ts` (เดิม hardcode `* 0.75` + ไม่ `round2` → เศษ float หลุดไป input จำนวนเงินตอนบันทึกจ่าย) · ลบโค้ดตาย: `buildMonthlyStatements`/`MonthStatement`/`monthKey`, `storage.deleteFile`, `readFontMetaSummary`, `DesignerSetupCard` (ถูกแทนด้วย redirect ไป `/designer/onboarding` ตั้งแต่รอบ onboarding), ไฟล์ orphan `components/Button.tsx` + `components/ui/Card.tsx` · **จงใจไม่แก้:** lint warning 77 ตัว (`react-hooks/set-state-in-effect` ของ React 19 — warning ไม่ใช่ error) · ช่องกรอกสูง 42/38px ไม่เท่ากัน, แท็บ 3 สไตล์, toast `z-[200]` vs `z-[190]` (ไม่เนียนแต่ไม่ใช่บั๊ก) |
| Stripe: ต่อของจริง + ทดสอบขั้นที่ 1 (test key ที่เครื่อง) | ✅ ผ่านครบ (25 ก.ค. 2026) — บัตร/PromptPay/3DS/ถูกปฏิเสธ · ยิง event ซ้ำไม่เกิด order ซ้ำ · ลายเซ็นปลอม 400 · **บังคับติ๊กยอมรับสัญญาบนหน้า Stripe** (`consent_collection`) ลิงก์มาจาก Terms/Privacy URL ใน Stripe Dashboard · **กับดัก: คลิกใน iframe ของ Stripe ผ่านเครื่องมือไม่ได้ ต้องให้เจ้าของกดจ่ายเอง** |
| ตะกร้าหลายฟอนต์ (1 คำสั่งซื้อ N รายการ) | ✅ โค้ดเสร็จ + apply DB (0069/0070/0071, 25 ก.ค. 2026) — ตาราง `order_items` เก็บยอด/ส่วนแบ่งรายรายการ (ซื้อข้ามนักออกแบบในตะกร้าเดียวได้) · `create_checkout_order_multi` + webhook อ่าน line_items ของ Stripe (ต้องมี `STRIPE_SECRET_KEY` บน Pages ด้วย) · CartContext (localStorage เก็บแค่ font_id) + submenu ตะกร้าบน Nav + `/cart` จริง · ปุ่ม "ซื้อฟอนต์นี้" = ใส่ตะกร้าอยู่หน้าเดิม (ไอคอนเด้ง) · หน้า success เคลียร์เฉพาะฟอนต์ในใบนั้น · **ทดสอบจ่ายจริงข้ามร้านผ่านแล้ว (OR-2569-0008)** |
| กับดักที่เจอระหว่างทำตะกร้า (จดไว้กันพลาดซ้ำ) | ⚠️ **policy ของสองตารางห้ามอ้างถึงกันไปกลับ** — 0069 ทำ orders↔order_items วนกัน แล้ว *ทุก* query ที่แตะ orders พังหมด (แก้ใน 0070 ด้วย RPC security definer) · ทุกที่ที่คิดเงินต้องเริ่มจาก `groupOrdersByDesigner` ห้ามวน orders ดิบแล้วอ่าน `designer_id` (ใบข้ามร้านเป็น null → ตกหล่น/นับซ้ำ) |
| สัญญาอนุญาตฉบับของดีไซน์เนอร์ แสดงครบทุกหน้า | ✅ โค้ดเสร็จ (26 ก.ค. 2026, `48da337` — ไม่แตะ DB) — เดิมมีแค่ `/quote` กับหน้าร้านดีไซน์เนอร์ที่เคารพ `designer_license_config` ที่อื่น hardcode `/agreement` ทำให้ฟอนต์ของคนที่ตั้งสัญญาเองโชว์สัญญาผิดฉบับ · ตอนนี้ FontDetail + "ดาวน์โหลดของฉัน" เปิด PDF ของดีไซน์เนอร์ผ่าน `PdfLightbox` แล้ว · **เกณฑ์ตัดสินใจตัวเดียวกันทุกที่: `!use_default && license_pdf_url` → PDF ไม่งั้น `/agreement/`** · MyDownloads ยิง config รวมทีเดียวด้วย `.in("designer_id", ...)` ไม่ยิงต่อฟอนต์ · ~~ยังไม่มี helper กลาง คัดลอกเงื่อนไขกัน 4 ที่~~ ~~ตะกร้ายังชี้ `/agreement` ตายตัว~~ ✅ **ปิดทั้งสองข้อแล้ว 1 ส.ค. 2569 — ดูแถวถัดไป** |
| **รวมเกณฑ์สัญญาอนุญาตเป็นจุดเดียว + ตะกร้า/หน้าจ่ายเงินตามดีไซน์เนอร์** | ✅ โค้ดเสร็จ (1 ส.ค. 2569 — ไม่แตะ DB) · **`designerLicensePdf()` / `designerLicensePdfMap()` ใน `src/lib/license.ts` = จุดตัดสินใจจุดเดียว** เลิกคัดลอกเงื่อนไข 4 ที่ · **`src/components/LicenseLink.tsx`** เลือกเองว่าจะเป็นปุ่มเปิด `PdfLightbox` หรือ `<Link>` ไปฉบับกลาง และ**ถือ state lightbox ไว้ในตัวเอง** ทั้ง 4 หน้าจึงเลิกถือ `pdfOpen` + `<PdfLightbox>` ของตัวเอง · **ตะกร้าเลิกชี้ `/agreement` ตายตัว** — ยิง config รวมด้วย `.in()` จาก `owner_id` ที่ `CartFont` มีอยู่แล้ว แล้วขึ้นบรรทัด "ฟอนต์ของ &lt;ชื่อร้าน&gt; ใช้สัญญาอนุญาตของผู้ออกแบบ" ต่อร้าน · **ทุกร้านใช้ฉบับกลาง = ข้อความเดิมเป๊ะ** (ยืนยันบนเว็บจริงแล้ว) · **ลำดับบรรทัดท้ายตะกร้าตามที่เจ้าของกำหนด:** (1) ซื้อโดยไม่ต้องสมัครสมาชิก — เฉพาะยังไม่ login (2) กรุณาศึกษาสัญญาอนุญาต — เฉพาะเมื่อมีฟอนต์ใต้ฉบับกลาง (3) บรรทัดละร้าน · ใช้ `gap-1` บน container คุมระยะ **ไม่ใช่ `mt-` รายบรรทัด** เพราะไม่รู้ว่าบรรทัดไหนจะโผล่เป็นตัวแรก · **`custom_text` บนหน้าจ่ายเงิน Stripe ต่อลิงก์รายร้านด้วย** เพราะจุดนั้นคือที่ที่การยอมรับมีผลจริง (`session.consent`) ชี้แต่ฉบับกลางแล้วหลักฐานจะไม่ตรงกับสัญญาที่บังคับใช้ · **บนหน้า Stripe แยกบรรทัดไม่ได้ ใช้ ` • ` คั่น — เหตุผลเต็มดูงานที่เหลือข้อ 2 (3)** · cap 5 ร้าน / 1,000 ตัวอักษร (เพดานจริง 1,200) **เกินแล้วถอยไปใช้ข้อความเดิม ห้ามให้สร้าง session ไม่ได้** · อ่าน config ไม่สำเร็จ = ใช้ข้อความเดิม ไม่ล้ม checkout · **✅ เส้นทาง PDF ทดสอบสดครบแล้ว** — 552 Studio ตั้งสัญญาของตัวเองบน production สำเร็จ ตะกร้าขึ้นบรรทัดของร้านถูก · lightbox เปิด PDF ถูกฉบับ · หน้าจ่ายเงิน Stripe มีทั้งลิงก์ฉบับกลางและของร้าน |
| **🔴 อัปไฟล์สัญญาอนุญาตของดีไซน์เนอร์พังมาตั้งแต่ `0052`** | ✅ แก้แล้ว (1 ส.ค. 2569, โค้ดอย่างเดียว ไม่ต้อง migration) — **ไม่ใช่ "ไฟล์ชนกัน" ตามที่เดากันตอนเจอ** · `OwnPricing` อัปไปที่ `license-pdf/<uuid>.pdf` (เอาชื่อ bucket มาเป็นโฟลเดอร์ซ้ำ) แต่ policy `designer manage own license pdf` ตรวจด้วย `split_part(name,'.',1) = auth.uid()` ซึ่งได้ `license-pdf/<uuid>` → **ไม่มีวันเท่ากับ uid = ทุกดีไซน์เนอร์อัปไม่ได้เลย** (พิสูจน์ด้วย SQL แล้ว) · ไฟล์เดียวที่มีใน bucket อัปไว้ 11 ก.ค. **ก่อน 0052** สมัยที่ policy เช็คแค่ว่า login หรือยัง จึงดูเหมือนว่า "เคยใช้ได้" · แก้ path เป็น `<uuid>.pdf` ที่ราก bucket ตรงกับที่ 0052 เขียนอธิบายไว้เอง · เพิ่ม `?v=<ts>` ท้าย URL เพราะ key เป็นชื่อเดิมทุกครั้ง อัปทับแล้ว CDN จะคา PDF เก่าไว้ · **toast เดิมพูดแต่ "เกิดข้อผิดพลาด" ทำให้เดาสาเหตุผิด** — ตอนนี้พ่นข้อความจริงจาก Supabase ออกมา + `upsert` ที่เคยไม่เช็ค error เลยก็เช็คแล้ว · **ไฟล์เก่า `license-pdf/f6586af8-….pdf` กลายเป็น orphan** (เจ้าตัวลบเองไม่ได้เพราะติด policy) ไม่มีอะไรอ้างถึง แต่ยังอ่านได้สาธารณะ — ลบทิ้งได้ตอนล้างข้อมูลทดสอบ |
| กดปิด "ตั้งค่าสิทธิเอง" แล้วค่าเดิมหาย ต้องกรอกใหม่ทั้งชุด | ✅ แก้แล้ว (1 ส.ค. 2569) — เดิม `save()` เขียน `tiers: null` และ `license_pdf_url: null` ทับตอน `use_default=true` = **ทำลายของเดิมถาวร** · ตอนนี้เก็บค่าไว้ ติ๊กกลับมาได้ของเดิมทันที (ปุ่ม "คืนค่า default ของเว็บ" ยังล้างจริงเหมือนเดิม — เป็นการสั่งล้างอย่างชัดเจน) · **ปลอดภัยเพราะทุกที่ที่อ่านกรองด้วย `use_default` ผ่าน `designerLicensePdf()` อยู่แล้ว** · **⚠️ ยกเว้นจุดเดียวที่ต้องตามไปแก้: `email-service` เดิม select แค่ `license_pdf_url` ไม่ดู `use_default`** → ถ้าไม่แก้ อีเมล delivery จะแนบลิงก์สัญญาฉบับที่ดีไซน์เนอร์เลิกใช้ไปแล้ว (แก้ในรอบเดียวกัน) |
| หนี้ `supabaseSelectAsService` (จากรอบไล่บั๊กอีเมล 1 ส.ค.) | ✅ ปิดแล้ว (1 ส.ค. 2569) — เดิมส่งแค่ `apikey` โดยคอมเมนต์อ้างว่า gateway synthesize `Authorization` ให้เอง **ซึ่งขัดกับผลทดสอบวันเดียวกันที่พบว่า apikey อย่างเดียวใช้กับ key แบบเก่า (JWT) ไม่ได้ → ตกเป็น role anon → `42501`** · แปลว่าอีเมลแจ้ง designer เรื่องใบเสนอราคาพังเงียบ ๆ ที่เครื่องมาตลอด (`.env.local` เป็น key แบบเก่า) ตอนนี้ใส่ key ทั้งสอง header เหมือน `supabaseSelect`/`checkout-service` แล้ว · เพิ่ม `diag` ด้วย — เดิมทุกความล้มเหลวยุบเป็น `designer_not_found` เหมือนกันหมด แยกไม่ออกว่าสิทธิ์ไม่พอหรือไม่มีแถวจริง |
| ตะกร้าบนมือถือ + ประวัติการดาวน์โหลดใน admin orders | ✅ โค้ดเสร็จ (26 ก.ค. 2026, `85b0b8c` — ไม่แตะ DB) — **บั๊กที่บล็อกการขายบนมือถือ: ไอคอนตะกร้าอยู่ในกลุ่ม `hidden md:flex` ทั้งกลุ่ม ต่ำกว่า 768px จึงไม่มีตะกร้า/ตัวเลขเลยสักที่** แก้ด้วยไอคอน `md:hidden` ข้างปุ่มสามขีด (ไม่ทำ submenu — ทัชไม่มี hover) · `/admin/orders` เพิ่มคอลัมน์ "ประวัติดาวน์โหลด" → popup แสดง ฟอนต์/ชื่อไฟล์/วันที่-เวลา/IP ล่าสุดขึ้นก่อน อ่านผ่าน `entitlements` เพราะ `download_logs` ผูกกับสิทธิ์ ไม่ได้ผูกกับ order · **สิ่งที่ log ไม่ได้เก็บ: น้ำหนักฟอนต์ (มีแค่ `file_path`) · ฟอนต์ฟรีลง `font_events` · ไฟล์ Demo ไม่ทิ้งร่องรอยเลย · subscription อยู่คนละตาราง (`sub_download_logs`)** |
| **หัก ณ ที่จ่ายในใบสรุปโอน + ใบ 50 ทวิ** | ✅ โค้ดเสร็จ + apply DB (`0073`, 1 ส.ค. 2569) — **สวิตช์ตัวเดียว `LEGAL_ENTITY_IS_JURISTIC` ใน `brand.ts`** วันจดบริษัทเปลี่ยนเป็น `true` แล้วใช้ได้ทันที ตอน `false` เอกสารเหมือนเดิมทุกประการ · **`payouts.amount` = ยอดเต็ม (gross)** + `wht_rate`/`wht_amount`/`net_amount`/`doc_no`/`wht_cert_no` แยกคอลัมน์ — **กลับทางกับเช็คลิสต์เดิมที่เขียนว่าเก็บยอดสุทธิ** เพราะใบ 50 ทวิบังคับให้ระบุยอดจ่ายคู่กับภาษีที่หัก · RPC `record_payout` คำนวณเองไม่เชื่อ client (เดิม insert ตรง ๆ เรียก `next_doc_no` ไม่ได้เพราะถูก revoke ใน `0032`) · **ใบ 50 ทวิกรอกฟอร์มจริงของกรมสรรพากร ไม่ได้วาดเอง** — AcroForm 81 ช่อง กับดักอยู่ในหัวไฟล์ `wht-cert-doc.ts` (ต้อง `updateFieldAppearances` ไม่งั้นไทยพัง · ช่องเลข 13 หลักเป็น comb ที่ pdf-lib ทำไม่ได้ ต้องวาดเอง · คอลัมน์บาท\|สตางค์ ต้องแยกสองฝั่งเส้นแบ่ง) · **ยื่นเป็น 3 เตรส/ภ.ง.ด.3 ไม่ใช่ 40(3)/ภ.ง.ด.2 — 🔴 รอ CPA ยืนยัน** สลับได้ที่ `WHT_INCOME_BASIS` ที่เดียว · สัญญาข้อ 6 จึงเขียนกลาง ๆ ว่า "เงินได้พึงประเมินตามกฎหมาย" |
| **ทบทวนเอกสารกฎหมายทั้งชุด เทียบ EULA มาตรฐานตลาด** | ✅ เสร็จ (31 ก.ค. 2569 — ไม่แตะ DB) เทียบกับ MyFonts/Monotype · Creative Market · YouWorkForThem · Creative Fabrica · DB Font · Cadson Demak · ร่างเต็มอยู่ที่ `docs/legal/DRAFT-2569-08.md` · **สองปัญหาที่แก้: (1) `/agreement` เขียน "ไม่คืนเงินทุกกรณี" ขัดกับ `/refund-policy` ตรง ๆ (2) "ให้ยึดประกาศล่าสุด" = แก้สิทธิ์ที่ขายไปแล้วย้อนหลังได้** · `/agreement` 7 ข้อ → 15 ข้อ เพิ่ม: นิยาม/อุปกรณ์ · สำเนาสำรอง 1 ชุด · ฝัง PDF read-only · **แยกให้ชัดว่า "ห้ามดัดแปลง" = ห้ามแก้ไฟล์ฟอนต์ ไม่ใช่ห้าม outline ในโปรแกรมออกแบบ** (เดิมตีความตรงตัวแล้วส่งไฟล์โรงพิมพ์ไม่ได้) · ผู้รับจ้างช่วงส่งได้ 1 ชุด (ตาม YWFT) · ebook/POD ที่ลูกค้าพิมพ์เอง/สินค้าตัวอักษรขายต่อ · **ห้ามเทรน AI** · เพดานความรับผิด · สิทธิ์ตลอดชีพแต่โมฆะเมื่อผิดสัญญา · กฎหมายไทย · **ตัด "สัญชาติไทยเท่านั้น" เปิดสากล** · `/designer-agreement` 8 → 12 ข้อ เพิ่มสิทธิ์ทำการตลาด (เดิมโพสต์โปรโมตฟอนต์ designer ไม่ได้ตามตัวอักษร) · non-exclusive · chargeback clawback · รอบจ่ายรายไตรมาส · `/privacy` **เพิ่ม Stripe ที่ขาดไป (ข้อบกพร่อง PDPA)** · **`EFFECTIVE_DATE` ที่ `src/lib/legal.ts` จุดเดียว** (เดิม hardcode ซ้ำ 5 ไฟล์) · **`npm run legal:freeze`** แช่แข็งฉบับเก่าเป็นหน้าแยกก่อนแก้ (มี guard กันแช่แข็งผิดลำดับ) · `licenseDocLines()` เพิ่มบรรทัด "ตามสัญญาอนุญาตฉบับ &lt;วันที่&gt;" ตรึงลงเอกสาร + `DOC_EXTENDED_CLAUSE` ข้อ (3) → **ข้อ (5)** |
| เทียบ payment gateway: Payso vs Stripe vs Omise | ✅ ตัดสินใจแล้ว (29 ก.ค. 2026, ไม่แตะโค้ด — บันทึกไว้กันรื้อซ้ำ) **สรุป: อยู่กับ Stripe ต่อ ไป go live ให้จบ** · ค่าธรรมเนียม (บวก VAT 7% ทั้ง 3 เจ้าเหมือนกัน) บัตรไทย Payso 2.95% / Omise 3.65% / Stripe 3.65%+฿10 · PromptPay 1.35% / 1.65% / 1.65% → **ฟอนต์ ฿990 จ่าย PromptPay ต่างกันแค่ ~฿3/ออเดอร์** (บัตรต่าง ฿18) และยังไม่เปิดขายจึงไม่รู้สัดส่วนบัตร:PromptPay · **Payso = PaySolutions** (`paysolutions.asia`) — redirect flow พื้นฐานเป็น HTML form POST ส่ง `total` ดิบ **ไม่มีลายเซ็น แก้ราคาเป็น ฿1 ได้** ต้องใช้ Secure Link API เท่านั้น · **postback ไม่มี signature** ต้องยิง Inquiry API (`/order/orderdetailpost`) ถามยอดจริงทุกครั้งก่อน mark paid · ไม่มี line items (มีแค่ `orderDetail` string เดียว) → ต้อง look up ตะกร้าจาก `order_items` ด้วย refno เอง + refno ต้องเป็นตัวเลข 12 หลัก unique · **ย้ายแล้วเสีย `consent_collection` = หลักฐานยอมรับสัญญาอนุญาตที่ Stripe เก็บให้ ซึ่งธุรกิจขายสิทธิ์ฟอนต์ต้องการ Payso/Omise ไม่มีเทียบเท่า** · **recurring ของ Payso มีจริงแต่ดิบ** — `restpayrecurring` สั่งตัดทีละงวด เราต้องยิงเองล่วงหน้า 1 วัน ระบบตัด 07:30–09:00 น. เท่านั้น ไม่มี plan object/auto-renew/dunning/retry/portal → ต้องเขียน scheduler เอง (pg_cron / Cloudflare Cron เพราะ static export) scheduler พลาด = ไม่ถูกตัดเงินเงียบ ๆ · **จุดเดียวที่ Payso ชนะขาดคือ subscription ฿290/ด. (3.2% vs Stripe 7.6%) เพราะ ฿10 คงที่ทำร้ายยอดเล็ก — แต่ Phase 4.1 ยัง pause อยู่** · **กลับมาพิจารณาใหม่เมื่อ:** ขายจริง 2–3 เดือนแล้วบัตรเกิน ~40% ของออเดอร์ / ตัดสินใจเปิด subscription จริงจัง · ถ้าทำ subscription บน Stripe → ดันรายปี ฿2,900 เป็นตัวหลัก (4.3% vs 7.6%) · แผนเต็ม: `~/.claude/plans/payso-virtual-hennessy.md` |
| **แยกแบรนด์แพลตฟอร์มออกจาก Dhammadha** — เตรียมโค้ด | ✅ โค้ดเสร็จ (29 ก.ค. 2026, ไม่แตะ DB) **ชื่อยังไม่เคาะ โค้ดยังเป็น DHAMMADHA STUDIO ทั้งหมด** · เหตุผลที่ต้องแยก: designer ต้องฝากขายในร้านของคู่แข่งที่กินค่าคอม 25% ด้วย = conflict of interest (เหตุผลรอง: แผนขยายอาเซียน) · โครงสร้างที่เลือก: แพลตฟอร์ม = แบรนด์ใหม่, **Dhammadha Studio = foundry เจ้าหนึ่งบนแพลตฟอร์ม** (`dhammadha.com` เก็บไว้เป็นเว็บสตูดิโอ ชี้เข้าหน้าร้าน) · **`src/lib/brand.ts` = จุดเดียวที่นิยามชื่อ/โดเมน/อีเมล** เปลี่ยนชื่อ = แก้ไฟล์เดียว (ทดสอบแล้วจริง: สลับเป็น TYPEDEE → title/nav/footer/mailto/เนื้อสัญญาเปลี่ยนตามทั้งเว็บ) · **แยกสามชั้นในโค้ด: แพลตฟอร์ม (เปลี่ยน) / foundry Dhammadha (คง) / `LEGAL_ENTITY` "ธรรมดาสตูดิโอ" = คู่สัญญาในหน้ากฎหมาย (คง — สัญญายังออกในนามนิติบุคคลเดิม)** · **ผลค้นโดเมน RDAP: `typedee.com` ว่าง · รากอาเซียนชนกำแพงหมด** (nimit/likhit/lontar/kawi/likha + รูปแปลง จดหมดตั้งแต่ 2000–2021 → ต้องซื้อตลาดรอง $3k–30k) · `Aksara` ถูก foundry อินโดฯ ใช้แล้ว |
| **โครงสร้างนิติบุคคล + ภาษี** | ✅ ตัดสินใจแล้ว (30 ก.ค. 2026, ไม่แตะโค้ด) — ธรรมดาสตูดิโอเป็นชื่อในวงการเท่านั้น **โดยนิตินัยยังเป็นบุคคลธรรมดา ไม่เคยจดบริษัท** · **เลือกแบบ A: บริษัททำแพลตฟอร์มอย่างเดียว รับเฉพาะ 25% · ธรรมดาสตูดิโอรับ 75% ในนามบุคคลเหมือน designer ทุกคน** · เหตุผลหลัก: **75/25 เป็นอัตราประกาศสาธารณะที่ทุกคนได้เท่ากัน = หลักฐาน arm's length ที่พิสูจน์ได้เอง** (ธุรกรรมบริษัท↔กรรมการคือจุดที่สรรพากรเพ่งเล็งที่สุด) · แบบ B (รวมรายได้ฟอนต์เข้าบริษัท) ตกเพราะ **บังคับโอนลิขสิทธิ์ฟอนต์เข้าบริษัท** (จำหน่ายทรัพย์สิน ต้องตีมูลค่า) + รื้อเหตุผลที่เพิ่งแยกแบรนด์ไป · ภาษีไม่ใช่ตัวตัดสิน: CIT SME 15% + ปันผลหัก 10% ≈ 23.5% ซึ่งไม่ได้ชนะ PIT ขั้นบันไดในช่วงรายได้ต่ำ · **ถ้ารายได้ฟอนต์ส่วนตัวโตมากค่อยตั้งบริษัทที่สองสำหรับ foundry** (แยกทีหลังง่ายกว่าแยกของที่รวมไปแล้ว) · **ราคาที่แสดง (฿990) = รวม VAT แล้ว** พอจด VAT ค่อยแยก 7% ในใบเสร็จ ลูกค้าไม่เห็นราคาขึ้น |
| Stripe Connect (จ่ายเงินเข้า designer โดยตรง) | ❌ **พิจารณาแล้วไม่เอา** (30 ก.ค. 2026) — ไทยรองรับ **direct charges + application fee เฉพาะ TH→TH** แต่ **`separate charges & transfers` ไม่รองรับ ระบุชัดเจน** และ destination charges ไม่อยู่ในรายการ · direct charge 1 ครั้งผูก connected account เดียว → **ตะกร้าข้ามร้าน (`0069`–`0071`, ใบ OR-2569-0008) ต้องถอดทิ้ง** ตะกร้าจาก 3 คน = 3 charges = PromptPay สแกน QR 3 ครั้ง · designer ทุกคนต้องผ่าน KYC + **selfie (API ทำไม่ได้ ต้อง redirect)** และสมัครแทนไม่ได้ ต้องติดต่อฝ่ายขาย Stripe → กั้น funnel รับ designer · เงินเข้าทันที = ขัดนโยบายจ่ายรายไตรมาส · **ทางที่เลือกแทน: แก้ที่สัญญาไม่แตะท่อเงิน** เพราะตัวการ/ตัวแทนตัดสินที่ "ใครควบคุมสินค้าก่อนส่งมอบ" ไม่ใช่เส้นทางเงิน |
| **เก็บงานที่ไม่ติดบล็อกชื่อแบรนด์/บริษัท** | ✅ เสร็จ (1 ส.ค. 2026) — **(1) `custom_text` บนหน้าจ่ายเงิน Stripe** ตั้งแล้วที่ `checkout-service.ts` (`tosAcceptanceMessage(site)`) ปิดคำถามค้างจากข้อ 2: **Stripe รองรับลิงก์ markdown** และ **การตั้ง `custom_text` ลบข้อความ default ทิ้งทั้งก้อนรวมลิงก์ ToS ของ Stripe เอง → ลิงก์ในข้อความห้ามถอด ไม่งั้นหน้าจ่ายเงินเข้าสัญญาอนุญาตไม่ได้เลย** · ข้อความไม่มีชื่อแบรนด์/โดเมน (ใช้ `${site}`) เปลี่ยนชื่อแล้วไม่ต้องแก้ · **(2) ฟอนต์ทดสอบมี 4 ตัวไม่ใช่ 2** ตั้ง `is_active=false` หมดแล้ว เหลือบนเว็บ 22 ตัว · **(3) `scripts/reset-test-data.sql`** เขียนพร้อมแล้ว **ยังไม่รัน** (รอ Stripe ขั้นที่ 3) · **กับดักที่เจอ: `order_items.font_id → fonts` เป็น `ON DELETE RESTRICT` FK ตัวเดียวในระบบที่ไม่ใช่ CASCADE** → ลบฟอนต์ก่อนล้าง order ไม่ได้ |
| **Stripe ขั้นที่ 2 — ทดสอบบน production ด้วย test key** | ✅ **ผ่านครบ (1 ส.ค. 2026)** — webhook โหมด test + Pages env + ซื้อจริง 2 ใบ (บัตร ฿1,300 / PromptPay ฿1,100) · order/order_items/entitlements/ส่วนแบ่ง 75-25/เลขที่เอกสาร/อีเมล delivery ถูกต้องหมด · **ตะกร้าข้ามร้านผ่านบน production ครั้งแรก** · **กับดักใหญ่ที่เจอ: Supabase secret key แบบใหม่ (`sb_secret_…`) ไม่ใช่ JWT ห้ามใส่ใน `Authorization: Bearer` ขณะที่ apikey เป็น anon — ต้องใส่ key ทั้งสอง header** (รายละเอียด + เหตุผลที่ทดสอบที่เครื่องไม่เจอ ดูข้อ 2 ด้านล่าง) |
| ศัพท์ "สิทธิการใช้งานส่วนบุคคล" ใช้ตรงกันทั้งเส้นทาง | ✅ โค้ดเสร็จ (1 ส.ค. 2026, `0d70d89`+`7546d6a`) — เดิมลูกค้าเห็นสองคำในเส้นทางเดียว: ตะกร้า/หน้าจ่ายเงินเขียน "สิทธิ์บุคคลทั่วไป" ส่วนอีเมลโชว์ค่าดิบ `personal` · ตอนนี้ **`LICENSE_LABEL.personal` ใน `src/lib/license.ts` = จุดกลางจุดเดียว** (ไฟล์นั้นออกแบบให้ใช้แทนการ render ค่าดิบทุกที่อยู่แล้ว) + `CartView` + ชื่อ line item บน Stripe ใช้คำเดียวกัน ตรงกับ `/agreement` · **หัวคอลัมน์ซ้ายบน FontDetail = "บุคคลธรรมดา"** (เดิม "บุคคลทั่วไป") · ไม่เหลือคำว่า "บุคคลทั่วไป" ใน `src` แล้ว |
| **ใบเสร็จรับเงินของการซื้อรายชุด (Retail)** | ✅ โค้ดเสร็จ + apply DB (`0075`, 1 ส.ค. 2569) — เดิมเลข `RC` ออกเฉพาะเส้นทางใบเสนอราคา **คนที่ซื้อผ่าน Stripe ไม่ได้เอกสารทางบัญชีเลยสักใบ** · `orders.receipt_no` + **ทริกเกอร์ before insert** ออกเลขให้ตอนสร้างออเดอร์ที่จ่ายแล้วและ `quote_id is null` (ใช้ทริกเกอร์แทนการ `create or replace` ตัว `create_checkout_order_multi` ทั้งก้อน เพราะฟังก์ชันนั้นยาว ~150 บรรทัดและอยู่บนเส้นทางเงิน) · **ผู้ออกใบเสร็จ = แพลตฟอร์ม ตามฐานะตัวการใน `/terms` ข้อ 4** ต่างจากใบเสร็จของใบเสนอราคาที่ออกในนาม designer · `src/lib/retail-receipt.ts` แปลงแถว orders → `QuoteDocData` ใช้ renderer เดิมตัวเดียวตามกฎใน CLAUDE.md · อีเมล delivery แยก 3 บรรทัด (ขอบคุณ / เลขที่คำสั่งซื้อ / เลขที่ใบเสร็จ) รองรับทั้งสองเส้นทาง · "ดาวน์โหลดของฉัน" กดเปิดใบเสร็จได้ **เรนเดอร์ในเบราว์เซอร์เพราะ webhook สร้าง PDF เองไม่ได้** · **ใบเสร็จเป็นของทั้งคำสั่งซื้อ ตะกร้าหลายฟอนต์เห็นเลขเดียวกันทุกแถว = ถูกต้อง ไม่ใช่ใบซ้ำ** · ⚠️ ออเดอร์ที่สร้างก่อน `0075` ไม่มีเลข (ปุ่มถูกซ่อน) — เติมมือให้ 14 ใบทดสอบแล้วเพื่อทดลอง ของจริงไม่ต้องเติมเพราะข้อมูลทดสอบจะถูกล้างก่อนเปิดขาย |
| **บั๊ก: จำนวน weight/style ที่แก้มือถูกเขียนทับ** | ✅ แก้แล้ว (1 ส.ค. 2569) — effect ที่เติมค่าจากการอ่านไฟล์เขียนทับช่องกรอก**ทุกครั้งที่เปิดหน้าแก้ไข** · **ไม่ใช่แค่แสดงผลผิด — กดบันทึกต่อจะทับค่าที่ถูกใน DB ด้วยค่าที่อ่านผิดเงียบ ๆ ค่าที่แก้มือจึงไม่มีวันอยู่รอด** · ตอนนี้เติมอัตโนมัติเฉพาะตอนที่ยังไม่มีค่าจากคน (ค่าใน DB หรือที่เพิ่งพิมพ์) · **แลกกับ: เปลี่ยนไฟล์ฟอนต์ใหม่แล้วตัวเลขไม่อัปเดตเอง ต้องแก้มือ** — จงใจ เพราะทับค่าที่ถูกต้องเงียบ ๆ อันตรายกว่า · บรรทัดสรุป "9 weights · 8 styles" ยังโชว์ผลอ่านล่าสุดให้เทียบเองได้ |
| **ใบแจ้งหนี้ (Invoice) ต่อเข้า flow ยืนยันรับชำระ** | ✅ โค้ดเสร็จ + apply DB (`0076`, 2 ส.ค. 2569, `fc65b6d`) — ตัวเอกสารมีอยู่แล้วใน `quote-doc.ts` (`DOC_SPEC.invoice`) รอบนี้ต่อท่ออย่างเดียว **ไม่แตะไฟล์เอกสารเลย** · **ใบแจ้งหนี้เป็นของเสริม ออกเฉพาะเมื่อลูกค้าขอ** (ลูกค้าส่งใบ PO มาแล้วขอเพื่อรอรับเงินตามรอบ) — ตอนกด "ยืนยันรับชำระ" มีบล็อกเลือกเอกสาร ใบเสร็จติ๊กค้างปิดไม่ได้ ใบแจ้งหนี้ **default ไม่ติ๊ก** · **กติกาตัวชี้ขาดเดียว = `quotes.invoice_no is null`** ไม่มีเลข = ไม่แนบไฟล์ ไม่เอ่ยในอีเมล ไม่ขึ้นปุ่มพิมพ์ (ไม่มีธงตัวที่สองให้ค่าเพี้ยนกันเอง) · สองเส้นทางออกเลข: `confirm_quote_paid(…, p_issue_invoice)` ตอนยืนยัน · `issue_quote_doc(id,'invoice')` สำหรับออกย้อนหลังผ่านปุ่ม "สร้างใบแจ้งหนี้" ทั้งคู่ idempotent · **🔴 กับดักที่เจอ: เพิ่มพารามิเตอร์ให้ RPC ด้วย `create or replace` = ได้ overload ตัวที่สอง ของเดิมไม่หาย แล้ว PostgREST ที่เรียกด้วยชื่ออาร์กิวเมนต์ชุดเดิมจะเข้าได้ทั้งคู่ → `function is not unique` ต้อง `drop function` ก่อนเสมอ (drop แล้ว grant หายด้วย ต้อง grant ใหม่)** · `handleDelivery` รับ `attachments` เป็น array แล้ว — **นับขนาด base64 รวมทุกไฟล์เทียบเพดาน ไม่ใช่ทีละไฟล์** ไม่งั้นสองไฟล์ทะลุได้เท่าตัว · `handleDocument` ต้องรองรับ `doc_type: "invoice"` ด้วย ไม่งั้นปุ่มส่งอีเมลใน `PrintLightbox` ถูกปฏิเสธ · ทดสอบเส้นทาง DB ครบใน DO block ที่ rollback ทั้งก้อน (ติ๊ก → RC+IV · ออกซ้ำได้เลขเดิม · ไม่ติ๊ก → null · ย้อนหลังได้เลขถัดไป) **ยังไม่ได้กดจริงบนหน้าจอหลัง login** |
| Go-live จริง (ย้ายฟอนต์ 35 ตัว, Stripe, Zoho, DNS cutover) | ⏳ ยังไม่ทำ — **ต้องเคาะชื่อ+โดเมน (ข้อ 0) และจดบริษัท (ข้อ 0b) ก่อน** เพราะงานข้อ 2/3/5/6 ผูกกับทั้งโดเมนและนิติบุคคล (บัญชี Stripe ต้องตรงกับนิติบุคคลที่รับเงิน) ทำก่อนแล้วเปลี่ยนทีหลัง = รื้อใหม่หมด ดูด้านล่าง |

**Branch:** ทำงานบน `main` สายเดียว — **งานถึง `b498763` commit + push ครบแล้ว**
(เจ้าของ push เองผ่าน GitHub Desktop)
· **Edge Function ตรงกับ repo แล้ว: `sub-font` v5 · `download-font` v9**
(deploy แยกจาก Cloudflare ไม่ต้องรอ push)
· **DB apply ถึง `0082` แล้วทั้งหมด** — `0077`–`0082` (subscription C1b–C1e)
· ⏳ **โค้ดเว็บรอ Cloudflare deploy** — หน้าที่จะเปลี่ยนเมื่อ deploy ขึ้น:
`/agreement` (ข้อ subscription + จำกัดสิทธิ์ตลอดชีพ) · `/designer-agreement` (ข้อ 4
คิดตามระยะเวลา) · `/admin/members` (หน้าใหม่) · `/admin/subscriptions` (เหลือแค่การ์ดตั้งค่า)
· เมนู Admin เพิ่ม Members
· **`0076` apply ลง DB จริงไปแล้ว แต่โค้ดยังไม่ deploy** — ไม่พังเพราะเว็บเวอร์ชันเก่า
ไม่ได้อ่านคอลัมน์ใหม่ แต่ปุ่มใบแจ้งหนี้จะยังไม่โผล่จนกว่าจะ deploy
· **ก่อนทดสอบ Stripe บน production ต้องเช็คว่า Cloudflare deploy รอบล่าสุดขึ้นแล้วจริง** —
DB apply 0069–0071 ไปก่อนหน้าโค้ด ถ้าเว็บยังเป็น bundle เก่าจะได้ทดสอบตะกร้าเวอร์ชันเก่า

## งานที่เหลือ (เรียงลำดับ)

0. **✅ เคาะชื่อ + จดโดเมนแล้ว: `typedee.com` (7 ส.ค. 2569) — โค้ดฝั่งชื่อ push ขึ้น production แล้ว**

   4 commit: `3cd87f0` (ชื่อ/เอกสาร/ยกเลิกสี navy) · `5c07c0a` (Nav/Footer ใช้ wordmark) ·
   `f3d28df` (reply_to + กัน quote ล้มเงียบ + robots/cart) · `343d15a` (`PUBLIC_SITE_URL` ในเอกสาร)
   ระบบแบรนด์เต็ม ๆ ดู [[project_typedee_brand_system]] · อีเมลดู [[reference_email_stack]]

   **✅ เสร็จแล้วในรอบนี้**
     (ข) โลโก้/wordmark — SVG อยู่ `public/brand/` (crop viewBox ชิดตัวอักษรแล้ว
         ต้นฉบับ Illustrator เว้นขอบเยอะจนตัวจริงเหลือ 40%) · `LOGO_SRC`/`WORDMARK_SRC`/
         `FAVICON_SRC` ใน brand.ts · Nav จอ<sm=เครื่องหมาย ≥sm=wordmark · Footer=wordmark เสมอ
     (ง) `SOCIAL` → `facebook.com/typedee.official` + `instagram.com/typedee.official` ·
         **ถอด tiktok+line ออก** (ยังไม่เปิดบัญชี — ห้ามใส่ URL ล่วงหน้า Footer/อีเมลจะยิง 404)
     · `LEGAL_ENTITY` → **"ไทป์ดี.คอม"** (เดิม "ธรรมดาสตูดิโอ") — `drawIssuerHeader()` ประกอบ
       "โดย {LEGAL_ENTITY_OPERATOR}" ให้เอง จึงไม่ต้องแตก token · **แก้สะกด "มณทล" → "มณฑล"**
       (ชื่อบนใบเสร็จ/ใบ 50 ทวิ ข้างเลขผู้เสียภาษี)
     · Resend: `typedee.com` verified · **API key เดิมผูกโดเมน dhammadha.com จึงตายตอนลบโดเมน**
       สร้างใหม่แล้วใส่ครบ 3 ที่ (`.env.local` / Pages env / **Supabase SMTP password**)
     · Cloudflare Pages: **ชื่อโปรเจกต์เปลี่ยนเป็น `typedee-web` แต่ hostname ยังเป็น
       `dhammadha-web.pages.dev`** (Cloudflare แก้ได้แค่ชื่อที่แสดง) → **Stripe webhook /
       Turnstile hostname / deploy hook ไม่ต้องแตะตอน cutover** ดู [[reference_cloudflare_pages]]

   **⏳ ยังไม่ทำ — ทำวัน DNS cutover พร้อมกันให้หมด**
     (ก) **`BRAND_DOMAIN` ใน Edge Function สองตัว** (Deno import `lib/brand.ts` ไม่ได้ จึงมีสำเนา
         ของตัวเอง) — `supabase/functions/download-font/index.ts:21` **และ**
         `supabase/functions/sub-font/index.ts:75` แก้แล้ว **ต้อง redeploy ทั้งคู่**
         ไม่งั้นไฟล์ที่ขาย/ส่งให้สมาชิกถูกประทับโดเมนเก่าในตัวไฟล์ แก้ย้อนหลังไม่ได้
         **ต้องพลิกให้ตรงจังหวะกับ DNS** เพราะช่วงคาบเกี่ยวจะประทับผิดฝั่ง
     (ค) ทบทวน**ถ้อยคำ**หน้ากฎหมาย (terms/privacy/refund/agreement/designer-agreement) ว่าประโยคไหน
         ควรพูดถึงแพลตฟอร์ม ประโยคไหนพูดถึงนิติบุคคล — ตัวแปรถูกต้องแล้ว แต่ถ้อยคำยังไม่ทบทวน
     · Pages → Custom domains เพิ่ม `typedee.com`+`www` → ถอด `dhammadha.com`
     · Turnstile widget `typedee-quote` → เพิ่ม hostname `typedee.com`, `www.typedee.com`
     · Supabase → Auth → URL Configuration → Site URL + Redirect URLs เป็น typedee.com
       (https://supabase.com/dashboard/project/skyuqpshxsskjwkbnrmd/auth/url-configuration)
     · Pages → Build → Git repository reconnect ไป `dhammadha/typedee-web`
       (repo rename แล้ว GitHub redirect ให้อยู่ ยังไม่พัง) **แล้วเช็ค deploy hook ว่า URL
       ยังตรงกับ env `CF_DEPLOY_HOOK`** ไม่งั้นปุ่ม publish ฟอนต์กดแล้วไม่ rebuild แบบเงียบ
     · Zoho mailbox รับ `info@typedee.com` — **MX ที่รากสงวนไว้ให้ Zoho ห้ามเอา MX ของ Resend
       ไปวาง** (ของ Resend อยู่ที่ `send.typedee.com`) · แล้วเปลี่ยน DMARC `rua=` เป็น
       `dmarc@typedee.com` (ตอนนี้ชี้ gmail ซึ่งจะไม่ได้รับรายงานตาม RFC 7489 §7.1)

   **✅ งานแบรนด์ฝั่งโค้ด — ปิดครบแล้ว**
     · **เฟส 1 — palette ส้ม (8 ส.ค. 2569)** ดูแถว "รีแบรนด์เฟส 1" ในตารางสถานะ
     · **เฟส 2 — ฟอนต์ (8 ส.ค. 2569)** ดูแถว "รีแบรนด์เฟส 2" ในตารางสถานะ
       หน้าเว็บเท่านั้น · **เอกสาร PDF ยังเป็น Noto โดยตั้งใจ** ถ้าวันหนึ่งอยากให้เอกสาร
       เป็นฟอนต์แบรนด์ด้วย ต้องแปลงเป็น TTF ให้ pdf-lib ฝังได้ แล้วเรนเดอร์เทียบทีละใบ
       (ใบเสนอราคา/แจ้งหนี้/เสร็จ retail/เสร็จ B2B/สรุปโอน/50 ทวิ) เพราะ `doc-layout.ts`
       วัดความกว้างจาก metrics ของฟอนต์จริง — เปลี่ยนฟอนต์ = เลย์เอาต์ขยับทุกใบ

     (จ) `dhammadha.com` → ทำเป็นเว็บสตูดิโอ ชี้ปุ่มซื้อไปหน้าร้านบนแพลตฟอร์ม
         · **แผนละเอียดพร้อมแล้ว (5 ส.ค. 2569): `~/.claude/plans/typedee-com-compiled-waterfall.md`**
           — repo ใหม่แยกขาด อ่าน Supabase เดิมด้วย anon key (RLS เปิดทางไว้หมดแล้ว **ไม่ต้องแตะ backend**) ·
           ฟอนต์ทุกตัวที่ publish ขึ้นอัตโนมัติ · canonical ชี้ตัวเอง (ให้ dhammadha ติดอันดับเองได้) ·
           Supabase DB webhook → Cloudflare Deploy Hook เวลามีฟอนต์ใหม่ ·
           **งานฝั่งนี้มีแค่ไฟล์เดียว: รับ `?add=1` ใน `FontDetail.tsx` เพื่อหยิบใส่ตะกร้าให้เลย**
           (ตะกร้าอยู่ใน localStorage ของ origin แพลตฟอร์ม เว็บสตูดิโอเขียนข้ามโดเมนไม่ได้) ·
           ปุ่มขอใบเสนอราคาใช้ `/quote/?font=&designer_slug=` ที่มีอยู่แล้ว ไม่ต้องแก้อะไร

0b. **🔴 จดบริษัท (แบบ A) — บล็อก go-live เท่ากับข้อ 0**
   · **บัญชี Stripe ต้องตรงกับนิติบุคคลที่รับเงิน** เปิดขายในนามบุคคลก่อนแล้วค่อยจดบริษัท
     = onboard Stripe ใหม่ทั้งชุด (KYC/bank/ToS/webhook) + ทำสัญญา designer ใหม่ภายหลัง
   · โดเมน/Supabase/Cloudflare ควรอยู่ในชื่อบริษัทด้วย
   · **✅ ตอบแล้วทั้งชุด (31 ก.ค. 2569) — เขียนลงสัญญาเรียบร้อย ดู `docs/legal/DRAFT-2569-08.md`**
     1. **ตัวการ (principal)** — รายได้นับยอดเต็มที่ลูกค้าจ่าย ส่วนแบ่ง designer เป็นค่าใช้จ่าย
        ⚠️ **กลับทางจากที่เคยวางแผนไว้ว่าจะเขียนถ้อยคำแบบตัวแทน** ถ้อยคำ "แพลตฟอร์มรับชำระเงิน
        แทนนักออกแบบในฐานะตัวแทนเรียกเก็บเงิน" **ห้ามเขียน** เพราะจะขัดกับการลงบัญชี
        และสรรพากรดูถ้อยคำในสัญญาเป็นหลักฐานสำคัญ
        · แลกกับ **เกณฑ์ VAT ฿1.8M มาถึงเร็วกว่า 4 เท่า** (~163 ชุด/เดือน แทน ~649)
          แต่ **กำไรจริงเท่ากันทั้งสองแบบ** (฿231.31/ชุด) ต่างแค่จังหวะชนเกณฑ์กับใครเป็นคู่สัญญา
        · เขียนไว้ที่ `/designer-agreement` ข้อ 4 + `/agreement` ข้อ 8 (อนุญาตช่วง sub-license)
          + `/terms` ข้อ 4 (แยกสองเส้นทาง: ขายรายชุด = แพลตฟอร์ม · ใบเสนอราคา = designer)
     2. **"75% ของอะไร"** — เขียนครั้งเดียวรองรับทั้งก่อน/หลังจด VAT ไม่ต้องกลับไปแก้สัญญา
        กับคนที่เซ็นไปแล้ว: *ฐานคำนวณ = ราคาขายหลังหัก VAT · ระหว่างยังไม่จด ใช้ราคาเต็ม*
        (฿990 → ก่อนจด ฿742.50 · หลังจด ฿693.92) + แจ้งล่วงหน้า 30 วันก่อนวันจดมีผล
        · **ราคาที่ประกาศไม่เปลี่ยน** ตอนจด VAT — ลูกค้ายังจ่าย ฿990 เท่าเดิม
     3. **หัก ณ ที่จ่าย 3%** + ออกใบ 50 ทวิทุกครั้ง — ✅ **ทำเสร็จแล้ว (1 ส.ค. 2569)**
        🔴 **แต่ประเภทเงินได้ยังต้องให้ CPA ยืนยัน** ระบบออกใบ 50 ทวิเป็น **แถว 3 เตรส
        + ภ.ง.ด.3/53 (หัก 3%)** ซึ่งเป็นวิธีที่ตลาดใช้กัน · ตามตัวอักษรแล้วค่าลิขสิทธิ์ฟอนต์
        เข้าข่าย **40(3)** ซึ่งต้องยื่น **ภ.ง.ด.2 อัตราก้าวหน้า** ทำเอกสารยากกว่ามาก
        · สัญญาจึงเขียนกลาง ๆ ว่า "เงินได้พึงประเมินตามกฎหมาย" ไม่ระบุมาตรา
        · ถ้า CPA ตอบว่าต้องเป็น 40(3) จริง → แก้ `WHT_INCOME_BASIS` + `F.row*`/`F.formPnd*`
        ใน `src/lib/wht-cert-doc.ts` ที่เดียว ตัววาดไม่ต้องแตะ
     4. **จังหวะจด VAT — ยังไม่เคาะ** เจ้าของกำลังหาข้อมูลว่าควรตั้งราคาแบบไหน
        สัญญาเขียนรองรับไว้แล้วจึงไม่บล็อกการเซ็น designer

1. **✅ ตรวจสด dashboard หลัง login — ผ่านครบทุกข้อแล้ว (1 ส.ค. 2569)**
   เจ้าของไล่เองครบทั้ง A–D · ที่เจอและแก้ไปในรอบเดียวกัน: ตะกร้ามือถือเล็กเกิน (44×44 แล้ว) ·
   หน้าเพิ่มฟอนต์เป็น 3 คอลัมน์ · ใบสรุปโอนเพิ่มหัวข้อ "ผู้รับเงิน" ·
   **บั๊กจำนวน weight/style ถูกเขียนทับ** (ดูตาราง "สถานะปัจจุบัน") · กล่องติ๊กหน้าสมัครสมาชิกลอยสูง
   · **ข้อที่ทดสอบไม่ได้ 2 ข้อ:** redirect ไป `/designer/onboarding` (ไม่มีบัญชีที่ตั้งค่าไม่ครบ)
   และปุ่มแชร์บน Android (ไม่มีเครื่อง) — ทั้งคู่ประเมินว่าไม่เสี่ยง
   **เช็คลิสต์เดิมเก็บไว้ด้านล่างสำหรับรอบหน้า** (เช่น หลังเปลี่ยนชื่อแบรนด์ ต้องไล่ซ้ำ)

   **🔴 A. ยังไม่เคยรันผ่านของจริงเลยสักครั้ง — เสี่ยงสุด ทำก่อน**
   - [ ] **`/admin/payouts` → กด "บันทึกจ่าย" จริง 1 รายการ** — `0073` เปลี่ยนจาก `insert` ตรง
         ไปเป็น RPC `record_payout` ที่คำนวณภาษีเอง **เส้นทางนี้ไม่เคยถูกกดบนของจริง**
         ถ้า client ยังส่งพารามิเตอร์ชุดเก่า = บันทึกจ่ายไม่ได้เลย (แล้วค่อยกดยกเลิกคืน)
   - [ ] **โหลดใบสรุปโอน + ใบ 50 ทวิ** — ตอน `LEGAL_ENTITY_IS_JURISTIC=false`
         เอกสารต้องหน้าตา**เหมือนก่อนมีฟีเจอร์นี้ทุกประการ** (ยังมีคำว่า "ยอดโอนรวม"
         ไม่มีบรรทัดหักภาษี) ถ้าเห็นบรรทัดภาษีโผล่ = สวิตช์รั่ว
   - [ ] **`/designer/pricing` → ปิด "ตั้งค่าสิทธิเอง" → บันทึก → เปิดกลับ**
         ค่า tier + ไฟล์ PDF เดิม**ต้องอยู่ครบ** (บั๊กเดิมล้างทิ้งถาวร เพิ่งแก้ 1 ส.ค.)
   - [ ] **สวิตช์ `quote_enabled`** — ปิดที่ `/designer/pricing` แล้วหน้าฟอนต์ของร้านนั้น
         ต้องไม่มีปุ่ม "ขอใบเสนอราคา" อีก

   **🟠 B. ของใหม่ที่ยังไม่ได้ดูด้วยตา**
   - [ ] `/admin/orders` — แท็บ Retail/Subscription + คลิก **"ประวัติดาวน์โหลด"** ต้องขึ้น popup
         (ฟอนต์/ชื่อไฟล์/เวลา/IP · ล่าสุดอยู่บนสุด)
   - [ ] `/admin/revenue` + `/designer/revenue` — **กด 4 กล่องด้านบน**ต้องกรองตารางได้จริง
   - [ ] `/admin/payouts` — เปลี่ยน dropdown ช่วงเวลา (ไตรมาสนี้/ก่อน/3 ปี/ทั้งหมด)
         · **badge "รอโอน X ราย" นับ all-time ส่วนคอลัมน์ในตารางเป็นรายไตรมาส — ตัวเลขไม่ตรงกัน
         คือถูกต้อง** ไม่ใช่บั๊ก
   - [ ] `/admin` + `/designer` — ช่องค้นหาฟอนต์ในตาราง
   - [ ] `/admin/font-review` — กดลบฟอนต์ ต้องมี modal ให้**พิมพ์ชื่อฟอนต์ยืนยัน**
   - [ ] `/designer/quotes` — ช่องเลขที่เอกสารในตารางต้องกว้างพอ (เพิ่งขยาย 80→120px)

   **🟡 C. เทียบ admin ↔ designer (ใช้ component ชุดเดียวกัน แก้ที่เดียวกระทบสองที่)**
   - [ ] เปิด `/admin/*` เทียบกับ `/designer/*` ทีละหน้า (pricing · settings · revenue ·
         quotes · analytics · add) หน้าตาต้องเหมือนกัน ต่างแค่ `/admin` **ไม่มีปุ่มลบฟอนต์**
   - [ ] **login ด้วยบัญชี designer จริง 1 ครั้ง** (ไม่ใช่ admin) — เส้นทางนี้แตะ RLS คนละชุด
   - [ ] `/designer/settings` — ช่อง "ที่อยู่" ต้องสูง 2 บรรทัด (เคยถูก CSS ทับเหลือบรรทัดเดียว)
   - [ ] designer ที่ยังตั้งค่าไม่ครบ ต้องถูกเด้งไป `/designer/onboarding`

   **📱 D. มือถือ (ย่อจอ < 768px)**
   - [ ] เมนู designer ต้องมี **"ออกจากระบบ" + "กลับหน้าแรก"** (เคยหายไปทั้งสองปุ่ม)
   - [ ] ไอคอนตะกร้าต้องเห็นบนมือถือ (เคยซ่อนหมดจนซื้อไม่ได้)
   - [ ] ปุ่มแชร์บนหน้าฟอนต์
1b. **✅ หัก ณ ที่จ่าย + ใบ 50 ทวิ — โค้ดเสร็จแล้ว (1 ส.ค. 2569) รอแค่สลับสวิตช์วันจดบริษัท**
   · **`LEGAL_ENTITY_IS_JURISTIC` ใน `src/lib/brand.ts` = สวิตช์ตัวเดียวของทั้งฟีเจอร์**
     วันจดบริษัทเปลี่ยนเป็น `true` แล้วระบบเริ่มหัก 3% + พิมพ์บรรทัดภาษี + ออกใบ 50 ทวิทันที
     ตอนเป็น `false` เอกสารหน้าตาเหมือนก่อนมีฟีเจอร์นี้ทุกประการ (รวมคำว่า "ยอดโอนรวม")
   · **`payoutBreakdown()` ใน `revenue.ts` = จุดเดียวที่รู้สูตร** หน้า admin / ใบสรุปโอน /
     ใบ 50 ทวิ ต้องเรียกตัวนี้หมด ไม่งั้นตัวเลขบนเอกสารสามใบไม่ตรงกัน
   · **⚠️ `payouts.amount` = ยอดเต็ม (gross) ไม่ใช่ยอดที่โอนจริง** — `net_amount` ต่างหาก
     คือเงินเข้าบัญชี (กลับทางกับที่เคยเขียนไว้ในเช็คลิสต์เดิมว่า "ช่องบันทึกต้องเป็นยอดสุทธิ")
     เหตุผล: ใบ 50 ทวิบังคับให้ระบุ *จำนวนเงินที่จ่าย* คู่กับ *ภาษีที่หัก* เก็บแค่สุทธิจะออกใบไม่ได้
     และ "จ่ายแล้วรวม" จะต่ำกว่าภาระที่ปลดจริง (ปลดหนี้ ฿10,000 = โอน ฿9,700 + นำส่งสรรพากร ฿300)
   · **ใบ 50 ทวิ = กรอกฟอร์มจริงของกรมสรรพากร ไม่ได้วาดเอง** (`public/forms/wh3.pdf`)
     เป็น AcroForm 81 ช่อง · **กับดัก 3 ข้อ อยู่ในหัวไฟล์ `src/lib/wht-cert-doc.ts`**:
     ต้อง `updateFieldAppearances(ฟอนต์ไทย)` ไม่งั้นไทยพัง · ช่องเลข 13 หลักเป็น **comb**
     ซึ่ง pdf-lib ทำไม่ได้ ต้องวาดเองทีละตัว · คอลัมน์ บาท|สตางค์ ต้องวาดแยกสองฝั่งของเส้นแบ่ง
     (วัดจาก content stream: เส้นที่ x=475 และ x=546.4 → ช่องสตางค์กว้าง ~15pt)
   · **สองอัตราที่ห้ามรวมกัน** — `QUOTE_WHT_RATE` (ลูกค้าองค์กรหักจาก designer ในใบเสนอราคา
     แพลตฟอร์มไม่แตะเงิน) กับ `PAYOUT_WHT_RATE` (บริษัทหักตอนจ่ายส่วนแบ่ง) อยู่ใน `revenue.ts`
     ทั้งคู่พร้อมคอมเมนต์ — บังเอิญเท่ากันที่ 3% แต่คนละธุรกรรม
   · `record_payout` RPC (`0073`) — เดิมหน้า admin `insert` ตรง ๆ ซึ่งเรียก `next_doc_no()`
     ไม่ได้ (ถูก revoke ใน `0032`) · RPC **คำนวณภาษี/ยอดสุทธิเองไม่เชื่อค่าจาก client**
   · **ยังเหลือทำวันจดบริษัท:** แก้ `LEGAL_ENTITY_*` (ชื่อ/เลขผู้เสียภาษี/ที่อยู่บริษัทใหม่)
     + `LEGAL_ENTITY_IS_JURISTIC = true` ใน `src/lib/brand.ts` ที่เดียว แล้ว redeploy
   · `users.vat_registered` เก็บแล้วแต่ยังไม่ถูกใช้คำนวณ (UI เขียนเองว่า "ยังไม่มีผลต่อ
     การคำนวณ") — ต้องเคาะพร้อมข้อ 0b.2
2. **Stripe ขั้นที่ 2 — ทดสอบบน production ด้วย test key** (ขั้นที่ 1 ที่เครื่องผ่านแล้ว)
   · **ทำได้เลยไม่ต้องรอข้อ 0/0b** — ขั้นนี้เป็น test key บน `pages.dev` ซึ่งเป็นของทิ้ง
     webhook โหมด test/live เป็นคนละตัวต้องสร้างใหม่ตอน go-live อยู่แล้ว · ที่ต้องรอ DNS คือขั้นที่ 3
   · ~~push + deploy โค้ดล่าสุดก่อน~~ ✅ ขึ้นแล้ว (`e7463bb`, 1 ส.ค. 2026)
   · ~~Stripe (โหมด Test) → Webhooks → endpoint~~ ✅ **สร้างแล้ว (1 ส.ค. 2026)** ผ่าน Stripe API
     `we_1TzN0NDWIHX6MqtE4CSqrwI8` → `https://dhammadha-web.pages.dev/api/stripe-webhook`
     `livemode:false` · events ครบ 2 ตัว (`checkout.session.completed` +
     `checkout.session.async_payment_succeeded`) · **`whsec_` ไม่เก็บใน repo** — อยู่ใน Pages env
     · ⚠️ ค่าใน `.env.local` เป็นคนละตัว (ของ `stripe listen` สำหรับ dev) **ห้ามทับกัน**
   · ~~Cloudflare Pages env~~ ✅ ตั้งครบ + redeploy แล้ว (1 ส.ค. 2026)
     **กับดัก: env ใหม่ของ Pages ไม่มีผลกับ deployment เดิม ต้อง build ใหม่เสมอ**
     · วิธีเช็ค: `curl -X POST <โดเมน>/api/stripe-webhook -d '{}'` ต้องได้ **`bad_signature` (400)**
       ถ้าได้ `not_configured` (500) แปลว่า env ยังไม่เข้า
   · **ผลซื้อจริงบน production ด้วย test key (1 ส.ค. 2026): เงิน→order→สิทธิ์ ผ่านหมด**
     `OR-2569-0009` ฿1,300 (บัตร) · `OR-2569-0010` ฿1,100 (PromptPay) · ส่วนแบ่ง 75/25 ตรงทั้งสองใบ
     · `order_items` 4 แถว + `entitlements` 4 สิทธิ์ครบ · เลขที่เอกสารเดินต่อไม่ข้าม
     · **ตะกร้าข้ามร้านผ่านบน production แล้ว** (`OR-2569-0010` `designer_id=null`,
       `order_items` มี designer 2 คน) — เส้นทางที่เคยพังตอนทำ 0069
   · ~~อีเมล delivery ไม่ออก~~ ✅ **แก้แล้ว (1 ส.ค. 2026, `94be26f`) — `pending_webhooks=0`
     ทั้งสอง event · อีเมลเข้าจริงทั้ง 2 ใบ · ถือว่าขั้นที่ 2 ผ่านครบ**
     · **🔴 สาเหตุ = กับดักที่จะเจอซ้ำได้อีก: `SUPABASE_SERVICE_ROLE_KEY` บน Pages เป็น
       secret key แบบใหม่ของ Supabase (`sb_secret_…`) ซึ่ง "ไม่ใช่ JWT"**
       `supabaseSelect` ส่ง `apikey=anon` + `Authorization: Bearer <secret key>` → gateway
       ยึด role จาก apikey (=anon) แล้วส่ง Bearer ต่อให้ PostgREST ซึ่ง parse ไม่ได้ →
       `PGRST301 Expected 3 parts in JWT; got 1` → อ่าน order ไม่ได้ → `order_not_found`
     · **ทำไมขั้นที่ 1 ที่เครื่องผ่าน: `.env.local` เป็น key แบบเก่า (JWT) — บั๊กโผล่เฉพาะ production**
       · **ทำไม order ยังถูกสร้าง:** RPC ใส่ key ไว้ทั้งสอง header อยู่แล้วเลยรอดโดยบังเอิญ
     · **กติกาของ header (จำไว้):** `apikey` → gateway อ่าน รับ key ได้ทั้งสองแบบ ·
       `Authorization: Bearer` → PostgREST อ่าน **รับ JWT เท่านั้น**
       · ทดสอบแล้ว: ส่ง `apikey` อย่างเดียว **ใช้กับ key แบบเก่าไม่ได้** (ตกเป็น role anon →
         `42501 permission denied`) · ใส่ key ทั้งสอง header ใช้ได้ทั้งสองแบบ = สูตรที่เลือก
     · ~~หนี้ที่ยังเปิด: `supabaseSelectAsService` ส่ง `apikey` อย่างเดียว~~ ✅ **แก้แล้ว
       (1 ส.ค. 2569)** ใส่ key ทั้งสอง header + เพิ่ม `diag` · **หมายเหตุที่เคยเขียนไว้ว่า
       "ตอนนี้ใช้ได้เพราะ Pages เป็น key แบบใหม่" นั้นถูกเฉพาะ production — ที่เครื่องพังจริง
       มาตลอด** เพราะ `.env.local` เป็น key แบบเก่า (เส้นทางแจ้ง designer เรื่องใบเสนอราคา)
     · **`.env.local` ยังเป็น key แบบเก่า = ยังต่างจาก production อยู่** (ไม่บล็อกแล้วเพราะโค้ด
       รองรับทั้งสองแบบ แต่ถ้าอยากให้ dev เหมือน prod ต้องเปลี่ยนเป็น `sb_secret_…`)
     · **หนี้ที่แก้ไปแล้วระหว่างไล่:** `sendResendEmail` กับ `supabaseSelect` เคยกลืน error ทิ้ง
       (`return res.ok` / `return null`) ทำให้ delivery log ของ Stripe บอกได้แค่ `email_failed`
       ตอนนี้ส่งสาเหตุจริงออกมาถึง response แล้ว (`d7f28a1`, `34bd6dc`)
   · ~~เจอระหว่างทดสอบ: อีเมลขึ้น "เรียน คุณ " ห้อยลอย + "สิทธิ์ใช้งาน: personal"~~
     ✅ **แก้แล้ว (`0d70d89`)** — **`greetingLine()` ไม่มีชื่อ = "สวัสดีครับ"**
     · **PromptPay ไม่เก็บชื่อผู้จ่าย** (`customer_details.name` เป็น null) จึงกระทบลูกค้า
     PromptPay **ทุกคน** ไม่ใช่เคสหายาก (ยืนยัน: `OR-2569-0010` null / ใบบัตร `OR-2569-0009` มีชื่อ)
     · **`LICENSE_LABEL` ไม่มี key `personal`** → `licenseLabel()` ตกไปคืนค่าดิบ เติมแล้ว
   · **Terms of service URL → `https://<โดเมน>/agreement/`** (Settings → Public details)
     ตั้งได้ **ค่าเดียวทั้งบัญชี override ต่อ session ไม่ได้** จึงชี้ไปสัญญาอนุญาต ไม่ใช่ `/terms/`
     — หน้า `/agreement/` มีย่อหน้า "ร่ม" อ้างถึงข้อกำหนดการใช้งาน + สัญญาฉบับของดีไซน์เนอร์แล้ว
     (29 ก.ค. 2026) · ต้องตั้ง **ทั้งโหมด test และ live** ไม่งั้นสร้าง session ไม่ได้ = ปุ่มซื้อพัง
     · **โหมด test ตั้งไว้แล้ว** (ยืนยัน 1 ส.ค. 2026 — สร้าง session ผ่าน ถ้าไม่ตั้งจะ error ตั้งแต่แรก)
   · ~~⚠️ ต้องลองตอนนี้: `custom_text[terms_of_service_acceptance][message]`~~ ✅ **เสร็จแล้ว
     (1 ส.ค. 2026)** — ตั้งใน `checkout-service.ts` (`tosAcceptanceMessage(site)`) ทดสอบบน
     หน้าจ่ายเงินจริงด้วย test key แล้ว ได้ข้อสรุปสองข้อที่**เอกสาร Stripe ไม่ได้เขียนไว้**:
     **(1) รองรับลิงก์ markdown** `[ข้อความ](URL)` → เรนเดอร์เป็น `<a href>` จริง (ยอมรับ
     `http://localhost` ด้วย ไม่บังคับ https) **(2) การตั้ง `custom_text` ลบข้อความ default
     ทิ้งทั้งก้อน รวมลิงก์ ToS ที่ Stripe ใส่ให้เองด้วย** → ถ้าไม่ใส่ลิงก์ในข้อความนี้ หน้าจ่ายเงิน
     จะไม่มีทางเข้าถึงสัญญาอนุญาตเลย (ช่องติ๊กเหลือกล่องเปล่า) **ลิงก์ในข้อความจึงห้ามถอด**
     · ลิงก์สร้างจาก `${site}` (origin ของ request) ไม่ hardcode โดเมน → ย้ายโดเมนแล้วตามเอง
     · **(3) ขึ้นบรรทัดใหม่ในข้อความนี้ทำไม่ได้ — ปิดประเด็นแล้ว อย่าลองซ้ำ** (ทดสอบ
       1 ส.ค. 2569 ด้วยการยิง session probe ตรงไป Stripe API แล้วอ่าน DOM ของหน้าจริง)
       Stripe ยัดข้อความทั้งก้อนลง `<p>` เดียวที่เป็น `white-space: normal` และ parse
       **เฉพาะลิงก์ markdown** เท่านั้น · `<br>` ถูก escape เป็น `&lt;br&gt;` ·
       `- ` (markdown list) ไม่ทำงาน · `\n` / `\n\n` / two-space hard break ตกถึง DOM
       จริงแต่ CSS ยุบเป็นช่องว่าง · **U+2028/U+2029 ก็ไม่ break** (วัดด้วย
       `Range.getClientRects()` บนหน้าจริง) → ต้องพึ่งตัวคั่นที่มองเห็นเท่านั้น ใช้ ` • `
       · **กับดัก: ใส่ `\n` แล้วดูเหมือนไม่พัง แต่ผลคือข้อความติดกันโดยไม่มีตัวคั่นเลย**
       ซึ่งอ่านยากกว่าเดิม
   · ซื้อจริงบนเว็บ PromptPay 1 + บัตร 1 → delivery ต้อง 200 → ตรวจ order/อีเมล/ไฟล์
3. **Stripe ขั้นที่ 3 — live key** (ทำหลัง DNS cutover จะได้ตั้ง webhook รอบเดียว)
   · webhook ของโหมด live **ต้องสร้างใหม่** ได้ `whsec_` คนละตัว
   · ซื้อจริงฟอนต์ถูกสุด PromptPay + บัตร แล้ว refund ทั้งคู่
3b. **ล้างข้อมูลทดสอบใน DB** ก่อนเปิดขาย — ✅ **สคริปต์พร้อมแล้ว (1 ส.ค. 2026):
   `scripts/reset-test-data.sql`** รันทีละส่วน 1→7 · **ยังไม่รัน — ต้องรอ Stripe ขั้นที่ 3 จบก่อน**
   เพราะการทดสอบสร้าง order เพิ่มทุกรอบ
   · **เป้าหมายที่เจ้าของกำหนด: หลังรันเสร็จเหลือแค่ `users` กับฟอนต์ที่ขายจริง**
     `quotes` + `payouts` จึงย้ายจาก "เลือกรันเอง" มาเป็น **ขั้นตอนบังคับ (ส่วนที่ 3)**
     ลบยกตารางเพราะสองตารางนี้ไม่มีเครื่องหมายแยก test/live ในตัวเอง
     → **ห้ามรันสคริปต์นี้หลังเปิดขายจริงเด็ดขาด** (ส่วน order ยังมีตาข่ายกัน `cs_live_*` อยู่)
   · **ลำดับที่ตรวจจาก FK จริงแล้ว — แก้จากที่เคยเข้าใจผิด:** `payouts` **ไม่มี FK
     ผูกกับ orders เลย** ลบตอนไหนก็ได้ · ตัวที่บังคับลำดับจริงคือ `orders.quote_id`
     ซึ่งเป็น **SET NULL ไม่ใช่ RESTRICT** → ลบ quotes ก่อนจะไม่ error แต่ไปตัด
     สายสัมพันธ์ของ order ที่ยังอยู่เงียบ ๆ จึงต้องลบ order ให้หมดก่อนเสมอ
   · **`doc_counters` เพิ่ม `PO`/`WT` (จาก `0073`) และ `IV` (จาก `0076`)** — ของเดิมรีเซ็ตแค่ OR/QT/RC
     ทำให้ใบสรุปโอนใบแรกของจริงจะไม่ได้เลข `PO-xxxx-0001` (ตอนนี้ PO เดินไปถึง 6 แล้ว)
   · เกณฑ์คัด: `order_no like 'ORTEST%'` **หรือ** `provider_session_id like 'cs_test_%'`
     (ชื่อคอลัมน์คือ **`provider_session_id`** ไม่ใช่ `stripe_session_id` — เคยจดผิดในไฟล์นี้)
     ตอนนี้จับได้ครบ 11/11 ใบ · ใบ live (`cs_live_*`) ไม่โดนแตะโดยตั้งใจ
   · ลบ `orders` พอ — `order_items`/`entitlements`/`download_logs` CASCADE ตามให้เอง
   · **🔴 ลำดับสลับไม่ได้: `order_items.font_id → fonts` เป็น `ON DELETE RESTRICT`**
     (FK ตัวเดียวในระบบที่ไม่ใช่ CASCADE) → ต้องล้าง order ก่อนถึงจะลบฟอนต์ทดสอบได้
   · รีเซ็ต `doc_counters` — ตอนนี้ `OR`=8 `QT`=5 `RC`=4
   · การลบฟอนต์**ไม่ล้างไฟล์ใน Storage** (ปุ่มลบใน admin ก็เหมือนกัน) ต้องจดพาธก่อนลบ
3c. **ถอดฟอนต์ทดสอบออกจากเว็บ** — ✅ **เสร็จแล้ว (1 ส.ค. 2026)** — **มี 4 ตัวไม่ใช่ 2 ตาม
   ที่เคยจด**: `test-error` (฿25,000), `test-error-free-font`, `test-03`, `test-02`
   · ตั้ง `is_active=false` ทั้ง 4 (ไม่ลบถาวร — ย้อนกลับได้ และการลบถาวรอยู่ในสคริปต์ข้อ 3b แล้ว
     ไม่ต้องทำสองรอบ · `test-02` ลบตอนนี้ไม่ได้อยู่แล้วเพราะติด RESTRICT จาก order_items 2 แถว)
   · ผลจริง: เหลือฟอนต์บนเว็บ 22 ตัว · หายจากหน้าแรก/สไลด์/`/fonts` · `/api/checkout`
     คืน `font_not_found` แล้ว · **หน้า detail เป็น SSG — URL เก่ายังเข้าได้จนกว่าจะ deploy รอบถัดไป**
4. ย้ายฟอนต์ 35 ตัวจากเว็บเก่า (ใช้ปุ่ม ⚡ ใน FontForm)
5. Zoho Mail `info@dhammadha.com` (ตอนนี้ใช้ `dhammadha@outlook.com` ชั่วคราว)
   · **พอมีเมลจริงแล้วต้องกลับไปแก้ Stripe → Settings → Business → Customer support email**
   (ตอนนี้เป็น outlook ชั่วคราว ลูกค้าเห็นอีเมลนี้บนหน้าจ่ายเงิน/ใบเสร็จของ Stripe)
6. DNS cutover → dhammadha.com (ต้อง proxied/เมฆส้ม) + **อัป Supabase Auth Site URL/Redirect URLs**
7. เปิดตัว + เริ่มชวน designer ผ่าน `/become-a-designer`
8. **Phase 4.1 Subscription — C2 (Tauri desktop app) แล้วต่อ D (เปิด trial)**
   · **ทำขนานได้ระหว่างรอ go-live** — ช่วงทดสอบเป็น ฿0 สมัครตรงผ่าน RPC ไม่แตะ payment gateway
   · C1 (`sub-font` Edge Function) เสร็จแล้ว → API ที่แอปต้องใช้พร้อมและพิสูจน์ด้วย JWT จริงแล้ว
   · **ก่อนเริ่ม C2:** ติดตั้ง Rust toolchain (ยังไม่มีบนเครื่อง) · **ควรเคาะชื่อแบรนด์ก่อน scaffold**
     เพราะ bundle identifier (`com.<brand>.app`) ฝังลง code signing + การลงทะเบียนระดับ OS
     เปลี่ยนทีหลัง = ผู้ใช้ต้องติดตั้งใหม่
   · Apple Developer ID ($99/ปี) + Windows code-signing cert — ไม่บล็อกการทดสอบวงปิด
9. **Milestone E (ที่แผนเดิมไม่มี) — recurring billing จริง** ยังไม่มีโค้ด `mode:"subscription"`
   ที่ไหนเลย (`checkout-service.ts` เป็น `mode:"payment"` อย่างเดียว) · **บล็อกโดยการจดบริษัท**
   (บัญชี Stripe ต้องตรงนิติบุคคล) · **ตอนถึงจุดนั้นต้องทบทวน Payso อีกครั้ง** — บันทึก 29 ก.ค.
   ระบุว่า ฿290/ด. Payso 3.2% vs Stripe 7.6% (฿10 คงที่ทำร้ายยอดเล็ก) ถ้าอยู่กับ Stripe
   ให้ดันรายปี ฿2,900 เป็นตัวหลัก (4.3%)

รายละเอียด/เหตุผลของแต่ละข้อ (Stripe/ย้ายฟอนต์/DNS ฯลฯ) → [docs/ROADMAP.md](docs/ROADMAP.md) แต่ละ Phase ·
Phase D dashboard → [docs/design/DESIGN.md](docs/design/DESIGN.md) §18

## แผนถึง go-live + ประมาณการเวลา (คิดไว้ 1 ส.ค. 2569)

> **สมมติฐานที่ใช้คำนวณ (เจ้าของยืนยัน 1 ส.ค. 2569):** เคาะชื่อ+จดโดเมนภายในสัปดาห์นี้ ·
> จดบริษัทยังไม่เริ่ม **จะยื่นเอง** · ฟอนต์ 35 ตัวไฟล์+ข้อมูลพร้อมหมด แค่ยกขึ้น
> — สมมติฐานข้อไหนเปลี่ยน ให้เลื่อนวันตามนั้น **1:1** โดยเฉพาะข้อแรก

**พร้อมเปิดขายจริง ~8–15 ก.ย. 2569 (≈5–6 สัปดาห์)** · เร็วสุด ~ต้น ก.ย. ถ้าธนาคาร/Stripe ไม่สะดุด ·
ช้าสุดที่ยังสมเหตุสมผล ~ต้น ต.ค. ถ้ายื่นจดบริษัทเองแล้วโดนตีกลับ 1 รอบ

**คอขวดไม่ใช่โค้ด** — งานโค้ดที่เหลือทั้งหมดรวมกัน ~3–5 รอบงาน ใส่ลงในช่องว่างระหว่างรอเอกสารได้หมด

### Critical path (ต่อคิวกัน ทำขนานไม่ได้)

| # | งาน | ใคร | ใช้เวลา |
|---|---|---|---|
| 1 | เคาะชื่อ + จดโดเมน + ค้นเครื่องหมายการค้า (จำพวก 9/35/42) | เจ้าของ | 2–5 วัน |
| 2 | จองชื่อนิติบุคคล DBD (ชื่อควรล้อกับแบรนด์) | เจ้าของ | 1–3 วัน |
| 3 | ยื่นจดตั้งบริษัท (e-Registration ยื่นเอง) | เจ้าของ | 1–2 สัปดาห์ |
| 4 | เปิดบัญชีธนาคารนิติบุคคล (ต้องมีหนังสือรับรองก่อน) | เจ้าของ | 1–2 สัปดาห์ |
| 5 | เปิด Stripe ในนามบริษัท + KYC + ผูกบัญชี | เจ้าของ | 2–5 วัน |
| 6 | DNS cutover (proxied/เมฆส้ม) + Supabase Site URL — **ข้อ 6 เดิม** | เจ้าของ+Claude | 1 วัน (+propagate 24 ชม.) |
| 7 | Stripe ขั้นที่ 3 live key + webhook ใหม่ + ซื้อจริง/refund — **ข้อ 3 เดิม** | Claude+เจ้าของ | 0.5 วัน |
| 8 | ล้างข้อมูลทดสอบ `scripts/reset-test-data.sql` — **ข้อ 3b เดิม** | Claude | 1 ชม. |
| 9 | **เปิดขาย + เริ่มชวน designer** (ข้อ 7 เดิม) | | |

**ทำไมยุบขั้นไม่ได้:** บัญชี Stripe ต้องตรงกับนิติบุคคลที่รับเงิน (ข้อ 0b) — เปิดขายในนามบุคคลก่อน
= onboard Stripe ใหม่ทั้งชุด + ทำสัญญา designer ใหม่ · และ webhook โหมด live ต้องสร้าง**หลัง** DNS
ไม่งั้นสร้างสองรอบ · ข้อ 6 ทางเทคนิคทำได้ตั้งแต่หลังข้อ 1 แต่ควรทำติดกับข้อ 5 ด้วยเหตุผลนี้

### งานขนาน (ทำระหว่างรอข้อ 3–5 — ไม่กินเวลาบน critical path)

**เจ้าของ:** ยกฟอนต์ 35 ตัว (ข้อ 4 เดิม, ~4–6 ชม. · publish ยิง deploy hook ทุกตัว ทยอยเป็นชุด) ·
ตรวจสด dashboard หลัง login (ข้อ 1 เดิม, ~1–2 ชม. Claude ทำแทนไม่ได้) · โลโก้/wordmark ใหม่ ·
Zoho MX โดเมนใหม่ (ข้อ 5 เดิม — **อย่าลืมกลับไปแก้ Stripe → Business → Customer support email**) ·
ถาม CPA (3 เตรส vs 40(3) + จังหวะจด VAT) · อัป Supabase Pro ตอนเริ่มขายจริง

**Claude:** `src/lib/brand.ts` (ทันทีที่เคาะชื่อ) → **redeploy `download-font` Edge Fn**
(มี `BRAND_DOMAIN` สำเนาของตัวเอง ไม่ redeploy = ไฟล์ที่ขายถูกประทับโดเมนเก่า) ·
ทบทวนถ้อยคำหน้ากฎหมาย (ข้อ 0.ค — แก้เนื้อสัญญาที่มีผลแล้วต้อง `npm run legal:freeze` ก่อน) ·
CSP · เผื่อ 1 รอบงานสำหรับสิ่งที่เจอตอนตรวจสด dashboard · (ตัวเลือก) `.env.local` → `sb_secret_…`
· **เว็บสตูดิโอ `dhammadha.com` (ข้อ 0.จ) แนะนำเลื่อนไปหลัง launch** — redirect ไปหน้าร้านก่อนก็พอ
(แผนลงมือเขียนไว้ครบแล้ว `~/.claude/plans/typedee-com-compiled-waterfall.md` — **ห้ามเริ่มก่อน DNS cutover**
เพราะปุ่มทุกปุ่มบนเว็บสตูดิโอชี้ไปโดเมนใหม่ที่ยังไม่มีอยู่จริง)

### ปฏิทินคร่าว ๆ (สมมติเคาะชื่อ 7 ส.ค. 2569)

| สัปดาห์ | critical path | ขนาน |
|---|---|---|
| 4–8 ส.ค. | เคาะชื่อ · จดโดเมน · ค้น TM · จองชื่อ DBD | `brand.ts` + redeploy `download-font` + ถ้อยคำกฎหมาย |
| 11–22 ส.ค. | ยื่นจดตั้งบริษัท | ยกฟอนต์ 35 ตัว · ตรวจสด dashboard · โลโก้ · Zoho MX · CSP |
| 25 ส.ค.–5 ก.ย. | เปิดบัญชีธนาคารนิติบุคคล | แก้ `LEGAL_ENTITY_*` + `IS_JURISTIC=true` · เก็บงานที่เจอ |
| 8–12 ก.ย. | Stripe ในนามบริษัท → DNS cutover → Stripe ขั้นที่ 3 → ล้างข้อมูลทดสอบ | อัป Supabase Pro |
| **~15 ก.ย.** | **เปิดขาย** | |

### ความเสี่ยงที่จะทำให้เลื่อน (เรียงตามโอกาสจริง)

1. **เปิดบัญชีธนาคารนิติบุคคล — แกว่งที่สุด ไม่ใช่การจดบริษัท** บางสาขานัดคิว/ต้องมีตราประทับ/
   กรรมการครบ → โทรถามสาขาเรื่องเอกสารตั้งแต่**วันที่ยื่นจด** ไม่ใช่วันที่ได้หนังสือรับรอง
2. ยื่นจดบริษัทเองแล้วโดนตีกลับ — เสีย ~1 สัปดาห์/รอบ
3. Stripe KYC ขอเอกสารเพิ่ม — ปกติ 2–5 วัน บวกได้อีก ~1 สัปดาห์
4. ชื่อ/โดเมนหาไม่ลงตัว — **เลื่อนทุกอย่าง 1:1**
5. ตรวจสด dashboard เจอของพังเกินคาด — เผื่อไว้ 1 รอบงานแล้ว

**ยืนยันแล้วว่า *ไม่* บล็อก launch:** คำตอบ CPA เรื่อง 3 เตรส/40(3) (รอบจ่ายส่วนแบ่งแรก ต.ค. 2569
ยังมีเวลา) · จด VAT (สัญญาเขียนรองรับทั้งก่อน/หลังแล้ว) · Phase 4.1 Subscription (ข้อ 8 เดิม) ·
เว็บสตูดิโอ dhammadha.com · `users.vat_registered` ที่ยังไม่ถูกใช้คำนวณ

## เช็คลิสต์ความปลอดภัยก่อน go-live (ตรวจ 1 ส.ค. 2569)

รันจาก Supabase advisors + ไล่พิสูจน์ทีละข้อด้วย SQL — **ไม่พบช่องโหว่ที่ทำให้ข้อมูลลูกค้า
หรือเงินรั่ว** ฐาน RLS แน่นตามที่ออกแบบไว้ · ที่พบเป็นเรื่อง enumeration กับสแปม
(`/code-review` กับ `/security-review` ตรวจ **diff** เท่านั้น ตอนไม่มีของค้างจะได้ผลว่าง
ใช้ตรวจทั้งระบบไม่ได้ — อย่าเข้าใจผิดซ้ำ)

**เจ้าของต้องทำเอง (Claude ทำแทนไม่ได้):**
1. ✅ **`TURNSTILE_SECRET_KEY` — ตั้งบน Pages (Production) แล้ว + พิสูจน์จากของจริงแล้ว
   (1 ส.ค. 2569)** ยิง `/api/send-email` บน production ด้วย token ปลอม / ไม่ส่ง token เลย /
   เส้นทาง contact → ได้ **`403 turnstile_failed` ทั้งสามแบบ** = เส้น fail-closed ทำงานจริง
   · **วิธีเช็คซ้ำวันหลัง (ไม่ส่งอีเมลจริงถ้าตั้งถูก):**
     `curl -X POST <โดเมน>/api/send-email -H 'Content-Type: application/json' -d '{"type":"contact","turnstile_token":"x","payload":{}}'`
     ต้องได้ 403 · ถ้าได้อย่างอื่น = env หลุด และคำขอนั้นจะกลายเป็นอีเมลจริง
   · เหตุผลที่ต้องเฝ้า: [email-service.ts:254](src/lib/email-service.ts:254) เขียนว่า
     `if (!env.TURNSTILE_SECRET_KEY) return true;` **ไม่ตั้ง = Turnstile ถูกข้ามทั้งระบบ**
     · ⚠️ Pages แยก env **Production / Preview** คนละชุด ตั้งผิดฝั่ง = production ไม่มีผล
2. เปิด **Leaked Password Protection** (เช็ครหัสผ่านกับ HaveIBeenPwned)
   · **🔴 เปิดตอนนี้ไม่ได้ — เป็นฟีเจอร์ของแผน Pro ขึ้นไป และ org ยังเป็น `free`**
     (ยืนยันจาก API: org `DHAMMADHA STUDIO` plan = free) นี่คือเหตุผลที่หาเมนูไม่เจอ
     ไม่ใช่หาไม่พบ · **ผูกข้อนี้ไว้กับตอนอัป Supabase Pro ในสัปดาห์ go-live** ทำก่อนไม่ได้
   · ที่อยู่เมนูตอนอัปแล้ว: **Authentication → Sign In / Providers → Email →
     "Prevent use of leaked passwords"**
     ลิงก์ตรง: `https://supabase.com/dashboard/project/skyuqpshxsskjwkbnrmd/auth/providers?provider=Email`

**งานโค้ด:** ✅ `0074_security_hardening.sql` **apply แล้ว + พิสูจน์แล้วบน production** —
ปิด list ไฟล์ใน public bucket 7 อัน · `set_updated_at` ตรึง search_path ·
`submit_public_quote` จำกัด 5 คำขอ/อีเมล/ชั่วโมง (หน้า `/quote` แปล `rate_limited` เป็นไทยแล้ว)
· ผลตรวจ: `list` ทั้ง 7 bucket คืน `[]` แต่ไฟล์จริงทุก bucket ยังได้ `200`
(covers/previews/tester-cache/fonts-demo/fonts-free/specimens/license-pdf) · เปิดเว็บจริงแล้ว
หน้าแรก/`/fonts`/หน้าฟอนต์ขึ้นครบ ไม่มี error ใน console · ยิง RPC ครั้งที่ 6 ได้ `rate_limited`
จริง **และเลี่ยงด้วยการสลับตัวพิมพ์/เว้นวรรคไม่ได้** (`lower(trim())`) · ลบแถวทดสอบทิ้งแล้ว

**🔴 `supabase db push` ใช้กับโปรเจกต์นี้ไม่ได้ — ต้อง apply ผ่าน MCP `apply_migration` เท่านั้น**
remote เก็บเวอร์ชันเป็น timestamp (`20260731174441` = `payout_wht`) ส่วนไฟล์ในรีโปตั้งชื่อ
`0073_payout_wht.sql` → CLI จับคู่ไม่ได้แล้วขอให้รัน `migration repair --status reverted`
ซึ่ง**ห้ามทำเด็ดขาด** (จะ replay `0001` ทับของจริง เช่น `0072` จะ re-run
`update designer_license_config set quote_enabled = true` = เปิดใบเสนอราคาให้คนที่ปิดไว้เอง)
· เหตุผลเต็มอยู่ใน [CLAUDE.md](CLAUDE.md) §Database

**⚠️ ตรวจแล้วปลอดภัย — ห้ามแก้ตาม linter** (advisor รายงานเป็น ERROR/WARN แล้วชวนให้แก้ผิดทาง):
- **`designer_profiles` (ระดับ ERROR)** — เปิดแค่ `id, name, business_name, designer_slug,
  portfolio_url` ของคนที่มี slug **ไม่มีอีเมล/เบอร์/บัญชีธนาคาร/เลขผู้เสียภาษี** ·
  แก้ตาม linter = ต้องเปิด RLS ของ `users` ให้ anon = **แย่กว่าเดิม**
- `handle_new_user` / `rls_auto_enable` — เป็น trigger กับ event_trigger function
  Postgres ไม่ยอมให้เรียกตรง เรียกผ่าน REST ไม่ได้
- `record_payout` — มี `get_my_role() <> 'admin' → 42501` + ตรวจช่วงค่าครบ
- `verify_order` — ปิดบังชื่อผู้ซื้อเป็น `X***`
- `subscription_waitlist` — table grant ให้ `authenticated` แต่ policy กั้นที่ admin (RLS ยังกั้น)
- `doc_counters` RLS เปิดแต่ไม่มี policy = **ปฏิเสธทุกคนโดยตั้งใจ** ตั้งแต่ revoke `next_doc_no()` ใน `0032`

**Performance advisor: 163 รายการ (154 WARN / 9 INFO) — จงใจยังไม่แก้ทั้งหมด**
`multiple_permissive_policies` 132 ตัวคือก้อนใหญ่สุด **ห้ามแตะก่อน launch** เพราะ permissive
policy ถูก OR กัน การยุบรวมเปลี่ยนความหมายของสิทธิ์ · `auth_rls_initplan` 22 ตัว
(`auth.uid()` เรียกทุกแถว → `(select auth.uid())`) คุ้มที่สุดแต่ทำหลัง launch ·
`unindexed_foreign_keys` 7 + `unused_index` 2 ตารางยังเล็กเกินกว่าจะวัดผลได้

## แผนที่เอกสาร — ไปอ่านที่ไหนต่อ

| ต้องการอะไร | ไปที่ |
|---|---|
| Commands, route structure, auth/roles, DB tables | [CLAUDE.md](CLAUDE.md) |
| แผนธุรกิจเต็ม, รายละเอียดทุก phase backend, เช็คลิสต์ go-live | [docs/ROADMAP.md](docs/ROADMAP.md) |
| ดีไซน์ระบบ (สี/ฟอนต์/spacing), ประวัติ redesign แต่ละ phase, กับดัก/หนี้ที่จงใจไม่แก้ | [docs/design/DESIGN.md](docs/design/DESIGN.md) |
| Subscription phase 4.1 แผนละเอียด | [docs/PHASE-4.1-SUBSCRIPTION.md](docs/PHASE-4.1-SUBSCRIPTION.md) |
| Decision/gotcha ที่ derive จาก docs ไม่ได้, feedback วิธีทำงาน, งานค้างล่าสุด | memory index: `~/.claude/projects/-Users-montonn-Desktop-dhammadha-web/memory/MEMORY.md` |

## วิธีทำงานของ Claude ในโปรเจกต์นี้ (ย่อจาก memory — [[feedback_git_workflow]] [[feedback_scope]])

- Git: commit ในเครื่องบน branch ปัจจุบันเท่านั้น **ห้าม push/deploy/สร้าง branch เอง** — เจ้าของ push+deploy เอง
- ทำเฉพาะสิ่งที่สั่ง ห้ามแก้ที่ไม่เกี่ยวข้อง; copy หน้าเว็บห้ามใช้คำ B2B/B2C
- งานใหญ่หลายไฟล์/หลาย section (เช่นแก้ตาม DESIGN.md ทีละ phase) — เก็บ note ระหว่างทางไว้ใน
  scratchpad ของ session (ต่อ subsection ที่ทำ) แทนการเปิดอ่านไฟล์เอกสารใหญ่ซ้ำทั้งไฟล์ทุกครั้ง
  ที่ต้องการ context — อ่านเฉพาะ section ที่เกี่ยวกับงานจริง (`grep`/`offset+limit`) ประหยัด token
- จบงานแต่ละก้อน → อัปเดตตาราง "สถานะปัจจุบัน"/"งานที่เหลือ" ในไฟล์นี้ให้ตรง + เขียน/อัปเดต
  memory (`project` type) เก็บเฉพาะ decision/gotcha ที่หาที่อื่นไม่ได้ — **ไม่ log รายละเอียดที่
  derive จาก git log/docs ได้อยู่แล้ว**

---
*อัปเดตล่าสุด: 8 ส.ค. 2569 (**รีแบรนด์เฟส 2 — ฟอนต์ เสร็จทั้งเฟส ยังไม่ push (5 commits)** · **รอบ 3 เก็บรายละเอียดหน้าเว็บตามที่เจ้าของตรวจ:** การ์ดราคาหน้าแรกรวมเป็นบรรทัดเดียว · **`WaitlistForm` สีช่องกรอกตามพื้นที่วางอยู่ผ่าน prop `on` — ใช้สองที่ที่พื้นต่างกัน ฮาร์ดโค้ดสีเดียวได้ช่องล่องหนที่อีกฝั่งเสมอ (เกิดมาแล้วทั้งสองฝั่ง)** · `<select>` เลือกฟอนต์ใน `/quote` cap 420px เพราะ**ป๊อปอัพกว้างตามตัว select เสมอ สั่ง CSS ที่ `<option>` ไม่ได้** · `LABEL` ฟอร์มสาธารณะเป็น `grey-600` ตรงกับแดชบอร์ด (แก้จุดเดียวเปลี่ยนทุกฟอร์ม) · ปุ่มล้างตัวกรอง `ghost`→`outline` · **ตรึงแถบกรอง `/fonts` ใต้ nav — `bg-page` จำเป็น ไม่งั้นการ์ดทะลุขึ้นมาซ้อน และห้ามมี `overflow-*` ที่บรรพบุรุษ ไม่งั้น sticky ตายเงียบ ๆ** · เก็บ `desktop/src/App.tsx` ให้ตรงกติกา + **ยืนยัน desktop ด้วยตาแล้วผ่าน `vite dev` พอร์ต 1420** (Rust ยังไม่มี แต่ฟอนต์/สีวัดได้ครบ) · **ที่ยังค้างของเฟส 2: ยังไม่ push · ยังไม่ได้ตรวจหน้าหลัง login ด้วยตา · เอกสาร PDF คงเป็น Noto โดยเจ้าของเคาะ** · หนี้เล็กที่ยังไม่เก็บ: token `logo-sub` ตาย (0 call site) · แถบเลขหน้า `/fonts` ไม่มี `…` ย่อ · ไฟล์ IBM Plex TTF 860KB ใน `docs/design/` ไม่ได้ใช้แล้ว · **รอบ 2 หลังเจ้าของตรวจของจริง: ถอย typedee เหลือแค่หัวข้อใหญ่ (hero/font-slug/h1) เพราะ "บุคลิกฟอนต์มันจัดเกินไป" — h2 ลงมาทั้งหมดเป็น Plex Bold รวมการ์ดฟอนต์/badge/ปุ่ม/nav/label** · ทำโดยเปลี่ยน `font-ui` ใน config ให้ชี้ Plex แล้วกวาด `font-heading` → `font-ui` ทุกบรรทัดที่ไม่ได้คู่กับหัวข้อใหญ่ · **🔴 กับดักตอนกวาด: prune ชื่อโฟลเดอร์ `fonts` ใน `os.walk` ไปตัด `src/app/fonts/` ทิ้งด้วย** จับได้เพราะวัด `getComputedStyle` ไม่ใช่เพราะดูรูป · ถอด typedee Bold ออกเพราะไม่มีใครใช้แล้ว — **`next/font/local` preload ทุกไฟล์ใน `src` ไม่ว่าใครใช้จริงหรือไม่** → payload เหลือ 119KB น้อยกว่าก่อนรีแบรนด์ · **แก้ vertical metrics ของ typedee ให้ตัวอักษรอยู่กลางกล่อง** (`920/-280/gap0` คงผลรวมเดิม 1200 ความสูงบรรทัดไม่เปลี่ยน) · nav จอแคบเรียงใหม่ สามขีดซ้าย/wordmark กลาง/ตะกร้าขวา · ก่อนหน้าในวันเดียวกัน: หัวข้อ/ปุ่ม/ป้าย = `typedee` · เนื้อความ = IBM Plex Sans Thai Looped ทั้งไทยและละติน · **เอกสาร PDF ไม่แตะ** เจ้าของเคาะให้เฟส 2 ทำเฉพาะหน้าเว็บ · **typedee ส่งแค่ Bold 700 + Black 900 ไม่ส่ง Regular** เป็นมาตรการกันฟอนต์หลุด + `fsType=4` + license string · ไฟล์ .woff2 อยู่ `src/fonts/` ให้ `next/font/local` จัดการ **ห้ามวางใน `public/`** · **🔴 IBM Plex Sans Thai Looped ไม่มี variable font** ต้องประกาศ `weight` ตรง ๆ ไม่งั้น build ล้ม (ต่างจาก Noto เดิมที่จงใจละไว้ได้) · **🔴 คลาส `font-heading`/`font-ui` ที่ไปโผล่ที่น้ำหนัก 300/400 เบราว์เซอร์เลือก 700 ให้เงียบ ๆ ไม่มี error** — เก็บครบ 22 จุดแล้ว · หัวข้อ remap 800→900 (hero/font-slug/h1) และ 800→700 (h2) เพราะ typedee ไม่มี 800 · แก้ค่าใน config ที่เดียว call site 856 จุดไม่ต้องแตะ · เก็บบั๊กที่มีอยู่ก่อน: ยอดรวมตะกร้าใช้ `text-h3` ซึ่งเป็นคลาสตาย · วัดด้วย `getComputedStyle` บนเบราว์เซอร์จริงทุกบทบาท ไม่ได้ดูด้วยตาอย่างเดียว · **ยังไม่ได้ตรวจหน้าหลัง login ด้วยตา** · ก่อนหน้าในวันเดียวกัน: **รีแบรนด์เฟส 1 เสร็จ + push ขึ้น production แล้ว** — `286c6c6` · **กับดักที่กัดซ้ำในรอบนี้ จำไว้ก่อนแตะ UI:** (1) **`cn()` ไม่ merge class ที่ชนกัน** — ต่อ `text-black` ทับคลาสรวมที่มี `text-page` อยู่ข้างในแล้วแพ้เสมอ เพราะ Tailwind ปล่อย `.text-page` หลัง `.text-black` → ต้องแยกสีออกจากคลาสรวมแล้วเลือกครั้งเดียวที่จุดเรียกใช้ (กัด 2 ครั้ง: ป้าย Admin เทาบนเทา · เมนู "ฟอนต์" หายตอนเปิด submenu) · (2) **`{/* comment */}` วางเป็นตัวแรกหลัง `? (` หรือ `&& (` ไม่ได้** parser มองเป็น object literal แล้ว JSX พังทันที (กัด 3 ครั้ง) · (3) **สถานะ "เลือกอยู่" ที่เปลี่ยนพื้นเป็นดำ ต้องพลิกสีลูกครบทุกตัว** รวมป้ายที่ฝังในแถว/ปุ่ม radio/ลิงก์ ไม่งั้นดำบนดำโดยไม่มี error · **บั๊กการเงินที่เก็บไปด้วย: ใบเสร็จ retail เคยพิมพ์ยอดน้อยกว่าที่ Stripe เก็บจริง** เพราะ renderer หัก ณ ที่จ่าย 3% เสมอ — ตอนนี้คุมด้วย flag `withholding` (ปริยาย true = B2B · retail ส่ง false) · ก่อนหน้าในวันเดียวกัน: **รีแบรนด์เฟส 1 — palette ส้ม typedee เสร็จ** · พลิกขั้วพื้นหลัง page F0F0F0 / surface FFFFFF (แก้ค่าใน config ที่เดียว call site 170 จุดไม่ต้องแตะ) · ปุ่มพื้นดำ hover ส้ม ตามที่เจ้าของสั่ง · **ตัวอักษรบนพื้นส้มต้องเป็นดำเสมอ ขาวได้แค่ 3.33:1** · `text-mint-text` 80 จุดแยกตามบทบาท ไม่ใช่แทนที่ด้วยสีเดียว — ลิงก์ไป `.link-accent` อักษรดำ+เส้นใต้ส้ม ส่วนตัวเน้นเล็กกว่า 24px ต้องเป็นดำเพราะ E03C00 ตกเกณฑ์จริง · **กับดัก: `\btext-mint\b` กิน `text-mint-text` · `bg-mint` ในternary ที่ลูกเป็น text-black ตายตัว สลับเป็นดำแล้วดำบนดำ** · sidebar พื้นดำ "เลือกอยู่" ต้องเป็นเม็ดเทาไม่ใช่ดำ · เก็บบั๊กที่มีอยู่ก่อน: ช่องกรอก WaitlistForm มองไม่เห็นมาตลอด · **Tailwind ไม่ error เมื่อ token หาย grep คือหลักฐานเดียว** · ยังไม่ push · เฟส 2 (ฟอนต์ typedee + IBM Plex) ยังไม่เริ่ม) · ก่อนหน้า 4 ส.ค. 2569: (**สถานะฟอนต์ใหม่ "Subscription Exclusive" + ถ่วงน้ำหนักส่วนแบ่ง subscription ใหม่ทั้งสอง pool** — `0083`–`0085` apply แล้ว · `is_sub_exclusive` เป็น flag ที่สอง **ไม่ใช่ตัวแทน `is_subscription`** (subset ของกัน) · ตัวคูณ 1.25 ต้องเข้าก่อน normalize เสมอ ไม่งั้น pool บวมแล้วส่วนเกินไปโผล่เป็น `platformAmount` ติดลบเงียบ ๆ · pool 12% เปลี่ยนเป็นเฉลี่ยตามวันจริงผ่านตาราง `font_subscription_periods` + trigger (เดิมเข้าแผนวันที่ 30 ก็ได้เต็มเดือน และตัวเลขเดือนเก่าขยับทุกครั้งที่มีคน toggle) · ด่านกันขายรายชุดอยู่ที่ `checkout-service` ไม่ใช่แค่ซ่อนปุ่ม · **กับดักที่เจอระหว่างทาง: `subscription_month_data` เรียกซ้ำใน transaction เดียวไม่ได้** (temp table `on commit drop` ชน — ไม่กระทบ production เพราะ client เรียกครั้งละ request) · **`cn()` ไม่ merge class ที่ชนกัน** ส่ง `className="text-grey-600"` เข้า `<Button>` แล้วสีไม่เปลี่ยนจริง (วัดในเบราว์เซอร์) ต้องใช้ `<button>` ตรง ๆ · ป้าย FREE เป็นตัวขาวบนพื้น mint (1.89:1) = **เจ้าของสั่งเองหลังเห็นตัวเลข ไม่ใช่ regression** · ก่อนหน้าในวันเดียวกัน: หน้าสถิติกดหัวคอลัมน์เรียงลำดับได้ · ปุ่มแชร์ในตารางฟอนต์ + แยก `ui/ShareMenu` · สร้างโปรโมชั่นจากในตารางฟอนต์ของฉัน · เลิกนับโปรที่หมดอายุ (เดิมเช็ค `is_sale` ตรง ๆ ซึ่งไม่รู้จักวันหมดอายุ) · **ฟอนต์ทดสอบที่ซ่อนอยู่ 4 ตัว ลบได้จริงแค่ 3 — `TEST 02` ติด `order_items` FK RESTRICT** ต้องลบ order ก่อน หรือรอล้างพร้อมข้อมูลทดสอบ · ลบแถวใน DB ไม่ได้ลบไฟล์ใน Storage) · ก่อนหน้า 1 ส.ค. 2569: (**เพิ่มหัวข้อ "แผนถึง go-live + ประมาณการเวลา"** — critical path คือ จดบริษัท → **เปิดบัญชีธนาคารนิติบุคคล (ตัวที่แกว่งที่สุด ไม่ใช่การจดบริษัท)** → Stripe ในนามบริษัท → DNS → Stripe ขั้นที่ 3 · ประมาณ **พร้อมเปิดขาย 8–15 ก.ย. 2569** · **โค้ดไม่ใช่คอขวด** งานที่เหลือ ~3–5 รอบงาน ใส่ในช่องว่างระหว่างรอเอกสารได้หมด · สิ่งที่เคยดูเหมือนบล็อกแต่ไม่บล็อก: คำตอบ CPA (รอบจ่ายแรก ต.ค.) · จด VAT · เว็บสตูดิโอ dhammadha.com · ก่อนหน้าในวันเดียวกัน: **สัญญาอนุญาตฉบับของดีไซน์เนอร์ใช้ได้จริงครบเส้นทางแล้ว** — ทดสอบสดบน production: 552 Studio ตั้งสัญญาเอง → ตะกร้า/lightbox/หน้าจ่ายเงิน Stripe ถูกทุกจุด · **กับดักที่ปิดประเด็นแล้วอย่าลองซ้ำ: ข้อความยอมรับบนหน้า Stripe ขึ้นบรรทัดใหม่ไม่ได้เลย** (`<br>` ถูก escape · markdown list ไม่ทำงาน · `\n`/hard break/U+2028 ถูก CSS ยุบ — พิสูจน์ด้วย probe session + อ่าน DOM หน้าจริง) ใช้ ` • ` คั่นแทน · **`\n` เป็นกับดักซ้อน: ดูเหมือนไม่พังแต่ยุบเป็นช่องว่างเปล่า ประโยคติดกันไม่มีตัวคั่น** · **🔴 อัปไฟล์สัญญาของดีไซน์เนอร์พังมาตั้งแต่ `0052`** — path `license-pdf/<uuid>.pdf` ไม่เคยตรงกับ policy ที่ตรวจ `split_part(name,'.',1)=auth.uid()` แก้เป็น `<uuid>.pdf` · ปิด "ตั้งค่าสิทธิเอง" แล้วไม่ล้าง tiers/PDF ทิ้งอีกแล้ว (พร้อมตามไปแก้ `email-service` ที่อ่าน `license_pdf_url` โดยไม่ดู `use_default`) · ช่องเลขเอกสารในตารางใบเสนอราคา 80px → 120px · ก่อนหน้า: **เก็บหนี้ทางเทคนิค 3 ก้อน** — `designerLicensePdf()`/`LicenseLink` รวมเกณฑ์สัญญาอนุญาตที่เคยคัดลอกกัน 4 ที่ให้เหลือจุดเดียว · **ตะกร้า + `custom_text` บนหน้าจ่ายเงิน Stripe แสดงสัญญาของแต่ละร้านแล้ว** (เดิมชี้ `/agreement` ตายตัว ทั้งที่หลักฐานการยอมรับเกิดที่หน้า Stripe) — เคสทุกร้านใช้ฉบับกลางข้อความเหมือนเดิมเป๊ะ · **`supabaseSelectAsService` ส่งแค่ `apikey` = พังจริงที่เครื่องมาตลอด** (ไม่ใช่แค่ "หนี้ที่รอวันพัง" อย่างที่เคยจด) แก้เป็นใส่ key ทั้งสอง header + `diag` · **⚠️ เส้นทาง PDF ของดีไซน์เนอร์ยังไม่ได้ทดสอบสด — DB ยังไม่มีใครตั้ง `use_default=false`** · ก่อนหน้าในวันเดียวกัน: **Stripe ขั้นที่ 2 ผ่านครบ** + ศัพท์สิทธิ์ใช้คำเดียวกันทั้งเส้นทาง ("สิทธิการใช้งานส่วนบุคคล" · FontDetail = "บุคคลธรรมดา") · อีเมลไม่ขึ้น "เรียน คุณ " ห้อยลอยกับลูกค้า PromptPay อีกแล้ว — ซื้อจริงบน production 2 ใบ เงิน→order→สิทธิ์→อีเมล ถูกต้องหมด · **กับดักใหญ่: Supabase secret key แบบใหม่ไม่ใช่ JWT ต้องอยู่ใน `apikey` ด้วย ไม่งั้น PostgREST ตอบ PGRST301 — ทดสอบที่เครื่องไม่เจอเพราะ `.env.local` เป็น key แบบเก่า** · แก้จุดที่กลืน error ทิ้งใน `email-service.ts` แล้ว · ก่อนหน้า: เก็บงานที่ไม่ติดบล็อกชื่อแบรนด์/บริษัท — ตั้ง `custom_text` หน้าจ่ายเงิน Stripe **ยืนยันจากหน้าจริงว่ารองรับลิงก์ markdown และ `custom_text` ลบข้อความ default ทิ้งทั้งก้อน ลิงก์ในข้อความจึงห้ามถอด** · ซ่อนฟอนต์ทดสอบ **4 ตัว** (ไม่ใช่ 2 ตามที่เคยจด) เหลือบนเว็บ 22 ตัว · `scripts/reset-test-data.sql` พร้อมแต่ยังไม่รัน · กับดัก `order_items.font_id = ON DELETE RESTRICT` · ในวันเดียวกัน: หัก ณ ที่จ่าย 3% + ใบ 50 ทวิ — สวิตช์เดียว `LEGAL_ENTITY_IS_JURISTIC` · `payouts.amount` เป็นยอดเต็มไม่ใช่สุทธิ · ใบ 50 ทวิกรอกฟอร์มราชการจริง · 🔴 3 เตรส หรือ 40(3) รอ CPA · ก่อนหน้า 31 ก.ค.: ทบทวนเอกสารกฎหมายทั้งชุดเทียบ EULA มาตรฐานตลาด — `/agreement` 7→15 ข้อ · **เคาะฐานะทางบัญชี = ตัวการ (principal) ซึ่งกลับทางจากแผนเดิมที่จะเขียนแบบตัวแทน** · เงินได้ 40(3) หัก 3% · `npm run legal:freeze` · `EFFECTIVE_DATE` จุดเดียว · ก่อนหน้า: เอกสารทุกใบยึดพิกัดจากไฟล์ต้นแบบ Illustrator — `doc-layout.ts` เป็นตัวเดียวที่คุมเลย์เอาต์ · เพิ่มใบแจ้งหนี้ + สวิตช์เปิด/ปิดระบบใบเสนอราคาต่อ designer (`quote_enabled`, 0072) บังคับถึงระดับ RPC · ก่อนหน้า 30 ก.ค.: โครงสร้างนิติบุคคล — จดบริษัททำแพลตฟอร์มอย่างเดียว จ่าย 75% ให้ธรรมดาสตูดิโอในนามบุคคล · ราคารวม VAT · Stripe Connect ไม่เอา · หัก ณ ที่จ่าย = ต้องมี · 29 ก.ค.: แยกแบรนด์แพลตฟอร์ม `src/lib/brand.ts` — รอเคาะชื่อ · อยู่กับ Stripe · 26 ก.ค.: ต่อ Stripe จริง + ทดสอบขั้นที่ 1 ผ่าน · ตะกร้าหลายฟอนต์ + `order_items` 0069–0071)*
