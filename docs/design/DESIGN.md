# DESIGN.md — ระบบดีไซน์หน้าสาธารณะ DHAMMADHA STUDIO

> **เอกสารนี้คือแหล่งความจริง** ถ้าเอกสารนี้กับ Claude Design project ขัดกัน — **รีโปชนะ**
>
> ที่มา: moodboard จาก Figma ใน [`moodboard/`](./moodboard/) — โดยเฉพาะ [`style.png`](./moodboard/style.png) (Figma Styles panel) ซึ่งให้ type scale กับ color styles มาตรง ๆ

---

## 0. ย้อนกลับเมื่อพัง — อ่านก่อน

**ถ้า production พัง: ใช้ Cloudflare Pages deployment rollback** ใช้เวลา ~30 วินาที
**อย่าคว้า `git revert`** — มันช้ากว่า และต้องรอ build ใหม่

git tag มีไว้ **rebuild ของเดิม** ไม่ใช่เครื่องมือกู้ production:
- `v1-pre-redesign` @ `36fa60b` = ดีไซน์เดิมทั้งหมดก่อนรื้อ
- กู้ดีไซน์เก่าทีละไฟล์: `git checkout v1-pre-redesign -- src/components/Footer.tsx`
- แต่ละ phase มี tag ของตัวเอง (`redesign/p2-tokens`, …)

---

## 1. ข้อตกลงที่แช่แข็ง (ห้ามละเมิดโดยไม่ถามเจ้าของ)

| กฎ | เหตุผล |
|---|---|
| **ขอบเขต = หน้าสาธารณะเท่านั้น** | dashboard admin/designer อยู่บนดีไซน์เก่า และต้องอยู่ต่อจนกว่าจะมีรอบของมันเอง |
| **`src/components/Button.tsx` แช่แข็ง** | 26 ไฟล์ import อยู่ ~15 เป็น admin/designer → แก้ = ดีไซน์ dashboard เปลี่ยนตาม |
| **`globals.css` บรรทัด `body { font-family }` ห้ามแตะ** | ฟอนต์ถูกใส่ที่นั่น**ที่เดียว** สลับเป็น Looped ตรงนั้น = admin เปลี่ยนฟอนต์ทั้งหมด |
| **ไม่มี dark mode** | โปรเจกต์ไม่มี `dark:` สักจุด เพิ่มคือฟีเจอร์ที่ไม่มีใครขอ |
| **ไม่เพิ่ม dependency** | `cn()` เขียนเองพอ ไม่ต้อง clsx/cva |
| **ห้ามแตะ logic คิดราคา / flattenFont / handleBuy** | ดู §8 |
| **ทุกฟังก์ชันต้องทำงานเหมือนเดิมเป๊ะ** | นี่คือรอบดีไซน์ ไม่ใช่รอบฟีเจอร์ |

---

## 2. Typography

### 2.1 ตระกูลฟอนต์

| บทบาท | ฟอนต์ | ใช้กับ |
|---|---|---|
| **Heading** | Noto Sans Thai | หัวข้อทุกระดับ, ชื่อฟอนต์บนการ์ด, ราคา |
| **Body** | **Noto Sans Thai Looped** | เนื้อความ, คำอธิบาย, ป้ายกำกับ |
| **UI** | Noto Sans Thai | ปุ่ม, nav, control, input |

- โหลดผ่าน **variable font ทั้งคู่** (ละ `weight` ใน `next/font/google`)

> **⚠️ แก้คำอ้างที่ผิด — วัดจริงตอนลงมือ Phase 2**
>
> แผนเดิมเขียนว่า *"เปลี่ยนไป variable แล้วเพิ่มฟอนต์ที่สองยังเบากว่าเดิม"* — **ผิด**
>
> `next/font` **resolve ไป variable font ให้อยู่แล้วตั้งแต่แรก** แม้โค้ดเดิมจะประกาศ 9 static weights ไว้ก็ตาม → build ทั้งสองแบบได้ไฟล์ขนาด **26/15/29 KB เท่ากันเป๊ะ** แปลว่า**ไม่เคยมีความสิ้นเปลืองให้ประหยัด** สมมติฐาน "โหลด 9 weights ทิ้งไว้" ผิดตั้งแต่ต้น
>
> | | ขนาด | ไฟล์ |
> |---|---|---|
> | ก่อน — Noto Sans Thai อย่างเดียว | **71 KB** | 3 |
> | หลัง — + Noto Sans Thai Looped | **140 KB** | 6 |
> | | **+68 KB (เท่าตัว)** | |
>
> **นี่คือราคาของการตัดสินใจให้ body เป็น Looped** — เป็น woff2, cache ได้, `display: swap` การละ `weight` ยังถูกต้อง (โค้ดตรงกับสิ่งที่เกิดขึ้นจริง) แต่**ไม่ได้ให้ส่วนลด**
>
> ถ้าวันหน้าอยากลด: ตัด subset `latin`/`latin-ext` ของ Looped ทิ้งได้ (เนื้อความไทยไม่ต้องใช้) — ประหยัดได้ ~41KB แต่ต้องตรวจว่าไม่มีเนื้อความละตินปนอยู่
- CSS var: `--font-noto-thai` (ชื่อเดิม คงไว้) + `--font-noto-thai-looped` (ใหม่)
- **`Noto Sans Thai UI` ตัดทิ้ง** — ไม่มีใน allowlist ของ `next/font/google` (Google Fonts API เสิร์ฟได้ แต่ next/font ไม่รู้จัก) UI text ใช้ Noto Sans Thai แทน

### 2.2 น้ำหนัก

- **Heading ใช้ 700 (bold) กับ 800 (extra bold) เท่านั้น**
- **ลำดับความสำคัญมาจาก*ขนาด* ไม่ใช่น้ำหนัก** — ห้ามใช้ weight ไล่ชั้น
- ⚠️ ทั้งแอปตอนนี้ใช้แค่ 600/500/400 — **ไม่มี 700/800 ที่ไหนเลย** น้ำหนักหัวข้อคือของใหม่จริง

### 2.3 Type scale — ตรงจาก Figma Styles panel

line-height ทุกตัวเป็น `Auto` ใน Figma = ใช้ metrics ของฟอนต์เอง → **ในโค้ดปล่อยค่าเดิม อย่าไปกำหนด** (ตกลงกันว่าค่อยปรับถ้าเจอปัญหาจริง)

| ชื่อ | ขนาด | ฟอนต์ / น้ำหนัก | ใช้ที่ |
|---|---|---|---|
| `hero` | **96** | Heading / 800 | ตัวใหญ่สุด — สงวนไว้ |
| `font-slug` | **48** | Heading / 800 | ชื่อฟอนต์บนหน้ารายละเอียด |
| `h1` | **40** | Heading / 700 | หัวข้อหมวด ("ฟอนต์ล่าสุด") |
| `h2` | **24** | Heading / 700 | หัวข้อรอง |
| `body` | **16** | **Body (Looped)** / 400 | เนื้อความ |
| `body-sm` | **14** | **Body (Looped)** / 400 | ป้ายกำกับ, ตัวเล็ก |
| `ui` | **16** | UI / 500 | ปุ่ม, nav, control |
| `fg-heading` | **16** | Heading / 700 | หัวคอลัมน์ footer |

### 2.4 พื้นล่างที่ห้ามละเมิด

- **ตัวหนังสือห้ามต่ำกว่า 14px** — ปัจจุบันมี `9px`/`10px`/`11px` อยู่ 16 จุด และ `12px`/`13px` อีก 63 จุด **ทั้งหมดต้องหายไป**
- เหตุผล: อักษรไทยมีสระบน-ล่าง ที่ 9-11px มันตีกันจนอ่านไม่ออก — **ความ "ดูอึดอัด" ของเว็บตอนนี้มาจากตรงนี้พอ ๆ กับเรื่องสี**

### 2.5 Responsive type

`hero` 96 กับ `font-slug` 48 บนจอ 375px **ล้นแน่นอน** — ทุก step ตั้งแต่ `h1` ขึ้นไปต้องมีค่าที่มือถือ:

| ชื่อ | 375 | 768 | 1280+ |
|---|---|---|---|
| `hero` | 40 | 64 | 96 |
| `font-slug` | 28 | 36 | 48 |
| `h1` | 24 | 32 | 40 |
| `h2` | 20 | 22 | 24 |
| `body` / `body-sm` / `ui` / `fg-heading` | เท่าเดิมทุกจอ | | |

---

## 3. Color

### 3.1 Palette — 5 ตัวตาม Figma

**เจตนา: ใช้ชื่อตาม Figma ไม่แปลงเป็น role name**

> หมายเหตุ: ตอนวางแผนเคยเสนอให้เลิกใช้ชื่อสี (`navy`/`mint`) แล้วใช้ชื่อตามบทบาท (`accent`/`surface`) — **กลับคำ** เพราะ (ก) palette มีแค่ 5 สี ระบบเล็กเกินกว่าจะคุ้มกับการมีสองคำศัพท์ (ข) Figma เป็นแหล่งความจริงด้านดีไซน์ การให้นักออกแบบกับโค้ดพูดคนละภาษาจะทำให้สองฝั่งเลื่อนออกจากกัน (ค) `navy`/`mint` **มีอยู่แล้วในโค้ดด้วยค่าเดิมเป๊ะ** → ไม่ต้องแตะอะไรเลย admin ปลอดภัยฟรี

**✅ ค่าทั้งหมดยืนยันจากเจ้าของแล้ว (2026-07-17)**

| ชื่อ | ค่า | สถานะ | ใช้ที่ |
|---|---|---|---|
| `navy` | `#2B1B3D` | ✅ มีอยู่แล้ว **ค่าเดิม ไม่แตะ** | ตัวหนังสือหลัก |
| `mint` | `#5ECEC8` | ✅ มีอยู่แล้ว **ค่าเดิม ไม่แตะ** | accent, hover ปุ่ม nav, logo, ไอคอนหัวใจ |
| `white` | `#FFFFFF` | ➕ ใหม่ | พื้นหลัก, ตัวหนังสือบนพื้นดำ |
| `black` | `#080808` | ➕ ใหม่ **ยืนยันแล้ว** | **พื้น nav, พื้น footer**, ตัวหนังสือเข้ม, ราคา |
| `grey` | `#808080` | ➕ ใหม่ **ยืนยันแล้ว** | placeholder cover, **ตัวหนังสือรองบนพื้นดำ** (5.07:1 ✅) |

`navy` กับ `mint` มีค่าเดิมอยู่ในโค้ดเป๊ะ → **ไม่ต้องแตะ admin ปลอดภัยฟรี** ที่เพิ่มคือ `white`/`black`/`grey` ซึ่งเป็น token ใหม่ล้วน

### 3.2 Neutral ramp — ปั่นจาก `grey #808080`

Figma มี `grey` ตัวเดียว แต่หน้าเว็บจริงต้องใช้เทาหลายระดับ (ตัวหนังสือรอง, เส้นคั่น, พื้นอ่อน, แถบล่าง footer) และโค้ดตอนนี้มี **เทาดิบ 12 เฉด** (`#aaa` 38 ครั้ง, `#888`, `#ddd`, `#bbb`, `#ccc`, `#999`, `#666`, `#555`, `#444`, `#333`, `#eee`, `#f0f0f0`) ไล่กันเองแบบไม่มีกฎ — `#888` กับ `#999` ต่างกันจนตาแทบแยกไม่ออกแต่ถูกใช้ปนกัน

**ยุบเหลือ ramp เดียว ปั่นจาก `#808080` ของ Figma** — ทุก step เป็นเทากลาง (R=G=B) เฉดเดียวกันหมด ต่างแค่ความสว่าง `#808080` อยู่กลาง ramp พอดี (L 50% เป๊ะ)

| token | ค่า | บนขาว | ใช้ทำอะไร | แทนของเดิม |
|---|---|---|---|---|
| `grey-50` | `#F5F5F5` | 1.09:1 | พื้นอ่อน (แถบล่าง footer) | `#f5f5f2` `#f5f5f5` `#f8f8f6` `#f8f8f8` `#fafaf8` |
| `grey-200` | `#E0E0E0` | 1.32:1 | เส้นคั่น | `#eee` `#f0f0f0` `#f0f0ec` `#ddd` |
| `grey-400` | `#B0B0B0` | 2.17:1 | ไอคอนตกแต่ง — **ห้ามใช้กับตัวหนังสือ** | `#ccc` `#bbb` |
| **`grey`** | **`#808080`** | 3.95:1 | **ตัวหลักจาก Figma** — ตัวหนังสือรอง**บนพื้นดำ** (5.07:1 ✅) | — |
| `grey-600` | `#666666` | **5.74:1 ✅** | **ตัวหนังสือรองบนพื้นขาว** | `#aaa` `#888` `#666` `#999` |
| `grey-800` | `#333333` | 12.63:1 ✅ | ตัวหนังสือเข้มรองจากดำ | `#333` `#444` `#555` |

**⚠️ `grey #808080` บนขาวได้ 3.95:1 — ผ่านเฉพาะตัวใหญ่ ตกสำหรับตัวหนังสือเล็ก** → ตัวหนังสือรองบนพื้นขาวต้องใช้ `grey-600` ส่วน `grey` ตัวจริงเหมาะกับพื้นดำ (ซึ่งตรงกับ moodboard ที่ nav/footer เป็นดำ)

### 3.3 Feedback

ของเดิมกระจายอยู่ — รวบเข้าระบบ:

| token | ค่า | แทนของเดิม |
|---|---|---|
| `success` | `#0A8A84` | `#0a8a84` (27 จุด) |
| `warning` | `#F0C040` | `#f0c040` (ป้าย Sale) |
| `danger` | `#E74C3C` | `#e74c3c`, `#c0392b` |

### 3.4 คอนทราสต์ — คำนวณจริงตาม WCAG 2.1 ไม่ได้ประมาณด้วยตา

| ตัวหนังสือ | บนพื้น | อัตราส่วน | AA 4.5 |
|---|---|---|---|
| `navy` | white | 15.83:1 | ✅ |
| `white` | black `#080808` | 20.03:1 | ✅ |
| `mint` | black `#080808` | **10.62:1** | ✅ |
| `grey` | black `#080808` | 5.07:1 | ✅ |
| `grey-600` | white | 5.74:1 | ✅ |
| `grey-800` | white | 12.63:1 | ✅ |
| **`mint`** | **white** | **1.89:1** | ❌ **ดูด้านล่าง** |

### 3.5 🚨 `mint-text` — หนี้คอนทราสต์ที่รู้ตัวและยอมรับไว้

**`mint #5ECEC8` บนพื้นขาว = 1.89:1** ต้องการ 4.5 — ตกแบบไม่เฉียด `FontCard` ใช้ mint เป็นชื่อนักออกแบบบนการ์ดขาว และ moodboard ก็ทำแบบเดียวกัน

แต่ **`mint` บนดำ = 10.62:1 ผ่านสบาย** → **moodboard ที่ทำ nav ดำแล้วใช้ mint เป็น accent นั้นถูกต้องทางเทคนิค** ปัญหาอยู่แค่ตอนเอา mint ไปวางบนพื้นขาวเท่านั้น

**การตัดสินของเจ้าของ (2026-07-17): ใช้ mint เดิมบนพื้นขาวไปก่อน ถ้าไม่เวิร์คอนาคตค่อยปรับ**

**เพื่อให้ "ปรับไม่ยาก" เป็นเรื่องจริง ไม่ใช่แค่ความหวัง — ตั้ง token แยกไว้ตั้งแต่แรก:**

```
mint-text: #5ECEC8   ← ตอนนี้ = mint เดิม (ตามที่เจ้าของเลือก)
```

- **component ทุกตัวใช้ `text-mint-text` ห้ามใช้ `text-mint` กับตัวหนังสือบนพื้นขาว**
- วันไหนอยากปรับ → แก้ **บรรทัดเดียวใน `tailwind.config.ts`** ทั้งเว็บเปลี่ยนตาม ไม่ต้องไล่แก้ทีละไฟล์
- ถ้าไม่ทำแบบนี้ วันหน้าจะกลายเป็นไล่แก้หลายสิบจุด

**ค่าสำรองที่คำนวณไว้แล้ว ถ้าวันหน้าอยากให้ผ่าน AA:** `#27817C` → **4.65:1 ✅** เป็นเฉดเดียวกับ mint เป๊ะ (H 176.8°, S 53.3%, ลด L เหลือ 33%)

> เกร็ด: โค้ดมี `#0a8a84` ใช้อยู่ **27 จุด** — มันคือ "mint เข้มสำหรับตัวหนังสือ" ที่โปรเจกต์ค้นพบเองไปแล้ว แค่ได้ 4.22:1 **พลาด AA ไป 0.28**

### 3.6 หนี้คอนทราสต์ของเดิม (ไม่ใช่เรื่องที่ดีไซน์ใหม่ทำพัง)

**`#aaa` ที่เว็บใช้เป็นตัวหนังสือรองอยู่ 38 จุด ได้แค่ 2.32:1** → **เว็บตอนนี้ตกมาตรฐานคอนทราสต์อยู่แล้ว** การย้ายไป `grey-600` (5.74:1) แก้เรื่องนี้ไปในตัว

---

## 4. รูปทรง (Shape)

### 4.1 มุม — เหลี่ยม

**ไม่มีมุมมน** ตามที่เจ้าของสั่ง ("ต่อไปเราจะไม่มีมุมมน มันไม่เท่ / เราจะใช้สี่เหลี่ยมเลย")

- **การ์ด, ปุ่ม, อินพุต, พาเนล, ป้าย Sale/FREE/NEW, tag → เหลี่ยมหมด**
- **ของที่กลมจริง ๆ ยังกลม** (`rounded-full`): avatar ใน Nav, ปุ่มไอคอนวงกลม (หัวใจ), จุดบอกตำแหน่ง carousel
- **ลูกศร carousel ใน moodboard ไม่มีวงกลมรองพื้นแล้ว** — เป็นลูกศรเปล่า ๆ บนภาพ (ของเดิมมีพื้นวงกลม `bg-black/20`)

**วิธีทำ: ไม่ต้องแตะ `borderRadius` ใน config เลย** — `border-radius` ใน CSS มีค่าเริ่มต้นเป็น `0` อยู่แล้ว และ Tailwind preflight ไม่ได้ใส่ให้ → **แค่ไม่ใส่ class `rounded-*` ก็เหลี่ยมแล้ว**

> ❌ **ห้ามตั้ง `borderRadius.DEFAULT: 0`** — ตรวจแล้วมี `rounded` เปล่า ๆ อยู่ **8 จุด และอยู่ใน admin/designer ทั้งหมด** (`admin/page.tsx:129-130`, `admin/font-review/page.tsx:282-283`, `designer/(dashboard)/page.tsx:125-126`, `admin/FontForm.tsx:581,583`) หน้าสาธารณะไม่มีเลยสักจุด → ทับ `DEFAULT` = thumbnail กับป้ายใน admin เหลี่ยมตาม = **ผิดขอบเขต**

ของเดิมมี radius **13 แบบ** ปนกัน (`rounded-[9px]`, `rounded-[6px]`, `rounded-xl`, `rounded-2xl`, `rounded-[14px]`, …) → **ยุบเหลือ 2: ไม่ใส่อะไรเลย (เหลี่ยม) กับ `rounded-full` (กลม)**

### 4.2 Spacing

- ยึด scale ของ Tailwind (ฐาน 4px) — **ห้ามใช้ arbitrary spacing**
- ของเดิมมี arbitrary value **474 จุด** ใน 8 ไฟล์ในขอบเขต ตัวเลขนี้ต้อง**ลดลงทุก phase ไม่ใช่เพิ่ม**

### 4.3 Elevation

เหลี่ยม + เงา ต้องคุมมือ ไม่งั้นดูเป็นกล่องลอย — moodboard แทบไม่ใช้เงาเลย ใช้**เส้นคั่น**กับ**พื้นต่างระดับ**แทน

| token | ค่า | ใช้ที่ |
|---|---|---|
| `shadow-sm` | `0 1px 2px rgba(0,0,0,0.04)` | ยกเบา ๆ |
| `shadow-md` | `0 2px 12px rgba(0,0,0,0.08)` | hover การ์ด (ค่าเดิมของ FontCard) |
| `shadow-lg` | `0 8px 32px rgba(0,0,0,0.12)` | modal |

### 4.4 Motion

| token | ค่า |
|---|---|
| `duration-fast` | `150ms` |
| `duration-base` | `250ms` |
| easing | `cubic-bezier(0.25, 0.1, 0.25, 1)` (ค่าเดิมของ carousel) |

---

## 5. Responsive

### 5.1 หลักการ

**Mobile-first เด็ดขาด** — class เปล่า = มือถือ แล้วค่อยเพิ่ม `sm:`/`md:`/`lg:` ขึ้นไป

ตรงข้ามกับของเดิมที่เขียนเดสก์ท็อปก่อนแล้วแปะ breakpoint ทีหลัง ซึ่งเป็นสาเหตุที่ทั้งแอปมี breakpoint แค่ **53 จุด**, `lg:` ใช้ **ครั้งเดียว**, `xl:`/`2xl:` **ศูนย์** และ `FontCard.tsx` — การ์ดที่ซ้ำอยู่ 5 พื้นผิว — **ไม่มี breakpoint สักตัว**

### 5.2 Breakpoint

| ชื่อ | ค่า |
|---|---|
| `sm` | 375px |
| `md` | 768px |
| `lg` | 1280px |
| `xl` | 1536px |

### 5.3 Container

- `max-w-site` = 1200px **คงเดิม**
- **padding ต้องไล่ตามจอ** — ของเดิมใช้ `px-8` ตายตัวทุกจอ ทำให้มือถือ 375px เหลือพื้นที่จริงแค่ 311px

| จอ | padding |
|---|---|
| 375 | `px-4` |
| 768 | `px-6` |
| 1280+ | `px-8` |

### 5.4 กฎ

**ทุก component ต้องระบุพฤติกรรมที่ 375 / 768 / 1280** — component ที่ยังไม่ระบุ **ถือว่ายังไม่เสร็จ**

---

## 6. Component

### 6.1 เกณฑ์ "เสร็จ"

แต่ละตัวต้องครบ **4 อย่าง**:
1. variants
2. states — `default` / `hover` / `focus-visible` / `active` / `disabled` / `loading`
3. พฤติกรรมที่ 375 / 768 / 1280
4. การ์ดใน Claude Design sheet

> **`focus-visible` สำคัญเป็นพิเศษ — ของเดิมไม่มีเลยสักที่**

### 6.2 รายการ (`src/components/ui/`)

| Component | สเปก |
|---|---|
| `Button` | **API เหมือน `Button.tsx` เดิมเป๊ะ** (`variant`/`size`/`as` polymorphism) → ย้ายได้ด้วยการเปลี่ยน import path ; เหลี่ยม ; ฟอนต์ UI |
| `Card` | ฐานของ FontCard + pricing card ; เหลี่ยม |
| `Badge` | Sale / FREE / NEW / tag — **เหลี่ยม** (เดิมเป็น pill) |
| `Input` | รวม search field ของ Nav ; เหลี่ยม |
| `Modal` | ใช้กับ specimen lightbox ใน `FontDetail` **ตัวเดียว** |
| `Container` | จัดการ `max-w-site` + responsive padding (§5.3) ที่เดียว |

### 6.3 สเปกจาก moodboard

**Nav** ([`nav bar.png`](./moodboard/nav%20bar.png), [`button.png`](./moodboard/button.png))
- **พื้นดำ** (ของเดิมพื้นขาว — นี่คือการเปลี่ยนที่ชัดที่สุด)
- logo = สี่เหลี่ยม mint + "DHAMMADHA STUDIO" ขาว หนา
- ลิงก์: ฟอนต์ · สัญญาอนุญาต · ราคาและแผนบริการ — ขาว หนา
- **hover ปุ่ม nav = พื้น mint** ([`button.png`](./moodboard/button.png) — Nav_Button component มี state hover เป็นบล็อก mint เหลี่ยม)
- search box ขาว เหลี่ยม + ไอคอนแว่นขยาย
- ไอคอนคน + **ไอคอนตะกร้า**
- **submenu หมวดหมู่** เปิดตอน hover "ฟอนต์": ทุกหมวดหมู่ | Serif | Sans Serif | Display | Handwriting

**FontCard** ([`font card.png`](./moodboard/font%20card.png))
- cover image ด้านบน, ปุ่มหัวใจ**วงกลม**มุมขวาบน (พื้นขาวโปร่ง)
- `FONT NAME` — Heading หนา ดำ
- `โดย Designer Name` — **mint** (⚠️ ดูปัญหาคอนทราสต์ §3.4)
- `x styles` — grey
- `฿XXX` — Heading หนา ดำ **ใหญ่** มุมขวาล่าง
- เหลี่ยมทั้งใบ
- ⚠️ moodboard **ไม่ได้วาดป้าย Sale/FREE/NEW** แต่ของจริงมี → **เก็บไว้ ทำเป็นเหลี่ยม** (ตัดออก = เสียฟังก์ชัน)

**Heart** ([`heart.png`](./moodboard/heart.png))
- ไอคอน 24×24 ในวงกลม
- default: หัวใจขอบขาว บนวงกลมขาวโปร่ง
- active/faved: **หัวใจทึบ mint**

**Slide / hero** ([`slide.png`](./moodboard/slide.png), [`main page.png`](./moodboard/main%20page.png))
- ภาพเต็มความกว้าง
- **ลูกศรเปล่า ๆ ซ้าย-ขวา ไม่มีวงกลมรองพื้น** (ของเดิมมี `bg-black/20`)

**Footer** ([`footer.png`](./moodboard/footer.png))
- **พื้นดำ**
- logo + **4 บล็อกสี่เหลี่ยมขาว** (social — ของจริงมี Facebook/Instagram อยู่แล้ว)
- 3 คอลัมน์: ผลิตภัณฑ์ / นโยบาย / ช่วยเหลือ — หัวคอลัมน์ = `fg-heading` (16 หนา)
- แถบล่างพื้นอ่อน: "สงวนลิขสิทธิ์ (C) ธรรมดาสตูดิโอ"
- ✅ **โครงตรงกับของจริงเกือบหมด → restyle ล้วน ไม่ต้องเพิ่มอะไร**

---

## 7. หน้าแรก — โครงใหม่

| moodboard | ของเดิม | ทำยังไง |
|---|---|---|
| **hero = สไลด์เต็มจอ** | hero ข้อความ + carousel "คัดสรรพิเศษ" แยกกัน | **ยุบเข้าด้วยกัน** — hero slider ใช้ pool เดิมจาก `buildSliderPool()` **ไม่ต้องแตะ logic** และ**ไม่มีหัวข้อ** |
| ~~ชื่อฟอนต์ในสไลด์~~ | — | **ตัดออก** ตามที่เจ้าของสั่ง — เลี่ยงการต้องโหลดฟอนต์จริงเป็น webfont (ปลอดภัยเรื่องลิขสิทธิ์ฟอนต์ และไม่ต้องเขียน `@font-face` ที่ยังไม่มี) |
| ~~หมวด "คัดสรรพิเศษ"~~ | มี | **hero slider แทนที่มัน** |
| "ฟอนต์ล่าสุด" + กริด | มี | restyle |
| ช่องท้ายกริด = "ดูฟอนต์ทั้งหมด →" | มี (`+N ฟอนต์อื่น ๆ`) | restyle ตาม moodboard |
| ADSENSE | มี | คงไว้ |
| ~~หมวดราคาและแผนบริการ~~ | มี + `SubscriptionPricingCard` | 🚫 **moodboard ไม่ได้วาด แต่ให้เก็บไว้ restyle** — ตัดออก = เสียทางเข้า `/subscribe` และฟอร์ม waitlist |

---

## 8. หนี้ที่จงใจไม่แก้ (Known debt, deliberately unfixed)

**hallmark จะฟ้องเรื่องพวกนี้ และมันจะ*ถูก* และ*ไม่เกี่ยว* — บันทึกแล้วเดินต่อ ห้ามให้เครื่องมือ QA มารื้อเส้นขอบเขต**

| หนี้ | ทำไมไม่แก้ |
|---|---|
| **Ternary คิดราคา 5 กิ่งซ้อนใน JSX** (`FontDetail.tsx:441-473`, `FontCard.tsx:134-145`) | เข้ารหัสลำดับ ฟรี→ลดราคาฟอนต์→โปรรวม→ปกติ→ไม่มี ที่ไม่มีที่อื่นเขียนไว้ ไม่มีเทสต์คอยจับ **โหมดพังคือลูกค้าเห็นราคาผิด** — เสี่ยงสูงสุด ผลตอบแทนต่ำสุด **restyle รอบ ๆ มัน** |
| **`activePromo` parse วันที่ DD/MM/YYYY ตอน render** (`FontDetail.tsx:141-149`) | เปลืองแต่ไม่พัง |
| **`flattenFont`/`RawFont` ซ้ำ 4 ที่** | ไม่มีผลต่อภาพ ; embed ผ่าน view `designer_profiles` มีเหตุผล (migration 0054 บล็อก anon อ่าน `users`) — รวบแล้วเสี่ยง 401 = **ฟอนต์หายทั้งกริด** |
| **`FontDetail.tsx` 689 บรรทัด** | ไม่แตกไฟล์ ; markup ซ้ำที่ `:562-586` ปล่อยซ้ำต่อ (แก้ 2 รอบ น่ารำคาญแต่ถูกต้อง) |
| **modal เขียนเองซ้ำ 4 ตัว** (`PdfLightbox`, `PrintLightbox`, `IssueQuoteModal`, `ConfirmPaidModal`) | ทั้งหมดเป็นของ admin/designer — รวบมันคือ refactor โค้ดนอกขอบเขตที่ใส่ชุดดีไซน์มาหลอก |
| **`Button.tsx` legacy ค้าง `hover:bg-[#f5f5f2]`** | **ถูกแล้ว** — หน้า admin ยังอยู่บนดีไซน์เก่าและควรกลมกลืนกับมัน ลบมันคือท่าปิดของการรื้อ**ทั้งโปรเจกต์** ไม่ใช่รอบนี้ |
| **`font-thai` ใน `tailwind.config.ts:20` เป็น config ตาย** | ไม่มีใครใช้เลย ปล่อยไว้ ไม่ต้องลบ |
| **search เดสก์ท็อป/มือถือใน `Nav.tsx` เขียนแยกกัน** (`:200-265` / `:326-347`) | แชร์ state `suggestions` เดียว — รวบคือ refactor logic ที่เสี่ยงช่องค้นหาทุกหน้า |

### ข้อยกเว้นเดียวที่อนุญาตให้แตะ logic

**`showCount` ของ carousel หน้าแรก** — `page.tsx:18` hardcode `MAX_VISIBLE = 3` ค่านี้คำนวณใน JS ป้อนเข้าสูตร `width`/`transform` inline **แก้ด้วย CSS ไม่ได้** บนมือถือจึงยัด 3 การ์ดใน 375px

เปลี่ยนได้แค่ *วิธีได้มา* ของ `showCount` ให้รู้ขนาดจอ (1/2/3) — **โครงสร้าง clone strip, state `pos`/`animated`, `onTransitionEnd` snap-back, autoplay, สูตร transform/width คงเดิมทุกไบต์**

ที่ปลอดภัยเพราะ `useEffect` ที่ `page.tsx:73-77` **reset `pos` เมื่อ `showCount` เปลี่ยนอยู่แล้ว** → โค้ดถูกออกแบบมาให้ค่านี้เปลี่ยนได้ตั้งแต่แรก นี่คือรอยต่อที่ตั้งใจไว้ ไม่ใช่การแฮ็ก

carousel อีก 2 ตัว (`DesignerDetail.tsx:12-119`, `FontDetail.tsx:188-206`) **ห้ามแตะ** — restyle ที่ชั้น CSS ได้หมด

---

## 9. ประตูรับรอง — ผ่านแล้ว ✅ (2026-07-17)

เจ้าของตรวจบน Claude Design sheet แล้วเคาะครบ:

| # | เรื่อง | ผล |
|---|---|---|
| 1 | `black` | **`#080808`** |
| 2 | `grey` | **`#808080`** (ตรงกับที่ประมาณไว้) |
| 3 | neutral ramp | **ปั่น 6 step จาก `#808080`** (§3.2) |
| 4 | `mint` บนขาวตก AA | **ใช้ mint เดิมไปก่อน** — ตั้ง token `mint-text` แยกไว้ให้ปรับทีหลังได้ด้วยบรรทัดเดียว (§3.5) |
| 5 | `hero` 96 | **เก็บไว้ในระบบก่อน** ยังไม่แน่ใจว่าจะใช้ไหม — ไม่มีที่ลงตอนนี้เพราะ hero กลายเป็นสไลด์ภาพ |

**→ เข้า Phase 2 ได้**

---

## 9.1 การตัดสินที่เกิดขึ้นตอนลงมือ Phase 2

สองเรื่องที่ไม่ได้อยู่ในแผน แต่โผล่มาตอนเขียนโค้ดจริง — บันทึกไว้เพราะทั้งคู่กลับคำจากแผนเดิม

### สีอยู่ใน `tailwind.config.ts` ที่เดียว — `globals.css` ไม่ได้แตะเลย

แผนเดิมเขียนว่าให้ `globals.css` เป็นแหล่ง token เดียว แล้ว `tailwind.config.ts` อ้าง `var(--x)` เพื่อกำจัดการประกาศซ้ำสองที่

**ทำแบบนั้นไม่ได้** — Tailwind คำนวณ **alpha modifier** จาก `var()` ที่เก็บ hex ไม่ได้ ถ้าทำ `black: "var(--black)"` แล้ว **`bg-black/50` · `bg-black/20` · `bg-white/20` · `bg-white/35` จะพังทันที** (ทั้งหมดนี้ถูกใช้อยู่จริงใน `FontForm.tsx`, `FontDetail.tsx`, `DesignerDetail.tsx`, `FontCard.tsx`) ทางที่ถูกคือเก็บเป็น channel (`--black: 8 8 8`) + `rgb(var(--black) / <alpha-value>)` ซึ่งเพิ่มความซับซ้อนโดยไม่ได้อะไรกลับมา

**ทางออกที่ใช้:** สีอยู่ใน `tailwind.config.ts` **ที่เดียว** ส่วน `:root` ใน `globals.css` เก็บแค่ฟอนต์ (ซึ่ง `next/font` ฉีดให้เอง) → **กำจัดความซ้ำซ้อนได้เหมือนกัน โดยไม่พัง และไม่ต้องแตะ `globals.css` เลยสักบรรทัด** = ลดไฟล์เสี่ยงลงอีกหนึ่ง

### `black: #080808` ทับ token ของ Tailwind — ข้อยกเว้นที่จงใจ

Tailwind มี `black` = `#000000` อยู่แล้ว ประกาศทับ = กระทบทั้งแอป **รวม admin**

จุดที่ได้รับผลกระทบ:
- `admin/FontForm.tsx:581,583` — `bg-black/50` (ป้ายลำดับบนรูป preview) **นี่คือ admin**
- `FontDetail.tsx:278,287` · `DesignerDetail.tsx:203-204` — `bg-black/20` (สาธารณะ อยู่ในขอบเขตอยู่แล้ว)

**ยอมรับ** เพราะ `#080808` ต่างจาก `#000000` แค่ 3% และทุกจุดที่ใช้เป็น overlay ที่ opacity 20-50% ทับบนรูปที่ผู้ใช้อัปโหลด → **ความต่างต่ำกว่าที่ตาคนแยกออก** แลกกับการรักษาคำศัพท์ให้ตรงกับ Figma ซึ่งกันไม่ให้ดีไซน์กับโค้ดเลื่อนออกจากกัน

> ⚠️ **ยังไม่ได้พิสูจน์ด้วยตา** — ตอนทำ Phase 2 ระบบรัน Bash/preview ล่มชั่วคราว ต้องยืนยันด้วย screenshot ของ `admin/font-review` เทียบก่อน-หลัง ก่อน tag `redesign/p2-tokens`

## 10. งานที่แยกไป milestone อื่น (ไม่ใช่รอบนี้)

### 🛒 ตะกร้า — หลายฟอนต์ต่อหนึ่ง checkout

**นี่คืองาน money-path ไม่ใช่งานดีไซน์** `checkout-service.ts` ผูกกับฟอนต์เดียวแบบตายตัวทุกชั้น:

| จุด | ตอนนี้ | ต้องเป็น |
|---|---|---|
| `checkout-service.ts:120` | `raw.font_id` string เดียว | array + คิดราคาทีละตัวฝั่งเซิร์ฟเวอร์ |
| `:157-161` | `line_items[0]` index ตายตัว | วนลูป N รายการ |
| `:163` | `metadata[font_id]` เดียว | Stripe metadata ใส่ array ตรง ๆ ไม่ได้ — ต้อง CSV หรือตาราง order |
| webhook `:271,293` | `metadata.font_id` → `p_font_id` **ให้สิทธิ์ฟอนต์เดียว** | วน RPC ทีละฟอนต์ **+ idempotent/atomic** |

**ความเสี่ยงตัวจริง: webhook วนให้สิทธิ์แล้วพังกลางทาง = ลูกค้าจ่ายเงินแล้วได้ฟอนต์ 2 จาก 3 ตัว** ต้องมีแผนกับการทดสอบของตัวเอง ไม่ใช่ขี่มาใน PR ที่วิธีตรวจงานคือถ่ายภาพหน้าจอ

**แต่ไอคอนตะกร้าใส่ไว้ใน Nav ตอนนี้เลย** หลังค่าคงที่ `CART_ENABLED = false` → พรีวิวกับ design sheet เปิดดูได้ แต่**ไม่ ship ไอคอนที่กดแล้วไม่มีอะไรเกิดขึ้น**ให้ลูกค้าเจอ พอ milestone ตะกร้าเสร็จก็พลิกเป็น `true` บรรทัดเดียว

### หน้าที่เลื่อนไปรอบหลัง

`/quote` (612) · `/subscribe` (225) · `auth/*` · `/become-a-designer` · `LegalPage.tsx` · `/agreement` — traffic ต่ำกว่าและเป็นฟอร์มเป็นหลัก ขี่ `Button` เก่าไปก่อนได้

---

## 11. ลำดับงาน

| Phase | ไฟล์ | เสี่ยง |
|---|---|---|
| ~~0~~ | tag `v1-pre-redesign` + branch `redesign/public-v2` | ✅ เสร็จ |
| 1 | `DESIGN.md` + moodboard | ต่ำ |
| 1.6 | Claude Design sheet → **ประตูรับรอง (ห้ามข้าม)** | ต่ำ |
| 2 | `layout.tsx`, `tailwind.config.ts`, `lib/cn.ts` — **เพิ่มอย่างเดียว** · `globals.css` **ไม่ได้แตะ** | ต่ำ |
| 3 | `components/ui/*` — **ไฟล์ใหม่ล้วน ไม่มีอะไรพังได้** | ต่ำ |
| 4 | `Footer.tsx` — **รอบสอบเทียบระบบ** | ต่ำมาก |
| 5 | `FontCard.tsx` — ROI สูงสุด (5 พื้นผิว) | ต่ำ-กลาง |
| 6 | `Nav.tsx` + submenu หมวดหมู่ + `/fonts` อ่าน `?category=` | **กลาง** |
| 7 | `page.tsx`, `fonts/page.tsx`, `DesignerDetail.tsx` | **สูง** |
| 8 | `FontDetail.tsx`, `TypeTester.tsx` | **สูง** |

**merge เข้า main เฉพาะที่ขอบ phase** — ทุก push = deploy ดังนั้นทุก commit ที่เข้า main ต้อง ship ได้เดี่ยว ๆ

---

## 12. การตรวจงาน

`npm run dev` ผ่าน launch config `Next.js Dev` (port 3000) ทุก page phase:

1. **ก่อน:** checkout tag ฐานของ phase → screenshot ที่ **375 / 768 / 1280** — หน้าที่ของภาพนี้**ไม่ใช่**เทียบพิกเซล แต่คือจับ**การสูญหายเชิงโครงสร้าง** (การ์ดหาย, ปุ่มไม่ render) พิกเซลเลื่อน**คือประเด็นของงานนี้**
2. **Console:** `onlyErrors: true` ต้องว่าง — React key warning กับ hydration error คือโหมดพังที่เกิดจริงเวลา restyle ผลลัพธ์ `.map()`
3. **Network:** query Supabase ต้องยังยิงและยังได้แถวกลับ — **นี่คือการตรวจฟังก์ชันตัวจริง** จับโหมด "401 = กริดฟอนต์ว่างเปล่า" ; เช็คด้วยว่าโหลดฟอนต์ **2 ตระกูล** (Noto Sans Thai + Looped) รวม ~140KB
4. **ตรวจระบบ:**
   - ไม่มี horizontal scroll ที่ 375px — `document.documentElement.scrollWidth > window.innerWidth`
   - arbitrary value **ลดลง ไม่ใช่เพิ่ม** — `grep -oE '\[[^]]+\]' <ไฟล์>`
   - ไม่มี `text-[9|10|11|12|13px]` เหลือในไฟล์ที่แตะแล้ว
   - ไม่มี `rounded-*` นอกจาก `rounded-full`
   - `getComputedStyle` ของเนื้อความ = **Looped** ; **และ admin ต้องยังเป็น Noto Sans Thai** (พิสูจน์ว่าไม่หลุดขอบเขต)
   - **`focus-visible` มองเห็นได้ทุก element ที่กดได้**
5. **Interaction:** หน้าแรก — ลูกศร/จุด/autoplay วนครบสองด้าน (`onTransitionEnd` snap-back คือจุดเปราะ ดูว่ากระตุกไหม) **+ ทั้ง 3 breakpoint หลังแก้ `showCount`** ; `/fonts` — filter, favourite ตอน logout ต้อง redirect พร้อม `?next=` ถูกตัว ; `FontDetail` — lightbox เปิด/ปิด/Esc, TypeTester, **ตรวจราคาครบทั้ง 5 กิ่ง**ด้วยฟอนต์จริงในแต่ละสถานะ ; `Nav` — search ทั้งสองความกว้าง, เมนูตาม auth ทั้ง login/logout
6. **ไม่ตรวจ dark** — ไม่มีอยู่
7. **`npm run build` ก่อน tag ทุก phase** — `output: "export"` แปลว่า static export พัง = deploy พัง และไม่โผล่ใน `next dev`

### hallmark / frontend-design

- **frontend-design: หัว phase** (เป็นตัวสร้าง) — Phase 3 และหัวของแต่ละ page phase พอ markup มีแล้วหมดประโยชน์
- **hallmark: ท้าย page phase เป็นประตู ไม่ใช่ระหว่างทาง** — รันบน diff ก่อน tag ; พบในไฟล์ในขอบเขต → แก้ ; พบนอกขอบเขต → **บันทึกใน §8 แล้วเดินต่อ** ; ไม่แตะ Phase 2 (ไม่มีอะไร render ให้วิจารณ์)
