// ค่าคงที่ของเอกสารกฎหมาย — แยกจาก `LegalPage.tsx` เพราะ `license.ts` ต้องใช้ด้วย
// และ `license.ts` ไม่ควรลาก Nav/Footer/Container เข้ามาในกราฟ import

/**
 * วันที่มีผลบังคับใช้ของเอกสารกฎหมายทุกฉบับ — แก้ที่นี่ที่เดียว
 *
 * ⚠️ ก่อนแก้ค่านี้ ต้องรัน `npm run legal:freeze` เพื่อแช่แข็งสัญญาอนุญาตฉบับปัจจุบัน
 * เป็นหน้าแยกก่อนเสมอ ไม่งั้นสิทธิ์ที่ขายไปแล้วจะไม่มีเอกสารฉบับที่ถูกต้องให้อ้างอิง
 * (ดู /agreement ข้อ 14 "การแก้ไขสัญญา")
 *
 * ค่านี้ถูกตรึงลง `quotes.fonts_detail` ตอนออกใบด้วย (ดู `licenseDocLines()`)
 * เอกสารที่ออกไปแล้วจึงชี้ฉบับที่ถูกต้องตลอดไป แม้จะแก้สัญญาภายหลัง
 *
 * ⚠️ `scripts/freeze-agreement.mjs` อ่านค่านี้ด้วย regex — ถ้าเปลี่ยนรูปแบบบรรทัด
 * (เช่น ย้ายไป template literal) ต้องแก้สคริปต์ด้วย
 */
export const EFFECTIVE_DATE = "1 สิงหาคม 2569";
